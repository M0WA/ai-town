## Phase 6: City Assets (Buildings & Vehicles)

### Goal

Deliver all V1 building and vehicle 3D assets, billboard imposters, atlas textures, LOD pipeline, and collision meshes — all passing export validation.

### Deliverables

- [ ] **Phase 6 entry gate (BLOCKING)**: `graphics-artist-3d-model` full `3d-model-standards.md` sign-off must be on record before any Phase 6 building or vehicle geometry is authored. Sign-off must cover all 14 export validation checks, naming convention, pivot convention, 5 mm tolerance, 10-floor hard cap, collision mesh dispatch order, billboard floor count limit, and LOD2 pivot conformance. No `.b3d` mesh files may be committed until this sign-off is recorded. (ref: `architecture/asset-standards/3d-model-standards.md`)

- [ ] **3D Model Standards compliance**: `.b3d` for all building assets (UV2/lightmap); `.obj` only for NOLIGHTMAP props; `-Z Forward, Y Up` Blender export; 1 Irrlicht unit = 1 m; pivot at bottom-center Y=0 (ref: `architecture/asset-standards/3d-model-standards.md`)
- [ ] **LOD requirements** per spec table: Large buildings (2000–5000 / 500–1000 / 300–500 tris LOD0/1/2); Small buildings/props (500–1500 / 100–300 tris / Billboard); Vehicles — the general 'Vehicles (1000–3000 / 200–500 tris)' range row is informational only; the binding caps are: car ≤1,500/≤300, bus/truck ≤2,500/≤450 tris LOD0/LOD1. Per-class caps supersede the general range for all production and validation purposes.; Road tile (≤48/≤16/≤8 tris); Infrastructure props (≤300/≤75/Billboard) (ref: `architecture/asset-standards/3d-model-standards.md`)
- [ ] LOD hysteresis thresholds: Large buildings (50/45 m, 200/185 m); Small buildings/props (30/25 m, 100/90 m); Vehicles (40/35 m, 100/90 m); Road tile uses small building thresholds (ref: `architecture/asset-standards/3d-model-standards.md`)
- [ ] LOD swap for buildings/vehicles: `node->setMesh(newLODMesh)` with `recalculateBoundingBox()` on each `SMeshBuffer` then `SMesh` before `setMesh()`; drop caller ref after `setMesh()`; mesh pointer MUST be typed `SMesh*` (not `IMesh*`) at the `recalculateBoundingBox()` call site — `recalculateBoundingBox()` is on the concrete `SMesh` class, not the `IMesh` interface; `node->getMesh()` returns `IMesh*` and must NOT be used for this purpose; retain the `SMesh*` typed reference from asset load through to the `setMesh()` + `drop()` sequence (ref: `architecture/graphics-architecture/scene-graph-ownership.md`)
- [ ] Billboard imposters: 1024×128 DDS DXT5 sRGB; 8 frames of 128×128; 4-level mip chain (clamped `GL_TEXTURE_MAX_LEVEL=3`); bake at pitch = −45° (45° below horizontal, camera looking downward — NEVER described as 'above horizon'; set in the DCC tool as a negative pitch angle; the sign convention must match Irrlicht's camera pitch range [−70°, −20°]; an incorrect positive pitch produces bakes at the wrong elevation and mismatched mip-3 silhouettes) at 8 angles ×45°; flat ambient-only lighting; straight (unassociated) alpha; `GL_TEXTURE_WRAP_S/T=GL_CLAMP_TO_EDGE`; 8 texel per-cell border; 112×112 usable area per frame; 14×14 px recognizable silhouette at mip 3; billboard LOD only for buildings ≤3 floors; taller buildings require `_lod2.b3d` geometry shell; alpha channel required for non-rectangular vehicle roof silhouette mask (DXT1 would provide only 1-bit alpha, insufficient for smooth silhouettes); no mip chain on vehicle sprite atlas (`GL_TEXTURE_MAX_LEVEL=0`); artist must author alpha channel as vehicle roof silhouette cutout, not a fully opaque rectangle; **billboard atlas upload checks `RenderSystem::isSRGBTextureSupported()`; if false, falls back to `GL_COMPRESSED_RGBA_S3TC_DXT5_EXT` (linear) with shader gamma correction (`pow(color.rgb, vec3(2.2))`) in the billboard fragment shader**; uploaded via the raw-GL sRGB path: `glGenTextures` + `glCompressedTexImage2D` with `GL_COMPRESSED_SRGB_ALPHA_S3TC_DXT5_EXT`; stored in `TextureCache` sRGB pool under `kTexUnitBillboard` (unit 9); `GL_TEXTURE_WRAP_S/T = GL_CLAMP_TO_EDGE` set during the upload sequence; NEVER use `IVideoDriver::getTexture()` for the billboard atlas (ref: `architecture/asset-standards/3d-model-standards.md`, `architecture/asset-standards/2d-texture-standards.md`)
- [ ] Building asset naming: `<zone>_<tier>_<variant>_lod<N>.<ext>` convention; `<asset_name>.meta` with `height_floors`, `category`, atlas cell (ref: `architecture/asset-standards/3d-model-standards.md`)
- [ ] Modular building kit: 4×4×3 m module grid; max 10 floors; per-module caps (wall ≤300/≤75 tris LOD0/1; base ≤400/≤100; roof ≤500/≤125; facade detail ≤100/≤25; max 10 facade details per assembled stack); LOD2 baked shell: ≤500 tris, UV channel 1 non-overlapping, `_lod2_lm.dds` 256×256 DXT5/BC3 uploaded with `GL_TEXTURE_MAX_LEVEL=0`; LOD2 shell uses multiply blend (diffuse × lightmap); LOD2 shell UV channel 0 must map into the same 2048×2048 city building atlas as LOD0/LOD1 modules — UV islands must be authored in atlas cell space, not standalone texture space; export validation check #4 (UV channel 0 in [0,1]) applies to LOD2 files as well as LOD0/1 (ref: `architecture/asset-standards/3d-model-standards.md`)
- [ ] Collision meshes: convex footprint `_col.obj` ≤24 tris; non-convex `_col_0/1/2.obj` ≤24 tris each; circular `_col_circle.obj` N≤8 sides; vehicle `_col.obj` 8 tris; small props (< 4 m radius) 2-tri quad; all side-faces only (no top/bottom caps); authored at Y=0; loader dispatch order: `_col_0` → `_col_circle` → `_col` → error (ref: `architecture/asset-standards/3d-model-standards.md`)
- [ ] Vehicle atlas: 2048×2048 DDS DXT1 (`vehicles_diffuse_atlas_d.dds`); 4×4 grid, 16 vehicle types, each cell 512×512 px; `tools/vehicle_atlas_registry.json`; UV V-flip for OpenGL convention; vehicle normal atlas `vehicles_normal_atlas_n.dds` (2048×2048 DXT5nm, **8×8 grid of 256×256 px cells** — differs from the diffuse atlas's 4×4 grid of 512×512 px cells; Check #12 validates UV mapping against this 8×8 grid). The normal atlas uses a finer grid to accommodate higher-detail normal data per vehicle class. (ref: `architecture/asset-standards/3d-model-standards.md`)
- [ ] Vehicle sprite LOD2: 256×256 DDS DXT5 (`vehicles_sprite_atlas_d.dds`); 16×16 px per vehicle; 1m×0.5m billboard quad (ref: `architecture/asset-standards/3d-model-standards.md`)
- [ ] **2D Texture Standards**: DXT5nm normal map packing (X→alpha, Y→green, Z discarded; Y-flip before swizzle for OpenGL convention); building facade atlas 2048×2048 DXT1 (or 4096 if `GL_MAX_TEXTURE_SIZE ≥ 4096`); 8-texel per-cell border; all non-transparent pixel content within [8, 504] texel range on both U and V axes per cell (496×496 usable zone per 512×512 cell; 8px border on each edge); building atlas mip chain clamped at 4 levels (`GL_TEXTURE_MAX_LEVEL=3`); road marking atlas 1024×1024 DXT5 (4×4 grid, 16 cells, 256×256 each, 4-level mip); anisotropy: terrain ≥8×, roads/props/normal/specular ≥4×, UI=disabled, splat=disabled; `road_asphalt_tileable.dds` 1024×1024 DXT5 sRGB, custom road shader, UV tiles 2× per road tile. **Building facade atlas upload: raw-GL sRGB path — `glGenTextures` + `glCompressedTexImage2D` with `GL_COMPRESSED_SRGB_S3TC_DXT1_EXT`; stored in `TextureCache::m_srgbTextures` (sRGB pool). NEVER use `IVideoDriver::getTexture()` for the building facade atlas.** (ref: `architecture/asset-standards/2d-texture-standards.md`, `architecture/graphics-architecture/texture-cache.md`)
- [ ] `road_asphalt_tileable.dds` (`graphics-artist-2d-texture`): 1024×1024 DDS DXT5 sRGB (`GL_COMPRESSED_SRGB_ALPHA_S3TC_DXT5_EXT`), 4-level mip chain, UV tiles 2×2 per road quad, seamless tiling verified, lane markings in alpha channel, no intersection-specific markings (ref: `architecture/asset-standards/2d-texture-standards.md`)
- [ ] `road_markings_atlas.dds` (`graphics-artist-2d-texture`): 1024×1024 DDS DXT5/BC3, 4×4 grid of 16 cells (256×256 px each), 8 px per-cell border, 4-level mip chain (`GL_TEXTURE_MAX_LEVEL=3`); content: straight lane markings, dashed lane markings, crosswalk stripes, turn arrows (left/right/straight), stop line — minimum 8 unique decal types for V1; upload via linear path (NOT sRGB — decal mask, not diffuse); alpha channel = decal mask; ref: `architecture/asset-standards/2d-texture-standards.md`, `architecture/asset-standards/building-atlas-layout.md`
- [ ] Per-module building specular maps (`graphics-artist-2d-texture`): one `<module_name>_s.dds` per V1 wall module variant, 512×512 DDS BC1 (grayscale roughness), linear space, 4-level mip chain, anisotropy ≥ 4×; hard surfaces (concrete, glass) authored with stronger specular; upload via linear pool (`IVideoDriver::getTexture()`); bound to `kTexUnitSpecular` (unit 2) (ref: `architecture/asset-standards/2d-texture-standards.md`)
- [ ] **Per-module building normal maps** (`graphics-artist-2d-texture`): one `<module_name>_n.dds` per V1 wall module variant, 512×512 DDS BC3 DXT5nm (X→alpha, Y→green; Y-flip before swizzle for OpenGL convention), linear space, 4-level mip chain, anisotropy ≥ 4×. Bound to `kTexUnitNormal` (unit 1). **Sign-off required alongside specular maps before UV authoring begins in Phase 6.** (ref: `architecture/asset-standards/2d-texture-standards.md`)
- [ ] Per-asset building lightmaps (`graphics-artist-2d-texture`): `<asset_name>_lm.dds` for each delivered V1 building variant; small/medium buildings 512×512 DDS DXT5/BC3; large buildings 1024×1024 DDS DXT5/BC3; all uploaded with `GL_TEXTURE_MAX_LEVEL=0` (lightmap exemption — no mip); flat ambient-only bake; UV channel 1 non-overlapping verified before bake begins (per export validation checks #4 and #5) (ref: `architecture/asset-standards/3d-model-standards.md`)
- [ ] Minimum V1 building asset set: at least 2 variants per zone-tier combination (2 variants × 3 zones × 3 tiers = minimum 18 building sets); each set includes LOD0.b3d, LOD1.b3d, LOD2.b3d (large) or billboard (small ≤3 floors); minimum 5 vehicle types (`car_sedan`, `car_hatchback`, `car_suv`, `bus_standard`, `truck_cargo`). **Minimum large-building count**: at least 2 High-density building sets per zone type (R/C/I), totalling at least 6 High-density models, must be delivered in V1. (ref: `architecture/asset-standards/3d-model-standards.md`)
- [ ] **LOD2 pivot conformance sign-off** (`graphics-artist-3d-model`): for each large building type, a DCC screenshot showing (a) two stacked floor modules with no visible join gap in Irrlicht scene view, (b) LOD2 shell silhouette overlaid on LOD1 assembled building at 8 standard bake angles confirming ≤10% area deviation. **This sign-off is a blocking exit criterion — no building asset may be committed to the asset pipeline until its LOD2 pivot has been reviewed and approved.** Sign-off before LOD2 shell commit is mandatory, not advisory. (ref: `architecture/asset-standards/3d-model-standards.md`)
- [ ] Terrain decoration props (rocks, embankments): explicitly deferred to post-V1 — no terrain decoration assets or collision meshes are in scope for V1; export validation script does not check for these files
- [ ] **Billboard bake DCC sign-off** (`graphics-artist-2d-texture`): for each billboard asset type, a DCC screenshot showing a single 128×128 frame downscaled to 14×14 px confirming silhouette recognizability. Bake settings: flat ambient-only lighting, straight alpha, camera pitch = −45°, 8 angles at 45° increments. **Sign-off owned by `graphics-artist-2d-texture` (not `graphics-artist-3d-model`)** — the 2D Texture Artist owns the atlas and bake pipeline. Sign-off required before asset commit. (ref: `architecture/asset-standards/3d-model-standards.md`, `architecture/asset-standards/2d-texture-standards.md`)
- [ ] UI sprite sheet DDS export (`graphics-artist-2d-texture` + `graphics-dev-irrlicht`): the Phase 5 UI sprite sheet art (2048×2048 RGBA8 source PNG) must be exported as a DDS file using `tools/export_textures.py --input <png_path> --output <dds_path> --format rgba8` and uploaded via `glTexImage2D` with `GL_RGBA8`, `GL_TEXTURE_MAX_LEVEL=0`, `GL_TEXTURE_MIN_FILTER=GL_LINEAR`. **Owner**: `graphics-dev-irrlicht` adds the `--format rgba8` path to `export_textures.py` (Phase 6 scope); `graphics-artist-2d-texture` exports the final DDS using this CLI. **`export_textures.py` CLI specification** (full required interface before Phase 6 CI integration): `python tools/export_textures.py --input <path> --output <path> --format <dxt1|dxt5|dxt5nm|rgba8> [--overwrite] [--validate-only]`. Exit code 0 on success, non-zero on any error. `--validate-only` dry-runs all format/dimension checks without writing output. The `validate-assets` CI job invokes this script; missing `--format` flags or ambiguous output paths are CI-breaking defects. (ref: `architecture/asset-standards/2d-texture-standards.md`)
- [ ] VRAM budget compliance: simultaneous scene texture VRAM ≤170 MB (per Phase 6 budget table — this is the working scene budget); total resident texture VRAM ≤1.0 GB ceiling (GPU headroom on 4 GB card). Both constraints must hold simultaneously.; ≤2,000 draw calls per frame; ≤50 unique LOD0/1/2 mesh variants simultaneously (ref: `architecture/asset-standards/2d-texture-standards.md`)
- [ ] `export validation script` (`tools/validate_assets.py`): all 13 required checks (ref: `architecture/asset-standards/3d-model-standards.md`):
  - Check #1: b3d format for building `_lod0`, `_lod1` files
  - Check #2: Small building/prop `_lod2.b3d` absent when `height_floors ≤ 3`
  - Check #3 (large building): `_lod2.b3d` present, within 300–500 tri budget, and `_lod2_lm.dds` uses DXT5/BC3 (not DXT1) — validated by reading the DDS fourCC; DXT1 on a LOD2 lightmap is a silent error that destroys the AO alpha channel
  - Check #4: UV channel 0 coordinates on all LOD levels fall within [0, 1] UV space (applies to LOD0, LOD1, and LOD2 shell files)
  - Check #5: UV channel 1 non-degenerate on all building `.b3d` files
  - Check #6: Assembled LOD0 total ≤ 5,000 tris (large) or ≤ 1,500 tris (small)
  - Check #7: Facade detail piece count ≤ 10 per assembled stack
  - Check #8: Pivot/extent tolerance 5 mm
  - Check #9: LOD hysteresis ≥ 5 m (close), ≥ 10 m (far)
  - Check #10: Vehicle atlas UV
  - Check #11: Small-building height check (`height_floors > 3` requires `_lod2.b3d`)
  - Check #12: Vehicle normal atlas UV
  - Check #13: Facade atlas cell pixels — all non-transparent pixel content within [8, 504] texel range on both U and V axes per cell (496×496 usable zone per 512×512 cell; 8px border on each edge). For DXT1 format (1-bit alpha), 'non-transparent' means DXT1 alpha bit = 1 (opaque). For DXT5/BC3 format, 'non-transparent' means alpha > 0. The check must read the DDS fourCC header to determine which transparency interpretation applies — do not assume full-range alpha for DXT1 files.
  - Check #14: For every file matching `assets/audio/music_*.ogg`, verify `assets/audio/music_*.json` exists and contains valid `bpm` and `beats_per_bar` keys; fail on missing or malformed sidecar
  Run in CI; fail build on any error.
- [ ] `.github/workflows/ci.yml` updated to add a `validate-assets` job (Linux only, `ubuntu-latest`, `timeout-minutes: 10`): installs Python 3, runs `python tools/validate_assets.py`, exits non-zero on any validation error; `validate-assets` added to `all-checks-pass` gate `needs:` list simultaneously with creating the job (must not wait until Phase 9); any third-party actions (e.g., `actions/setup-python`) must be SHA-pinned. **SHA resolution required before Phase 6 begins: `actions/setup-python` must be pinned to a real 40-character SHA (not a placeholder or short SHA). Unresolved SHAs will fail the supply-chain lint step introduced in Phase 0. The `dorny/test-reporter` action SHA must also be resolved to a real pinned value.** **WARNING to implementers: ALL `<ACTION-SHA-REQUIRED>` or short SHA placeholders in `.github/workflows/` MUST be resolved to full 40-character verified SHAs before the `build-linux` job for Phase 6 runs — the supply-chain lint step will fail on any placeholder.** (ref: `architecture/ci-cd/github-actions-workflow.md`, `architecture/ci-cd/caching.md`)
- [ ] sRGB entity lifetime tracking: `std::vector<std::string> m_srgbTextureFilenames` per entity; `releaseSRGB` in `SceneEntityManager::destroy()` Step 1b; LOD swap does NOT call `releaseSRGB` at swap time (only at destroy) (ref: `architecture/graphics-architecture/texture-cache.md`)
- [ ] Road tile LOD2: flat shaded quad using `RenderConstants::road_lod2_color` (no texture at LOD2); road marking decals disabled at LOD2 (ref: `architecture/asset-standards/3d-model-standards.md`). **`road_lod2_color` placement**: this is a RENDERING constant (`irr::video::SColor`) with no simulation significance — it MUST be placed in `src/rendering/render_constants.h` (a new file, NOT in `src/simulation/simulation_constants.h`). Placing it in `SimulationConstants` would create an incorrect `src/simulation/` dependency on a rendering artifact value. **`road_lod2_color` computation owner (`graphics-dev-irrlicht`)**: the constant value must be computed as the average linear RGB of `road_asphalt_tileable.dds` (DXT5 decode → linear-space average → pack as `irr::video::SColor`). This computation must be done before Phase 6 road tile LOD2 rendering is finalized. **CI check**: `tools/validate_assets.py` must include a check (add as Check #15) that reads `road_lod2_color` from `render_constants.h` and verifies it matches the computed average of `road_asphalt_tileable.dds` within a tolerance of ±3/255 per channel; this check is added alongside the Phase 6 `validate-assets` job update.
- [ ] Road tile custom GLSL shader: `addHighLevelShaderMaterialFromFiles()` with `MyRoadShaderCallback` (raw heap `new`, `->drop()` after pass); `OnSetConstants()` saves/restores `GL_ACTIVE_TEXTURE`; binds `road_asphalt_tileable.dds` raw `GLuint` to `kTexUnitDiffuse` (unit 0) via `TextureCache::getSRGBGLuint()`; shader error fallback to `EMT_SOLID` magenta in release; `requires-opengl` integration test confirming no GL errors after a draw call with the road shader active (ref: `architecture/graphics-architecture/shader-loading.md`, `architecture/asset-standards/2d-texture-standards.md`)
- [ ] Building/vehicle LOD swap integration test in `tests/rendering/building_lod_swap_test.cpp`: verify that after `node->setMesh(newLODMesh)`, the scene node's bounding box matches the new mesh's bounding box (not the previous LOD's bounding box); labelled `requires-opengl`; run under `xvfb-run` in CI. **Test geometry specification**: LOD0 mesh: single `SMeshBuffer` with 4 vertices forming a 10 m × 10 m quad at Y=0 (expected bounding box {−5,0,−5} to {5,0,5}); LOD1 mesh: single `SMeshBuffer` with 4 vertices forming a 5 m × 5 m quad at Y=0. **Negative case**: call `setMesh(lod1Mesh)` WITHOUT calling `recalculateBoundingBox()` first; verify the scene node bounding box still reports the LOD0 10 m extents (stale box). **Positive case**: call `lod1Mesh->recalculateBoundingBox()`, then `setMesh(lod1Mesh)`; verify the node reports 5 m extents. Both meshes must have actual geometry — degenerate empty meshes produce identical bounding boxes in both cases and will cause false passes. Register via `target_sources(opengl_tests PRIVATE tests/rendering/building_lod_swap_test.cpp)` — do NOT create a new CMake target. (ref: `architecture/graphics-architecture/scene-graph-ownership.md`)

### Exit Criteria

- Residential/Commercial/Industrial buildings at all three density tiers render correctly in-engine
- Billboard LOD2 transitions are perceptually seamless at 100 m distance
- Export validation script passes all 15 checks (13 asset checks + sidecar check #14 + road LOD2 color check #15) on all delivered assets
- VRAM usage: simultaneous scene VRAM ≤170 MB and total resident VRAM ≤1.0 GB on a representative mid-range test machine
- Draw call count stays under 2,000 per frame in a city with 50 building variants

### Team

| Role | Responsibility |
|---|---|
| `graphics-artist-3d-model` | All building models (LOD0/1/2/billboard), vehicle models, collision meshes, modular kit |
| `graphics-artist-2d-texture` | Building facade atlas, normal/specular maps, vehicle atlas, billboard bakes, road textures, UI sprite sheet DDS export (using `export_textures.py --format rgba8`) |
| `graphics-dev-irrlicht` | LOD swap code, `LODNode`, `BuildingAssetLoader`, export validation script, road custom shader |
| `cicd-dev-github` | Update `.github/workflows/ci.yml` to add the `validate-assets` job and wire it into the `all-checks-pass` gate `needs:` list |

### Dependencies

- Requires Phase 2 complete (TextureCache, sRGB upload path, SceneEntityManager)
- Requires Phase 5 (minimap zone color coding for building placement feedback)
- Requires Phase 2 atlas layout sign-off document (atlas layout joint approval by `graphics-artist-2d-texture` and `graphics-dev-irrlicht` — no building mesh UV channel 0 authoring may proceed until approved). **Building atlas sign-off must explicitly confirm that building variants within the same zone-tier share wall module atlas cells** (e.g., `res_low_01` and `res_low_02` both reference the `wall_residential_low` atlas cell) — the 16-cell atlas is sufficient only if this sharing is enforced. Recorded in `architecture/asset-standards/building-atlas-layout.md`. (ref: `architecture/asset-standards/building-atlas-layout.md`)
- **Pre-condition — `LODNode::swapMesh()` prototype**: `LODNode::swapMesh()` with the full `recalculateBoundingBox()` + `setMesh()` + `drop()` sequence (delivered in Phase 2 LODNode skeleton) must pass the `SetMeshGrabDropContract` test in `tests/rendering/lod_swap_smoke_test.cpp` before any Phase 6 building LOD meshes are produced. No building asset LOD production begins until this test is green. (ref: `architecture/graphics-architecture/scene-graph-ownership.md`)

### Risks & Spikes

- **RISK**: Billboard mip-3 silhouette (14×14 px) may not be recognizable for complex High-density buildings. **Spike**: author artist sign-off checklist item: downscale single frame to 14×14 in DCC tool and verify recognizability before delivery.
- **RISK**: Road tile custom GLSL shader (sRGB texture on `kTexUnitDiffuse`, no `SMaterial::setTexture()`) requires `GL_ACTIVE_TEXTURE` save/restore in `OnSetConstants()`. **Spike**: prototype road tile draw call with shader callback before bulk road tile asset production.
