<?xml version="1.0"?>
<!-- AI Town HUD proportional font — Phase 10 placeholder.
     Replace with a real Irrlicht bitmap font exported from FontTool or bmGlyph
     at 18 px, covering ASCII 32–126, before Phase 10 exit review.
     See assets/fonts/FONT_REQUIRED.txt for authoring instructions.
     The companion texture file hud_font_0.png must reside in the same directory.
     IrrlichtUIBackend loads this file in its constructor via
       m_guiEnv->getFont("assets/fonts/hud_font.xml")
     and falls back to the built-in font if the file is absent or malformed.
     This stub passes the Phase 10 CI asset-presence gate (Step 12e in ci.yml)
     without satisfying the full exit criterion ("loads without error, no
     fallback warning logged"). The real font delivery must replace this file. -->
<Font type="bitmap" Texture="hud_font_0.png" WrongCharacter="32" MaxHeight="18">
</Font>
