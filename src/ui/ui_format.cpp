// ui_format.cpp — implementation unit for ui_format.h.
//
// Both formatDollar() and ratingName() are inline free functions defined in
// ui_format.h; this translation unit exists to satisfy the D-1 / UI-1 spec
// requirement (header + implementation file pair) and to serve as the home for
// any future non-inline UI formatting helpers.
#include "src/ui/ui_format.h"
