# AI Town — Implementation Plan Index

**Date**: 2026-02-21
**Derived from**: All files under `architecture/` (canonical source of truth) and `CLAUDE.md`
**V1 Scope boundary**: Enforced per `architecture/game-design/minimum-viable-simulation.md`. Post-V1 items are explicitly labelled and excluded from phase deliverables.

---

## Spec Contradictions Flagged

The following contradictions were identified during the spec review. They must be resolved before the affected phase can exit. Items marked **RESOLVED** have been closed based on further spec review.

All open contradictions have been resolved. See the Resolved Contradictions list below.

### Resolved Contradictions

1. **[RESOLVED] Source pool slot [57] V1 treatment** — Confirmed: sources[57] in V1 is **idle** — allocated in the 62-source block by `alGenSources(62, ...)` at pool construction but never returned by `acquireSFXSource()` (range 0..54), `acquireStingerSource()` (indices 55..56), or `acquireStreamSource()` (indices 58..61). The phrase "evictable in V1" in earlier spec drafts was misleading. Post-V1 promotion to `StingerType::GAME_OVER` requires only: add `GAME_OVER=57` to StingerType, increment kStingerCount 2→3, extend the stinger setup loop to include index 57 — no pool restructuring. Spec updated in `source-pool.md`. **Closed in Phase 7 spec pre-work (2026-02-20)**.

2. **[RESOLVED] IAudioSystem method signature and count mismatch** — `architecture/testing/testability-architecture.md` updated to match the canonical 11-method `IAudioSystem` in `architecture/audio-architecture/audio-system.md`. Both files now agree: `playSound(SoundId id, SoundPriority priority, float gain = 1.0f)`, `playPositionalSound(SoundId id, vec3 pos, SoundPriority priority, float gain = 1.0f)`, `setTimeOfDay(TimeOfDay tod)`, and `transitionToGameplay()` are all present. `MockAudioSystem` MOCK_METHOD block updated to 11 methods. **Closed in Phase 0 plan update (2026-02-18)**.

3. **[RESOLVED] Emergency Municipal Bond interest rate** — Both `architecture/game-design/economy-model.md` and `architecture/ui-ux/modal-dialog-system.md` agree at 5%/year. The `economy-model.md` states "5% per in-game year (the same unified rate as forced loans)"; `modal-dialog-system.md` states "5%/year (same rate as forced loans — the bond's distinguishing cost is the doubled principal and 24-tick repayment period)". An earlier draft flag referenced "8%" which was a tax rate example ("Residential: 8% → 8.8%"), not an interest rate. No spec change required; the contradiction flag is closed. All code and tests must use 5%/year per `economy-model.md`. **Closed in Phase 12**.

4. **[RESOLVED] Zone loop channel count** — Zone loops (`zone_residential`, `zone_commercial`, `zone_industrial`) are mono positional (1 channel), pre-loaded into a single AL buffer from the SFX pool, and are NOT streamed. All three relevant audio specs are consistent: `streaming-architecture.md`, `v1-audio-asset-manifest.md`, and `audio-asset-formats.md` all agree on mono. An earlier draft flag referencing stereo was based on a misread of the `streaming-architecture.md` kSamplesPerBuffer section. No spec change required; the contradiction flag is closed. **Inline `SPEC CONTRADICTION` block in `phase-10.md` updated to `[RESOLVED]` on 2026-02-20.** Final QA pass in Phase 12.

5. **[RESOLVED] getDemandPressurePct vs getTrafficDemandFactor** — Both methods remain in `ICitySimulation.h` with distinct semantics, confirmed in `architecture/testing/testability-architecture.md` and `src/interfaces/ICitySimulation.h`: `getDemandPressurePct(ZoneType) const` returns the post-combination HUD aggregate demand [0.0, 1.0] (what the player sees in the demand bars); `getTrafficDemandFactor(ZoneType) const` returns the raw traffic-only smoothstep [0.0, 1.0] for Phase 11 save/load serialization. They are semantically distinct and must both be present. **Closed 2026-02-20.**

6. **[RESOLVED] sfx_budget_warn duration — 1–2 s confirmed** — `architecture/audio-architecture/v1-audio-asset-manifest.md` confirms `sfx_budget_warn` as WAV PCM, 1–2 s, non-looping one-shot, −24 LUFS, −1 dBTP. The 6 s minimum applies to engine loops only; non-looping one-shots have no loop fatigue concern. The `sfx_budget_warn` asset is confirmed as a WAV 1–2 s event chime for deficit threshold crossings. **Resolution: 2026-02-20. Closed.**

7. **[RESOLVED] Phase 1 vs Phase 3 ownership of `WallClock.cpp` and `input_event.h`** — `WallClock.cpp` and `input_event.h` are Phase 1 deliverables per `architecture/testing/testability-architecture.md` (IClock section) and the `CameraController` implementation dependency. `phase-3.md` has been updated to replace the `WallClock.cpp` and `input_event.h` authoring entries with VERIFY entries confirming the Phase 1 deliverables are present. Phase 3 does NOT re-author these files. **Closed 2026-02-21.**

---

## Roles

| Role ID | Discipline |
|---|---|
| `graphics-dev-irrlicht` | C++ graphics / Irrlicht engine |
| `sound-dev-opensoftal` | C++ audio / OpenAL Soft |
| `gamedesign-lookandfeel` | Gameplay balance, simulation feel |
| `gamedesign-ux` | UI/UX design |
| `graphics-artist-3d-model` | 3D model authoring |
| `graphics-artist-2d-texture` | 2D texture / atlas authoring |
| `sound-artist-opensoftal` | Audio asset authoring |
| `test-dev-cpp` | C++ testing (GTest + GMock + RapidCheck) |
| `cicd-dev-github` | GitHub Actions CI/CD |
| `proj-manager` | GitHub project sync (milestones, issues, Projects v2 board) |

---

## Phase Overview

| Phase | Name | Primary Deliverables | Team | Status |
|---|---|---|---|---|
| [0](phase-0.md) | Foundations & CI Skeleton | CMake scaffold, `vcpkg.json` (libvorbisfile chosen), FetchContent (GTest v1.14.0, RapidCheck SHA-pinned), `aitown_add_tests()` macro, `src/interfaces/` with `IClock.h`/`ISimulationRNG.h`/`ISimulationPauser.h`, GitHub Actions CI YAML (build-linux/build-windows/coverage-linux/all-checks-pass), supply-chain lint FIRST step, `--fail-under-percent 0` gate, branch protection + bootstrap procedure | `cicd-dev-github`, `graphics-dev-irrlicht`, `test-dev-cpp` | **DONE** |
| [1](phase-1.md) | Irrlicht Device Shell & Camera | `add_library(aitown_render STATIC ...)` mandatory, `RenderSystem` RAII (`EDT_OPENGL`, `Bits=32`, `Vsync=false`), `glewInit()` + `GL_MAX_TEXTURE_SIZE` query (three-tier response), GLEW **five-item atomicity** co-landing (vcpkg.json + CMakeLists + `GLEW::GLEW` before `Irrlicht` + DLL copy rules; `actions/setup-python` SHA resolved to 40-char hex before push — no angle-bracket tokens on shared branches), 8-step canonical frame sequence (OAL-2 rule + mutex contract for GAINHF; `SpeedMultiplier::x3` default confirmed; `UIManager::draw()` inside `drawScene()` — NOT in main loop), `IRenderer` interface (`IrrlichtRenderer.h` forward-declares `UIManager`), minimal `UIManager` stub (4-param constructor; `UIManager.h` forward-declares `IAudioSystem`/`ICitySimulation`/`InputEvent`; `src/ui/ui_constants.h` with 4 toolbar constants), platform `IEventReceiver` adapter (`m_rmbDragActive{false}` + `m_mmbDragActive{false}` explicit init; 3 distinct pass-through categories; scrim LMB/RMB comment; WindowFocusGained/Lost unconditional), `UIScaler` implementation (`VirtualPoint` nested type; 6-param constructor; no live IVideoDriver), `CameraController` (pitch `[-70°,-20°]`; zoom-scaled pan speed formula with 5 constexpr constants; `m_appHasFocus`; nullable seam), `IrrlichtUIBackend` compile target (`getScreenWidth()`/`getScreenHeight()` query device — no literal fallbacks; `static_assert` + `TEST(IrrlichtUIBackendCompileCheck, IsNonAbstract) { SUCCEED(); }` in `integration_tests`), DLL CI: updated grace window procedure (Phase 0 pre-populated hard-fail — five-item atomicity is the single atomic transition; no partial linkage commits to `develop`), `validate-assets` CI stub (four-item atomicity; billboard DDS sRGB via DX10 DXGI format field — NOT FourCC; height_floors conditional; rule (2) explicit 44100 Hz; rule (3) hard-error label; rule (5) ALL music files, ambient excluded; `requires-opengl` routing step in BOTH `build-linux` AND `coverage-linux` — exact same commit as `aitown_render` linkage), camera pitch sign-off gate + remediation path; Phase 9 entry prerequisite forward-reference, `sound-artist-opensoftal` sign-off (5 points incl. d1/d2 ambient bed crossfade design confirmation + Phase 10 perceptual gate; AL_ORIENTATION/cam.up stub documentation), 8 `CameraController` unit tests + 5 `UIScaler` unit tests + integration_tests CTest discovery >= 1, GLEW nm symbol duplication exit gate (immediate resolution — do not wait for Phase 2) | `graphics-dev-irrlicht`, `cicd-dev-github`, `test-dev-cpp`, `graphics-artist-3d-model`, `graphics-artist-2d-texture`, `sound-dev-opensoftal`, `sound-artist-opensoftal`, `gamedesign-lookandfeel` | Planned |
| [2](phase-2.md) | GL Capability, TextureCache Skeleton & Shader Infrastructure | GLEW availability spike (documented in `irrlicht-device-lifecycle.md` "Phase 2 Spike Results" + `render_system.h`), `find_package(GLEW REQUIRED)` + `GLEW::GLEW` linkage, GLEW vcpkg port verification (gh api 200 check), consolidated GL capability guard (`EDT_NULL` pre-check → `glewInit()` two-tier → `GL_MAX_TEXTURE_SIZE` / `GL_EXT_texture_sRGB` / `GL_EXT_texture_filter_anisotropic` queries), `getGPUProgrammingServices()` null check, `TextureCache` skeleton (3 pools: `m_srgbTextures`/`m_splatMaps`/linear, `m_driverType`, `getSRGBGLuint`/`getSplatMapGLuint`, `loadSRGB(path, GLenum format)` stub with dispatch comments, mip dispatch table, `GL_CLAMP_TO_EDGE` TODO for `_billboard`), `src/rendering/shader_constants.h` stub (`kTexUnitDiffuse=0`..`kTexUnitBillboard=9`, `static_assert`), 6 GLSL stub files (`#version 130`, co-landing with `shader_stub_compile_test.cpp`), `shader_stub_compile_test` PASSES under xvfb, `lod_swap_smoke_test.cpp` (`GTEST_SKIP()` body), LOD spike Checkbox A + B | `graphics-dev-irrlicht`, `cicd-dev-github` | Planned |
| [3](phase-3.md) | Interface Layer & Test Infrastructure | `IAudioSystem.h` verified + locked (11 methods; `syncListenerToCamera(const CameraState& cam)` parameter signature explicitly verified by `sound-dev-opensoftal`); `AudioSystem` stub header (no AL includes; BEHAVIORAL CONTRACT; frozen member names; `m_occlusionGainTarget` std::atomic<float>[]); `audio_types.h` extended (constants → `StingerType` → static_asserts; `kZoneLoopMaxPreloadDurationSeconds`); `sound_ids.h`; `al_check.h` no-op stub; `ICitySimulation.h` (17+ methods, `MockCitySimulation`, `CityRatingTier`); `ManualRNG` 6 self-tests; `ManualClock`; `MockRenderer`; `NullSimulationPauser`; `IUIBackend` (17 methods); `MockUIBackend`; `UIScaler` VERIFY (Phase 1 deliverable — `unproject()` + `getViewportRect()` verified implemented; all 5 named UIScaler unit tests verified still passing; `UIScaler_MouseInBottomBlackBar_VirtualY_ClampedToMax` compile-only stub added); `NotificationManager` stub (constructor `ICitySimulation*`; Phase 6 signatures locked); `UIManager` shell (10 draw slots, 6-priority chain, Priority 2 dual-guard, `ModalDialog` constructor accepts `ICitySimulation*`); `UIManagerDrawOrderTest` (NiceMock, InSequence, 10 slots + 2 additional test cases); verify 8 `CameraController` tests pass + add 1 compile-only stub case; 'Verify integration test routing (non-zero discovery)' CI step co-landing with `integration_tests` target; all CMake test targets (`ui_tests` 5-file consolidated, `terrain_tests` INTERFACE, `simulation_tests` final-form, `integration_tests`) | `graphics-dev-irrlicht`, `gamedesign-ux`, `sound-dev-opensoftal`, `test-dev-cpp`, `cicd-dev-github` | Planned |
| [4](phase-4.md) | CI Pipeline Expansion & Asset Tooling Stubs | GLEW vcpkg + `GLEW32.dll` hard-fail (CI-2 atomicity: vcpkg.json + CMakeLists + PowerShell check in same commit), Linux GLEW artifact globbed step (CI-1), CI-3 compiler-version detect ordering fix, CI-4 markdown lint verification, CI-5 `actions/setup-python` SHA resolution, CI routing verification (unit/integration/requires-opengl label listing), `validate-assets` 4-item atomicity verification, `tools/validate_assets.py` Part B comment block stubs, `tools/vehicle_atlas_registry.json` (5 V1 vehicle type stubs; `graphics-artist-3d-model` co-sign), `tools/music_sidecar_schema.json`, `shader_constants.h` correctness gate, `simulation_constants.h` Part B (12 service/population/zoning constants + int static_assert guards), SA-2/SA-3 music production briefs, Zone Loop/Vehicle SFX/Stinger/Ambient Bed production briefs, Crossfade audibility pre-production demo, texture artist + 3D model artist sign-off gates, bootstrap oscillation design review gate, `src/ui/` 25% coverage floor, **`cicd-dev-github`** replaces Phase 1 informational `src/ui/` step with blocking float-aware 25% `awk` gate in `coverage-linux` | `cicd-dev-github`, `graphics-dev-irrlicht`, `sound-artist-opensoftal`, `sound-dev-opensoftal`, `gamedesign-lookandfeel`, `graphics-artist-3d-model`, `graphics-artist-2d-texture`, `test-dev-cpp` | Planned |
| [5](phase-5.md) | Procedural Terrain | Chunked `IMeshBuffer` terrain (LOD0/1/2), `TerrainSystem` rebuild deque (≤2/frame, deduplication, `flushPendingRebuilds()` 100 ms budget), `SceneEntityManager` (full 4-step destroy sequence: linear release → sRGB release → splat release → driver setMaterial → evict → null → remove), `TextureCache` full 3-pool implementation (linear/sRGB raw-GL/splat raw-GL; EDT_NULL guard; VRAM estimation; `evictUnreferenced()` auto-unbind; `m_splatMaps` canonical name; `getSRGBGLuint` accessor), terrain GLSL shaders (sRGB + splat; gamma fallback; `GL_ACTIVE_TEXTURE` save/restore), terrain textures (DXT1 sRGB 2048×2048), terrain normal maps (DXT5nm), splat map PNG, atlas layout sign-off (`graphics-artist-3d-model` building atlas sign-off gate: shared cell variant approach + 496×496 px usable area + 16-cell V1 coverage, required before Phase 9 UV authoring), `lod_swap_smoke_test.cpp` `SetMeshGrabDropContract` test promoted from GTEST_SKIP to real body, `validate_assets.py` full 13-check implementation (check #11 corrected to `height_floors>=4`), **`--fail-under-percent` raised from 0 to 80 (BLOCKING)** | `graphics-dev-irrlicht`, `graphics-artist-2d-texture`, `graphics-artist-3d-model`, `test-dev-cpp`, `cicd-dev-github` | Planned |
| [6](phase-6.md) | Simulation Core | `CitySimulation` (4-arg constructor), `SimulationConstants`, economy model (tax revenue, utility fees, wages, loan mechanic, grace period, density unlock 3-month counter), zoning (R/C/I demand + bootstrapping + density unlock + zombie population prevention), population growth, traffic (A* agent, smoothstep demand coupling, 3/5-tick rolling average), service coverage (fire 800 m/police 600 m/power BFS/water 700 m, deficit degradation order), undo system (single-level, 2nd-budget-tick expiry, funds refund), game progression (Sandbox), game-over flow (Scenario skeleton), `simulation_tests` target (7 source files upfront), EarthworksCost audio callback | `gamedesign-lookandfeel`, `test-dev-cpp` | Planned |
| [7](phase-7.md) | Audio Foundation | `AudioSystem` RAII (`ALC_EXT_thread_local_context` hard-failure, partial-construction safety, duck state machine full impl, `setGameOverState()` fully implemented), 62-source pool (`kEvictableSFXCount=55`, stingers[55..56] V1, streams[58..61], sources[57] idle), streaming (OGG/libvorbisfile, 8×64 KB buffers, 10 ms wake, `m_nextBarBoundary=0`), HRTF (`default.mhr`), EFX lowpass occlusion (two boolean guards `m_efxAllocationAttempted`/`m_efxAvailable`), `AudioSourcePool` vehicle pairs (`kMaxVehiclePairs=12`, atomic pair acquisition/release), `IAlcFunctions` seam for headless CI, DLL + `default.mhr` CI verification, placeholder OGG assets | `sound-dev-opensoftal`, `sound-artist-opensoftal`, `cicd-dev-github` | Planned |
| [8](phase-8.md) | HUD & UI Panels | Full HUD (`HUD(IUIBackend*, IAudioSystem*, IClock*, ICitySimulation*)`, resource bar, speed selector, toolbar x:8–72 px, undo button y:608–656 px, grace period countdown, demand bars y:664–744 px, active tool indicator y:752–784 px, notification bell x:1820–1868 px, unsaved-changes dot x:1796–1812 px), Budget Detail Panel (320×200 px, 8 line items, density unlock preview tooltip with kNoUnlockThreshold sentinel guard), Tax Rate Panel (300×200 px), Query/Inspector Panel (240×160 px), Minimap (200×200 px, Service Coverage overlay, legend panel), Modal Dialog system (forced loan 2-screen 640×400 px, demolish confirm 480×240 px, WASD preset 480×240 px, game-over stub 560×320 px), NotificationManager (CRITICAL 48 px fixed, Normal 40–63 px, auto-pause, log 400×500 px at bell bottom-right), Settings/Pause Menu, Main Menu + New Game flow, full input arbitration (6-priority chain), Colorblind Mode fully wired (Graphics tab > Accessibility subsection), all UI tests, `IrrlichtUIBackend` full 17-method implementation | `gamedesign-ux`, `graphics-dev-irrlicht`, `graphics-artist-2d-texture`, `test-dev-cpp` | Planned |
| [9](phase-9.md) | City Assets (Buildings & Vehicles) | **BLOCKING entry gate**: `graphics-artist-3d-model` full `3d-model-standards.md` sign-off (all 14 export checks, naming, pivot, 5 mm tolerance, 10-floor cap, collision dispatch, billboard floor limit, LOD2 pivot conformance) before any `.b3d` files committed; V1 building models (min 18 sets: 2 per zone×tier; LOD0/1/2 `.b3d`; billboard 1024×128 DXT5 sRGB at −45° pitch), vehicle models (5 types, car ≤1500 tris LOD0), collision meshes, building facade atlas (2048×2048 DXT1 sRGB raw-GL), vehicle atlas (2048×2048 DXT1), road textures (`road_asphalt_tileable.dds`, `road_markings_atlas.dds`), UI sprite sheet (2048×2048 RGBA8 UNORM, DDS export via `export_textures.py --format rgba8`), LOD swap integration test (negative bounding-box case), `validate_assets.py` Check #14 (sidecar) + Check #15 (road LOD2 color), `validate-assets` CI job Check #14/#15 additions | `graphics-artist-3d-model`, `graphics-artist-2d-texture`, `graphics-dev-irrlicht`, `cicd-dev-github` | Planned |
| [10](phase-10.md) | Dynamic Soundscape | All V1 audio assets per manifest (2 menu music + 4 ambient beds + 6 gameplay stems + 3 zone loops + all WAV SFX + 2 stingers), adaptive music (constant-power crossfade, beat-boundary via `m_samplesQueued`, min 2 s default 3 s, interrupted crossfade `t_offset` formula), ambient beds (sources[60..61], time-of-day forced Calm, real-time crossfade), zone loops (mono positional, up to 16 simultaneous, ≤300 m cull, Check #16 CI gate), vehicle engine audio (idle/move crossblend, `AL_PITCH` 0.75–1.35, ≤150 m cull, 12 pairs max), stingers (music duck to 0.4, `stinger_milestone` at City Rating only), all UI/SFX wiring (10-asset EFX bypass list), `sfx_earthworks` wired to Phase 6 callback | `sound-artist-opensoftal`, `sound-dev-opensoftal` | Planned |
| [11](phase-11.md) | Save System & Game Flow | JSON save V1 (1 auto-save + 3 manual slots; auto-save 120 s or 5 ticks; auto-save on forced loan dialog + on Pause Menu open), save paths (`~/.config/aitown/saves/` Linux / `%APPDATA%\aitown\saves\` Windows), game-over flow integration (`transitionToGameOver()` Scenario-only, progressive warnings, auto-slow to 1×), main menu save-state integration, `stinger_milestone` wired to City Rating transitions (NOT raw 100K population), loading screen `flushPendingRebuilds()` integration + deduplication integration test, all save system unit tests (round-trip, corrupt JSON, schema version, auto-save timing, counter reset, suspended while paused/modal) | `gamedesign-lookandfeel`, `gamedesign-ux`, `graphics-dev-irrlicht`, `sound-dev-opensoftal`, `test-dev-cpp` | Planned |
| [12](phase-12.md) | Polish, Performance & V1 Hardening | 60 FPS @ 10,000 agents verified, map size 256×256–1024×1024 confirmed, ≤2,000 draw calls, VRAM ≤170 MB scene / ≤1.0 GB total, colorblind mode QA verification pass (Phase 8 delivers implementation; Phase 12 verifies correctness at 1280×720 and 1920×1080), coverage gate ≥80% verified (not raised — gate established Phase 5), all spec contradictions resolved, fixed-seed regression pinning (`0xDEADBEEF00000001`), full QA pass, `all-checks-pass` consistently green | All roles | Planned |

---

## Phase Files

- [Phase 0: Foundations & CI Skeleton](phase-0.md)
- [Phase 1: Irrlicht Device Shell & Camera](phase-1.md)
- [Phase 2: GL Capability, TextureCache Skeleton & Shader Infrastructure](phase-2.md)
- [Phase 3: Interface Layer & Test Infrastructure](phase-3.md)
- [Phase 4: CI Pipeline Expansion & Asset Tooling Stubs](phase-4.md)
- [Phase 5: Procedural Terrain](phase-5.md)
- [Phase 6: Simulation Core](phase-6.md)
- [Phase 7: Audio Foundation](phase-7.md)
- [Phase 8: HUD & UI Panels](phase-8.md)
- [Phase 9: City Assets (Buildings & Vehicles)](phase-9.md)
- [Phase 10: Dynamic Soundscape](phase-10.md)
- [Phase 11: Save System & Game Flow](phase-11.md)
- [Phase 12: Polish, Performance & V1 Hardening](phase-12.md)
- [Post-V1 Backlog](post-v1-backlog.md)
