// src/ui/ModalDialog.cpp
//
// Phase 8 stub translation unit for ModalDialog.
// Phase 3 delivered the ModalDialog class with inline method bodies
// in modal_dialog.h. Phase 8 replaces inline stubs with full
// implementation bodies here.
//
// Full implementation deliverables (Phase 8):
//   - Forced loan dialog (640x400, non-dismissible, 2-screen)
//   - Demolish confirmation (480x240)
//   - WASD preset confirmation (480x240)
//   - Game-over modal stub (560x320, non-dismissible)
//   - Background scrim (50% opacity)
//   - Keyboard navigation (Tab, Enter, Escape)
//   - closeModal() ordering: setPaused(false) THEN setModalActive(false)

#include "src/ui/modal_dialog.h"

// Phase 8: method bodies will be moved from modal_dialog.h to here.
// Currently all ModalDialog methods are inline in the header (Phase 3 stubs).
