# AI Town — Implementation Plan Index

**Date**: 2026-02-20
**Derived from**: All files under `architecture/` (canonical source of truth) and `CLAUDE.md`
**V1 Scope boundary**: Enforced per `architecture/game-design/minimum-viable-simulation.md`. Post-V1 items are explicitly labelled and excluded from phase deliverables.

---

## Spec Contradictions Flagged

The following contradictions were identified during the spec review. They must be resolved before the affected phase can exit. Items marked **RESOLVED** have been closed based on further spec review.

All open contradictions have been resolved. See the Resolved Contradictions list below.

### Resolved Contradictions

1. **[RESOLVED] Source pool slot [57] V1 treatment** — Confirmed: sources[57] in V1 is **idle** — allocated in the 62-source block by `alGenSources(62, ...)` at pool construction but never returned by `acquireSFXSource()` (range 0..54), `acquireStingerSource()` (indices 55..56), or `acquireStreamSource()` (indices 58..61). The phrase "evictable in V1" in earlier spec drafts was misleading. Post-V1 promotion to `StingerType::GAME_OVER` requires only: add `GAME_OVER=57` to StingerType, increment kStingerCount 2→3, extend the stinger setup loop to include index 57 — no pool restructuring. Spec updated in `source-pool.md`. **Closed in Phase 4 spec pre-work (2026-02-20)**.

2. **[RESOLVED] IAudioSystem method signature and count mismatch** — `architecture/testing/testability-architecture.md` updated to match the canonical 11-method `IAudioSystem` in `architecture/audio-architecture/audio-system.md`. Both files now agree: `playSound(SoundId id, SoundPriority priority, float gain = 1.0f)`, `playPositionalSound(SoundId id, vec3 pos, SoundPriority priority, float gain = 1.0f)`, `setTimeOfDay(TimeOfDay tod)`, and `transitionToGameplay()` are all present. `MockAudioSystem` MOCK_METHOD block updated to 11 methods. **Closed in Phase 0 plan update (2026-02-18)**.

3. **[RESOLVED] Emergency Municipal Bond interest rate** — Both `architecture/game-design/economy-model.md` and `architecture/ui-ux/modal-dialog-system.md` agree at 5%/year. The `economy-model.md` states "5% per in-game year (the same unified rate as forced loans)"; `modal-dialog-system.md` states "5%/year (same rate as forced loans — the bond's distinguishing cost is the doubled principal and 24-tick repayment period)". An earlier draft flag referenced "8%" which was a tax rate example ("Residential: 8% → 8.8%"), not an interest rate. No spec change required; the contradiction flag is closed. All code and tests must use 5%/year per `economy-model.md`. **Closed in Phase 9**.

4. **[RESOLVED] Zone loop channel count** — Zone loops (`zone_residential`, `zone_commercial`, `zone_industrial`) are mono positional (1 channel), pre-loaded into a single AL buffer from the SFX pool, and are NOT streamed. All three relevant audio specs are consistent: `streaming-architecture.md`, `v1-audio-asset-manifest.md`, and `audio-asset-formats.md` all agree on mono. An earlier draft flag referencing stereo was based on a misread of the `streaming-architecture.md` kSamplesPerBuffer section. No spec change required; the contradiction flag is closed. **Inline `SPEC CONTRADICTION` block in `phase-7.md` updated to `[RESOLVED]` on 2026-02-20.** Final QA pass in Phase 9.

5. **[RESOLVED] getDemandPressurePct vs getTrafficDemandFactor** — Both methods remain in `ICitySimulation.h` with distinct semantics, confirmed in `architecture/testing/testability-architecture.md` and `src/interfaces/ICitySimulation.h`: `getDemandPressurePct(ZoneType) const` returns the post-combination HUD aggregate demand [0.0, 1.0] (what the player sees in the demand bars); `getTrafficDemandFactor(ZoneType) const` returns the raw traffic-only smoothstep [0.0, 1.0] for Phase 8 save/load serialization. They are semantically distinct and must both be present. **Closed 2026-02-20.**

6. **[RESOLVED] sfx_budget_warn duration — 1–2 s confirmed** — `architecture/audio-architecture/v1-audio-asset-manifest.md` confirms `sfx_budget_warn` as WAV PCM, 1–2 s, non-looping one-shot, −24 LUFS, −1 dBTP. The 6 s minimum applies to engine loops only; non-looping one-shots have no loop fatigue concern. The `sfx_budget_warn` asset is confirmed as a WAV 1–2 s event chime for deficit threshold crossings. **Resolution: 2026-02-20. Closed.**

---

## Roles

| Role ID | Discipline |
|---|---|
| `graphics-dev-irrlicht` | C++ graphics / Irrlicht engine |
| `sound-dev-opensoftal` | C++ audio / OpenAL Soft |
| `gamedesign-lookandfeel` | Gameplay balance, simulation feel |
| `gamedesign-ux` | UI/UX design |
| `graphics-artist-3d-model` | 3D model authoring |
| `graphics-artist-2d-texture` | 2D texture / atlas authoring |
| `sound-artist-opensoftal` | Audio asset authoring |
| `test-dev-cpp` | C++ testing (GTest + GMock + RapidCheck) |
| `cicd-dev-github` | GitHub Actions CI/CD |
| `proj-manager` | GitHub project sync (milestones, issues, Projects v2 board) |

---

## Phase Overview

| Phase | Name | Primary Deliverables | Team | Status |
|---|---|---|---|---|
| [0](phase-0.md) | Foundations & CI Skeleton | CMake scaffold, `vcpkg.json` (libvorbisfile chosen), FetchContent (GTest v1.14.0, RapidCheck SHA-pinned), `aitown_add_tests()` macro, `src/interfaces/` with `IClock.h`/`ISimulationRNG.h`/`ISimulationPauser.h`, GitHub Actions CI YAML (build-linux/build-windows/coverage-linux/all-checks-pass), supply-chain lint FIRST step, `--fail-under-percent 0` gate, branch protection + bootstrap procedure | `cicd-dev-github`, `graphics-dev-irrlicht`, `test-dev-cpp` | **DONE** |
| [1](phase-1.md) | Render Skeleton & Camera | `RenderSystem` RAII (`EDT_OPENGL`, `Bits=32`, `Vsync=false`; `getGPUProgrammingServices()` null-checked), render loop order (`UIManager::draw()` — not `m_gui->drawAll()`), **frame sequence: (1) poll events (2) game logic tick (3) `CameraController::update(dt)` (4) `syncListenerToCamera()` + `AudioSystem::update()` (5) `beginFrame()` … (OAL-2: camera updated BEFORE listener sync)**, GL capability queries (consolidated EDT_NULL guard; `glewInit()` two-tier failure handling), `find_package(GLEW REQUIRED)` + `target_link_libraries(aitown_render PRIVATE GLEW::GLEW)` (TARGET `aitown_render` — ATOMICITY BLOCKING with DLL copy + PowerShell check), **Linux GLEW artifact step uses globbed `find build/vcpkg_installed -name "libGLEW.a"` (CI-1 — hard-coded triplet path removed)**, **Irrlicht.dll phasing: Linux=static `libIrrlicht.a` (no DLL check); Windows=`Irrlicht.dll` in `build/Release/` (CI-2); Irrlicht.dll grace window on `develop` documented (CI-CRITICAL)**, **compiler-version detect step MUST be separate step BEFORE `actions/cache` in both `build-linux` and `coverage-linux` (CI-3)**, **markdown-lint in `all-checks-pass` MUST NOT be removed — permanent gate (CI-4)**, **`actions/setup-python` SHA requires live resolution at implementation time via `gh release view` — not pre-resolved (CI-5)**, **validate-assets four-item atomicity: validate_assets.py stub + CI job + Run step + all-checks-pass needs: all in same commit (CI-2 atomicity)**, **`lcov --list` awk pipe-delimited extraction with `grep -v "^Total"` — canonical form in `coverage.md` (Test-4)**, `TextureCache` skeleton (3 pools + `m_driverType`; `m_splatMaps` canonical name — NOT `m_splatMapTextures` (Irrlicht-1); `getSRGBGLuint` accessor — NOT `getGLuint` (Irrlicht-2); const-ref eviction params; **Irrlicht-3: dispatch logic encoded as stub comments**; `loadSRGB()` `_d` dispatch EXCEPTION: `vehicles_sprite_atlas_d.dds` → LINEAR (2D-1)); **GLSL stubs co-landing requirement (Irrlicht-5)**, **LOD smoke test Phase 1 body: `GTEST_SKIP()` (NOT `SUCCEED()`) — timing deferred to Phase 2 (Irrlicht-4)**, `audio_types.h` (canonical order: constants → `StingerType` enum → `static_assert`; `kEvictableSFXCount=55`, `kTransientReserveStart=51`, `kMaxVehiclePairs=12`), `src/audio/al_check.h` no-op stub, `AudioSystem` stub (BEHAVIORAL CONTRACT; `m_occlusionGainTarget` is `std::atomic<float>[]` — NOT plain float[] (OAL-CRITICAL); `FnSetThreadCtx` LOCAL alias — NOT `PFNALCSETTHREADCONTEXTPROC` (OAL-1); zero AL includes; frozen members), **SA-2 music production brief: BPM=90, 5 stem titles (`music_calm_01/02`, `music_growth_01/02`, `music_crisis_01`) — ambient_day/night removed (SA-1); bar-count confirmation requirement (SA-3)**, **Zone Loop Production Brief: 100 ms fade-to-silence tail + 100 ms fade-from-silence head at each boundary (SA-2)**; `NotificationManager` stub (constructor `ICitySimulation*` — NOT `ISimulationPauser*`; does NOT call `getConsecutiveDeficitMonths()` internally — UIManager::update() is bridge (UX-3)); `ICitySimulation.h` (`MockCitySimulation`; `CityRatingTier`), **GameMode prerequisite: `ui_types.h` must exist with `enum class GameMode` before UIManager shell (GD-2)**, `UIManager` shell (Priority 2 dual-guard LOCKED; toolbar carve-out in 1920×1080 virtual space — `kToolbarLeft=8, kToolbarRight=72, kToolbarTop=64, kToolbarBottom=784` (UX-CRITICAL); `ModalDialog` constructor accepts `ICitySimulation*` + `m_didPauseSim{false}` (UX-2); `UIManagerDrawOrderTest` NiceMock; draw-order slots 7+8 covered by `DrawOrder_PauseMenuVisible_SlotSevenFiresAfterNotification` stub (Test-2)), **drag-delta uses physical pixel coordinates in `CameraController` — NOT virtual (UX-1)**, `Minimap` stub with `getBounds() const`, `TaxRatePanel` + `InspectorPanel` stubs, 4-param `HUD` constructor, `MockTerrainRNG` manual stub, `aitown_terrain` INTERFACE CMake library, `tools/vehicle_atlas_registry.json` (schema sign-off; LOD2 threshold confirmed: `height_floors>=4` → `_lod2.b3d`, `<=3` → billboard (3D-2); normal atlas bicubic mip before DXT5nm (3D-3); `sprite_atlas.upload_path=linear` (2D-1)), `validate_assets.py` stub (check #11 corrected to `height_floors>=4` geometry shell (3D-1)), `shader_constants.h` stub with `static_assert(kTexUnitBillboard<=15)` (2D-2), `simulation_constants.h` stub (**GD-1 gate split Part A locked**: 11 constants — `service_uncovered_desirability_penalty_per_tick=5` (int), `service_recovery_desirability_per_tick=8` (int), `earthworks_base_cost_per_tile=500`, `road_placement_cost_per_tile=500` (GD-3); int `static_assert` guards (GD-4); **Part B deferred to Phase 3**), `ManualRNG` 6 self-tests (6th: `ManualRNG_NextInt_OutOfRange_ThrowsAtCallTime` — Test-CRITICAL-1), `simulation_tests` Final-form with `city_simulation_stub_test.cpp` third source (Test-CRITICAL-2), `kNotificationSentinel` (NOT `kNotificationManagerSentinel` — Test-1), `terrain_tests` with `DISCOVERY_TIMEOUT 60` (Test-3), 6 ManualRNG self-tests in exit criteria (ctest filter updated) | `graphics-dev-irrlicht`, `gamedesign-ux`, `sound-dev-opensoftal`, `sound-artist-opensoftal`, `test-dev-cpp`, `gamedesign-lookandfeel`, `graphics-artist-3d-model`, `graphics-artist-2d-texture`, `cicd-dev-github` | Planned |
| [2](phase-2.md) | Procedural Terrain | Chunked `IMeshBuffer` terrain (LOD0/1/2), `TerrainSystem` rebuild deque, `SceneEntityManager`, `TextureCache` (3 pools: linear/sRGB raw-GL/splat map raw-GL), sRGB shader + gamma fallback, splat-map shader, terrain textures (DXT1 sRGB 2048×2048), terrain normal maps (DXT5nm), `validate_assets.py` 13-check implementation (building on Phase 1 stub; check #14 sidecar file check documented for Phase 6 extension), atlas layout sign-off, **`--fail-under-percent` raised from 0 to 80 (BLOCKING)** | `graphics-dev-irrlicht`, `graphics-artist-2d-texture`, `graphics-artist-3d-model`, `test-dev-cpp`, `cicd-dev-github` | Planned |
| [3](phase-3.md) | Simulation Core | `CitySimulation` (4-arg constructor), `SimulationConstants`, economy model, zoning (R/C/I demand + bootstrapping + density unlock), population growth, traffic (A* agent, smoothstep demand coupling), service coverage (fire 800 m/police 600 m/power BFS/water 700 m), undo system, game progression (Sandbox), game-over flow (Scenario skeleton), `simulation_tests` target (5 source files upfront) | `gamedesign-lookandfeel`, `test-dev-cpp` | Planned |
| [4](phase-4.md) | Audio Foundation | `AudioSystem` RAII (`ALC_EXT_thread_local_context` hard-failure, partial-construction safety), 62-source pool (`kEvictableSFXCount=55`, stingers[55..56] V1, streams[58..61]), streaming (OGG/libvorbisfile, 8×64 KB buffers, 10 ms wake), HRTF (`default.mhr`), EFX lowpass occlusion (2 boolean guards), duck state machine (full impl), `AudioSourcePool` vehicle pairs (`kMaxVehiclePairs=12`), DLL + `default.mhr` CI verification, placeholder OGG assets | `sound-dev-opensoftal`, `sound-artist-opensoftal` | Planned |
| [5](phase-5.md) | HUD & UI Panels | Full HUD (resource bar, speed selector, toolbar x:8–72 px, undo button y:608–656 px, grace period countdown, demand bars y:664–744 px, active tool indicator y:752–784 px, notification bell x:1820–1868 px), Budget Detail Panel (320×200 px), Tax Rate Panel (300×200 px), Query/Inspector Panel (240×160 px), Minimap (200×200 px, Service Coverage overlay), Modal Dialog system (forced loan 2-screen, demolish confirm, game-over, WASD preset), Notification System (CRITICAL/Normal queues, auto-pause, log 400×500 px), Settings/Pause Menu, Main Menu + New Game flow, full input arbitration (6-priority chain), all UI tests | `gamedesign-ux`, `graphics-dev-irrlicht`, `graphics-artist-2d-texture`, `test-dev-cpp` | Planned |
| [6](phase-6.md) | City Assets (Buildings & Vehicles) | **BLOCKING entry gate**: `graphics-artist-3d-model` full `3d-model-standards.md` sign-off (all 14 export checks, naming, pivot, 5 mm tolerance, 10-floor cap, collision dispatch, billboard floor limit, LOD2 pivot conformance) before any `.b3d` files committed; V1 building models (min 18 sets: 2 per zone×tier; LOD0/1/2 `.b3d`; billboard 1024×128 DXT5 sRGB at −45° pitch), vehicle models (5 types, car ≤1500 tris LOD0), collision meshes, building facade atlas (2048×2048 DXT1 sRGB raw-GL), vehicle atlas (2048×2048 DXT1), road textures (`road_asphalt_tileable.dds`, `road_markings_atlas.dds`), UI sprite sheet (2048×2048 RGBA8 UNORM), LOD swap integration test (negative bounding-box case), `validate_assets.py` full 13-check + sidecar check #14, `validate-assets` CI job added to `all-checks-pass` gate | `graphics-artist-3d-model`, `graphics-artist-2d-texture`, `graphics-dev-irrlicht`, `cicd-dev-github` | Planned |
| [7](phase-7.md) | Dynamic Soundscape | All V1 audio assets per manifest (2 menu music + 4 ambient beds + 6 gameplay stems + 3 zone loops + all WAV SFX + 2 stingers), adaptive music (constant-power crossfade, beat-boundary via `m_samplesQueued`, min 2 s default 3 s), ambient beds (sources[60..61], time-of-day forced Calm), zone loops (mono positional, up to 16 simultaneous, ≤300 m cull), vehicle engine audio (idle/move crossblend, `AL_PITCH` 0.75–1.35, ≤150 m cull, 12 pairs max), stingers (music duck to 0.4, `stinger_milestone` at City Rating only), all UI/SFX wiring | `sound-artist-opensoftal`, `sound-dev-opensoftal` | Planned |
| [8](phase-8.md) | Save System & Game Flow | JSON save V1 (1 auto-save + 3 manual slots; auto-save 120 s or 5 ticks; auto-save on forced loan dialog + on Pause Menu open), save paths (`~/.config/aitown/saves/` Linux / `%APPDATA%\aitown\saves\` Windows), game-over flow integration (`transitionToGameOver()` Scenario-only, progressive warnings, auto-slow to 1×), main menu save-state integration, `stinger_milestone` wired to City Rating transitions, loading screen `flushPendingRebuilds()` integration, all save system unit tests (round-trip, corrupt JSON, schema version, auto-save timing) | `gamedesign-lookandfeel`, `gamedesign-ux`, `graphics-dev-irrlicht`, `test-dev-cpp` | Planned |
| [9](phase-9.md) | Polish, Performance & V1 Hardening | 60 FPS @ 10,000 agents verified, map size 256×256–1024×1024 confirmed, ≤2,000 draw calls, VRAM ≤170 MB scene / ≤1.0 GB total, colorblind mode QA verification pass only (Phase 5 delivers all implementation; Phase 9 verifies correctness at 1280×720 and 1920×1080), coverage gate ≥80% verified (not raised — gate established Phase 2), all spec contradictions resolved, fixed-seed regression pinning, full QA pass, `all-checks-pass` consistently green | All roles | Planned |

---

## Phase Files

- [Phase 0: Foundations & CI Skeleton](phase-0.md)
- [Phase 1: Render Skeleton & Camera](phase-1.md)
- [Phase 2: Procedural Terrain](phase-2.md)
- [Phase 3: Simulation Core](phase-3.md)
- [Phase 4: Audio Foundation](phase-4.md)
- [Phase 5: HUD & UI Panels](phase-5.md)
- [Phase 6: City Assets (Buildings & Vehicles)](phase-6.md)
- [Phase 7: Dynamic Soundscape](phase-7.md)
- [Phase 8: Save System & Game Flow](phase-8.md)
- [Phase 9: Polish, Performance & V1 Hardening](phase-9.md)
- [Post-V1 Backlog](post-v1-backlog.md)
