// src/ui/MainMenuPanel.cpp
//
// Phase 8 stub translation unit for MainMenuPanel.
// Phase 3 delivered the MainMenuPanel class with inline method bodies
// in main_menu_panel.h. Phase 8 replaces inline stubs with full
// implementation bodies here.
//
// Full implementation deliverables (Phase 8):
//   - Main Menu screen (New Game, Load Game, Settings, Quit buttons)
//   - New Game flow (difficulty selector, mode selector, seed entry)
//   - Loading screen with Cancel button and Finalizing state
//   - Simulation start ordering: m_sim->start() BEFORE transitionToGameplay()
//   - Audio transition: m_audio->transitionToGameplay() on new-game confirm

#include "src/ui/main_menu_panel.h"

// Phase 8: method bodies will be moved from main_menu_panel.h to here.
// Currently all MainMenuPanel methods are inline in the header (Phase 3 stubs).
