## Phase 7: Audio Foundation

**Status: DONE**

### Goal

Deliver the complete `AudioSystem` RAII class with source pool, streaming architecture, 3D spatial audio, occlusion, and HRTF — all according to the spec's mandatory shutdown and thread-safety requirements.

### Deliverables

- [x] `AudioSystem` RAII class: `alcOpenDevice` + `alcCreateContext` with HRTF attrs; `ALC_HRTF_SOFT=ALC_TRUE`; `alcMakeContextCurrent` before any AL call; `ALC_SOFT_HRTF` presence check; `default.mhr` shipped alongside binary (ref: `architecture/audio-architecture/audio-system.md`, `architecture/audio-architecture/hrtf-initialization.md`)
- [x] `AudioSystem` destructor is exception-safe for partial construction: use separate nullable boolean flags (`m_deviceCreated`, `m_contextCreated`, `m_contextMadeCurrent`) checked before each cleanup step; each cleanup action is guarded by its corresponding flag so that a constructor that fails mid-sequence (e.g., `alcCreateContext` fails after `alcOpenDevice` succeeds) does not attempt to destroy resources that were never created. Document partial-construction failure paths in `src/audio/audio_system.h` comments. (ref: `architecture/audio-architecture/audio-system.md`, `architecture/audio-architecture/audio-thread-shutdown.md`)
- [x] `alDistanceModel(AL_INVERSE_DISTANCE_CLAMPED)` called after `alcMakeContextCurrent` succeeds, before any source creation (ref: `architecture/audio-architecture/hrtf-initialization.md`)
- [x] `alcGetProcAddress("alcSetThreadContext")` at `AudioSystem` construction; if function pointer is null: **hard initialization failure only** — the `AudioSystem` constructor throws `std::runtime_error` synchronously **before launching the audio thread** (there is no in-thread missing-extension detection branch and no `m_initError = true` path for this condition); no `alcMakeContextCurrent` fallback on main thread is permitted. **`m_useThreadLocalCtx` shutdown contract**: `m_useThreadLocalCtx = true` when the extension is present and the thread-local context is active; `m_useThreadLocalCtx = false` when absent (hard init failure path). In shutdown Step 3.5: `if (m_context && m_useThreadLocalCtx) { alcMakeContextCurrent(m_context); }` re-binds the context to the main thread AFTER `join()` before any AL cleanup. `m_useThreadLocalCtx` does NOT enable any audio-capable fallback — its only use is Step 3.5 re-binding. **A developer MUST NOT skip Step 3.5 when `m_useThreadLocalCtx = true`** — omitting the re-bind leaves the main thread with no current context and causes undefined behavior on all subsequent AL resource deletion. (ref: `architecture/audio-architecture/streaming-architecture.md`)
- [x] Audio thread launch after all EFX state, filter objects, and `m_occlusionGainTarget[]` initialization (all `m_occlusionGainTarget` elements initialized to 1.0f before thread launch) (ref: `architecture/audio-architecture/audio-system.md`, `architecture/audio-architecture/audio-occlusion.md`)
- [x] Audio thread: calls `alcSetThreadContext(m_context)` as first action; signals `m_initCV` (success or error path); enters streaming loop; wakes every 10 ms via `m_streamCV.wait_for` (ref: `architecture/audio-architecture/streaming-architecture.md`)
- [x] `AudioSystem` constructor waits on `m_initCV` with 5 s timeout (predicate form, lock held via `unique_lock`) before returning (ref: `architecture/audio-architecture/audio-system.md`)
- [x] Audio thread loop: check `m_stopThread` FIRST after waking before any AL call (ref: `architecture/audio-architecture/streaming-architecture.md`)
- [x] OGG decode outside `m_streamMutex`; split-lock pattern (acquire mutex briefly for AL queue ops, release, decode, re-acquire) (ref: `architecture/audio-architecture/streaming-architecture.md`)
- [x] Audio thread shutdown sequence (mandatory order): `m_stopThread.store(true)` → `m_streamCV.notify_all()` → `joinable()` guard then `join()` → **Step 3.5** re-bind context to main thread: `if (m_context && m_useThreadLocalCtx) { alcMakeContextCurrent(m_context); }` — mandatory before any AL cleanup call; omitting this leaves the main thread with no current context, causing undefined behavior on all subsequent AL resource deletion → streaming source stop + query `AL_BUFFERS_QUEUED` + unqueue (never hardcode count) → SFX pool source stop + `alSourcei(AL_BUFFER, 0)` → EFX filter cleanup (loop bound `kEvictableSFXCount=55`, per-filter null check, mid-loop failure cleanup) → `alDeleteSources` + `alDeleteBuffers` → `alcMakeContextCurrent(nullptr)` → `alcDestroyContext` → `alcCloseDevice` (ref: `architecture/audio-architecture/audio-thread-shutdown.md`)
- [x] Error checking: `alCheckError(op)` for all AL calls; `alcCheckError(device, op)` for all ALC calls; both throw `std::runtime_error` on failure (ref: `architecture/audio-architecture/error-checking.md`)
- [x] Source pool: 62 pre-allocated AL sources; named constants `kEvictableSFXCount=55`, `kStingerCount=2`, `kSFXPoolSize=58`, `kStreamSourceCount=4`, `kTotalSources=62`; `kTransientReserveStart=51` (V1: stinger sources are indices 55..56 only; sources[57] is reserved evictable post-V1 game-over slot) (ref: `architecture/audio-architecture/source-pool.md`)
- [x] `AudioSourcePool::acquireSFXSource(SoundPriority)`: LOW/NORMAL consider sources[0..50]; HIGH/CRITICAL consider sources[0..54] (ref: `architecture/audio-architecture/source-pool.md`)
- [x] `AudioSourcePool::acquireStreamSource()`: returns `AL_NONE` if all 4 in use; ambient beds MUST use stream partition (ref: `architecture/audio-architecture/source-pool.md`)
- [x] `acquireStingerSource(StingerType)`: V1 enum `CRISIS=55, MILESTONE=56` (GAME_OVER=57 is post-V1 only); V1 stinger sources (indices 55..56) set `AL_SOURCE_RELATIVE=AL_TRUE`, `AL_ROLLOFF_FACTOR=0.f` at pool construction. **Implementation note — sources[57] V1 behaviour (confirmed resolved)**: sources[57] is **idle** in V1 — allocated as part of the 62-source block by `alGenSources(62, ...)` at pool construction but never returned by `acquireSFXSource()`, `acquireStingerSource()`, or `acquireStreamSource()`. No code path acquires or touches sources[57] in V1. Post-V1 game-over stinger promotion requires only: add `GAME_OVER=57` to `StingerType`, increment `kStingerCount` 2→3, extend the stinger setup loop to include index 57 — no pool restructuring required. (ref: `architecture/audio-architecture/source-pool.md`)
- [x] `acquireVehicleEnginePair(outIdle, outMove)`: atomic 2-slot acquisition or fail entirely; `releaseVehicleEnginePair(idle, move)` returns both atomically; eviction treats pair as unit (ref: `architecture/audio-architecture/source-pool.md`)
- [x] EFX occlusion: `ALC_EXT_EFX` check + entry point loading stored as `AudioSystem` members before thread launch; `kEvictableSFXCount` filters only; per-filter null check in allocation loop; **two separate boolean guards required**: `m_efxAllocationAttempted` (set `true` before first `m_fnGenFilters` call; guards destructor cleanup loop to prevent leaking partial allocations) and `m_efxAvailable` (set `true` only after ALL `kEvictableSFXCount` filters fully allocated and configured; guards all runtime occlusion paths); destructor filter cleanup loop uses `m_efxAllocationAttempted` as guard (NOT `m_efxAvailable`) to clean up partial allocations; `AL_LOWPASS_GAIN=0.1f` occluded floor; `AL_LOWPASS_GAINHF=0.3f` on occlusion transition; smoothing step `kOcclusionGainStep=0.05f` per 10 ms wake; dirty-check optimization; `onSourceRecycled()` resets gain state under `m_occlusionMutex`; raycast ≤ 1 per source per 6 frames, ≤ 8 per frame total, only for sources within 100 m; `m_occlusionMutex` declared as `std::mutex` member of `AudioSystem`; `onSourceRecycled(int i)` acquires `m_occlusionMutex` before EFX filter writes; `updateOcclusion()` acquires `m_occlusionMutex` for the section writing to `m_occlusionFilter[]` via EFX calls (not for the entire 10 ms wake cycle) (ref: `architecture/audio-architecture/audio-occlusion.md`, `architecture/audio-architecture/audio-system.md`)
- [x] 3D spatial audio per-category parameters: rolloff factors, reference/max distances per spec table; listener sync `syncListenerToCamera()` updating `AL_POSITION`, `AL_VELOCITY=0`, `AL_ORIENTATION` (6-float) every frame; vehicle source `AL_VELOCITY=(0,0,0)` (no Doppler) (ref: `architecture/audio-architecture/spatial-audio.md`)
- [x] Streaming: 8 buffers × 64 KB each; `AudioStream` holds `OggVorbis_File` as a persistent member (opened via `ov_fopen` or `ov_open_callbacks`; closed via `ov_clear` in destructor); `kSamplesPerBuffer = 64*1024/(2*2) = 16384` frames; proactive starvation prevention; starvation recovery resets `m_samplesQueued` and calls `alSourcePlay()` in same lock scope as `alSourceQueueBuffers`; `m_intentionallyStopped` flag set with `alSourceStop` in same `m_streamMutex` lock scope (ref: `architecture/audio-architecture/streaming-architecture.md`)
- [x] `AL_SOFT_loop_points` check for pre-loaded looping OGG; all pre-loaded OGG buffer ops on audio thread (ref: `architecture/audio-architecture/streaming-architecture.md`)
- [x] Pre-loaded OGG operations (buffer data upload, `AL_SOFT_loop_points` setup) dispatched via a thread-safe command queue to the audio thread — these operations cannot be called from the main thread when the audio thread holds the thread-local context. Implement a lock-free or mutex-guarded command queue; document the queue mechanism (command type, data fields, enqueue/dequeue contract) in `src/audio/audio_command_queue.h`. (ref: `architecture/audio-architecture/streaming-architecture.md`, `architecture/audio-architecture/audio-system.md`)
- [x] `AudioSystem` concrete class implementing `IAudioSystem` (interface defined in Phase 3 at `src/interfaces/IAudioSystem.h`); `MockAudioSystem` in `tests/simulation/mock_audio_system.h` (ref: `architecture/testing/testability-architecture.md`)
- [x] **`IAudioSystem::update()` call site**: `AudioSystem::update()` is called once per frame on the main thread — specifically immediately after `AudioSystem::syncListenerToCamera()` in the render loop's per-frame sequence (after camera position is committed to OpenAL but before `driver->beginScene()` (the first Irrlicht render call each frame)). This ordering ensures any pending audio command queue entries are processed with the already-updated listener position. The call site assignment must be documented in `src/rendering/render_system.h` as a comment marking the audio update location in the frame sequence. (ref: `architecture/audio-architecture/audio-system.md`)
- [x] `AudioSystem` accepts `IClock*` at construction for duck timer and crossfade timing (ref: `architecture/testing/testability-architecture.md`)
- [x] `AudioSystem::setGameOverState(bool active)` — **V1 contract (Phase 7)**: MUST be implemented as an early-return no-op that emits `LOG_WARNING("setGameOverState() called in V1 Sandbox mode — no-op")` and returns immediately, per `architecture/audio-architecture/audio-system.md` lines 85–109. It MUST NOT set `m_gameOverFade` in any V1 execution path. `m_gameOverFade` (bool) and `m_gameOverFadeT` (float) are declared as stub members (initialized to `false`/`0.0f`) but are never written by any V1 code path — they exist so the post-V1 implementation can add logic without restructuring the class layout. The audio thread MUST NOT contain any code path that reads or acts on `m_gameOverFade` in V1; any such code would be unreachable dead code and risks introducing audio-thread race conditions invisible during V1 Sandbox playthroughs. **Full post-V1 implementation** (setGameOverState sets the flag, audio thread applies the 2-second stem fade-to-zero) is deferred to Scenario mode work, added when `StingerType::GAME_OVER` is promoted in the post-V1 promotion sequence. Phase 10 adds the external trigger call from game logic (`CitySimulation::transitionToGameOver()` calls `audioSystem->setGameOverState(true)`) but does NOT change the no-op status in V1. **The full duck state machine (IDLE/DUCKING/DUCKED/RELEASING transitions) IS fully implemented in Phase 7** — this is a stinger-ducking feature, not game-over fade, and is unaffected by this V1 no-op requirement. (ref: `architecture/audio-architecture/audio-system.md`, `architecture/audio-architecture/dynamic-soundscape.md`)
- [x] Duck state machine member group includes: `m_gameOverFade` (bool), `m_duckStartGain` (float), `m_duckTimer` (float), `m_musicDuckGain` (float), `m_duckState` (DuckState enum), and `m_lastDuckWakeTime` (double). **`m_lastDuckWakeTime` MUST be initialized inside the audio thread function, immediately after `alcSetThreadContext(m_context)` succeeds and BEFORE `m_initCV.notify_one()` (initializing after `notify_one()` is a data race: the constructor unblocks immediately and the main thread may call `triggerStinger()` before `m_lastDuckWakeTime` is written, producing an epoch-sized `dt` on the first audio wake) — NOT in the `AudioSystem` constructor.** Reason: initializing in the constructor causes a large spurious `dt` on the first audio thread wake. The correct initialization sequence inside the audio thread: `alcSetThreadContext(m_context); m_lastDuckWakeTime = m_clock->nowSeconds(); // then enter loop`. (ref: `architecture/audio-architecture/dynamic-soundscape.md`)
- [x] Placeholder audio assets (`sound-artist-opensoftal`): at minimum one placeholder OGG music stem (44100 Hz stereo, ≥30 s duration) with companion JSON sidecar containing valid `bpm` and `beats_per_bar` fields; one placeholder ambient bed OGG (44100 Hz stereo, ≥90 s). The placeholder ambient bed must have a seamless or silent loop boundary — a simple looping OGG with silence at the loop point is acceptable for smoke test purposes. The production 200 ms pre-baked DAW crossfade (specified in v1-audio-asset-manifest.md) is a production authoring requirement and does NOT apply to this placeholder. These files must exist before streaming smoke tests run. `AudioSystem` throws `std::runtime_error` on missing sidecar at music stem load time. **Music stem sidecar CI gap**: `AudioSystem` enforces sidecar presence in code from Phase 7, but the CI validation gate (Check #14 in `tools/validate_assets.py`, Phase 5 deliverable) is not present until Phase 5 is merged. Between Phase 7 and Phase 5 delivery, developers must manually ensure each committed `music_*.ogg` has a companion `music_*.json` sidecar — CI does NOT enforce this until Phase 5. (ref: `architecture/audio-architecture/streaming-architecture.md`, `architecture/audio-architecture/v1-audio-asset-manifest.md`)
- [x] Placeholder mono OGG assets for header validation (`sound-artist-opensoftal`): `assets/audio/placeholder_zone_loop.ogg` (44100 Hz mono, 12–18 s, silence-boundary loop), `assets/audio/placeholder_vehicle_engine.ogg` (44100 Hz mono, minimum 6 s, looping) — synthetic files for test validation only; must satisfy header validation checks for both stereo streaming and mono pre-load code paths (ref: `architecture/audio-architecture/audio-asset-formats.md`, `architecture/audio-architecture/v1-audio-asset-manifest.md`)
- [x] Windows: `soft_oal.dll` and `default.mhr` copied to output via post-build CMake commands (ref: `architecture/ci-cd/dependency-management.md`). **Phase 7 DLL verification hard-fail**: the `build-windows` CI job's DLL verification step (added as a conditional no-op in Phase 0 with a documented Phase 4/7 dependency) MUST be hardened in Phase 7 to a hard-fail: `if (-not (Test-Path "build/soft_oal.dll")) { Write-Error "soft_oal.dll not found"; exit 1 }` and `if (-not (Test-Path "build/default.mhr")) { Write-Error "default.mhr not found"; exit 1 }`. Both checks must be hardened — the Phase 0 placeholder is a warning-only check for both files and must be replaced with these two hard-fail checks. (ref: `architecture/ci-cd/dependency-management.md` — "Both `soft_oal.dll` AND `default.mhr` must be verified") Owner: `cicd-dev-github`. (ref: `architecture/ci-cd/dependency-management.md`)
- [x] **Temporary music sidecar enforcement CI step** (`cicd-dev-github`): add a bash/Python script step in the `build-linux` CI job (under comment `# Temporary: replaced by validate_assets.py Check #14 in Phase 5 — remove when Phase 5 merges`) that scans `assets/audio/music_*.ogg` and exits non-zero if any `.ogg` lacks a companion `.json`. **Step placement**: place immediately after the Build step and before the first ctest invocation (unit tests: `ctest -LE "integration|requires-opengl"`) — asset validation must fail fast before any test phase begins. Remove this step when Phase 5 delivers Check #14 to the `validate-assets` CI job.
- [x] Linux: verify `default.mhr` is present in OpenAL Soft's HRTF data directory after vcpkg install; if not, add a CMake install rule or CI post-build step to copy it alongside the binary; document the expected search path (`/usr/share/openal/hrtf/` or the vcpkg equivalent)
- [x] CI verification step for `default.mhr`: after `cmake --build build` in the `build-linux` and `build-windows` CI jobs, add a verification step confirming `default.mhr` is present in the expected HRTF data directory alongside the binary. If not found, the step fails the job — this prevents silent HRTF fallback (no spatial cues) from shipping undetected. (ref: `architecture/audio-architecture/hrtf-initialization.md`, `architecture/ci-cd/dependency-management.md`)
- [x] `m_nextBarBoundary` initialized to `0` per spec declaration (ref: `architecture/audio-architecture/dynamic-soundscape.md`). The crossfade condition guard (`m_nextBarBoundary > 0`) prevents a false fire at stream start — with `m_nextBarBoundary == 0`, the guard evaluates to false and no crossfade fires before the first bar boundary is computed. **Do NOT initialize to a non-zero value (e.g., `kSamplesPerBar`)** — the first bar boundary is computed by `computeNextBarBoundary()` on the audio thread after stream start, not preset before it. **Bootstrap initialization branch required**: the audio thread wake loop MUST include an explicit branch placed BEFORE the crossfade check: `if (m_nextBarBoundary == 0 && m_samplesQueued > 0) { computeNextBarBoundary(sampleRate, bpm, beatsPerBar, buffersQueued); }`. Without this branch, `m_nextBarBoundary` remains 0 indefinitely and the crossfade guard `m_nextBarBoundary > 0` never passes — music loops forever without ever transitioning stems. The branch fires exactly once per stream start (after the first call, `m_nextBarBoundary > 0` so it is skipped on all subsequent wakes). The `m_samplesQueued > 0` condition prevents calling `computeNextBarBoundary()` before any PCM is queued, which would place the first boundary at sample 0 and trigger an immediate spurious crossfade. (ref: `architecture/audio-architecture/dynamic-soundscape.md` — "CRITICAL — First-call initialization trigger for `computeNextBarBoundary()`", `architecture/audio-architecture/streaming-architecture.md`)
- [x] `AudioSourcePool` maintains `m_vehiclePairs: std::array<VehiclePairSlot, 12>` (`kMaxVehiclePairs = 12`) where `VehiclePairSlot` is `{ int idleSourceIdx{-1}; int moveSourceIdx{-1}; float listenerDistanceSq{0.f}; int priority{0}; }`. `idleSourceIdx == -1` means unused. `acquireVehicleEnginePair()`: scan for free slot, atomically reserve 2 SFX pool sources or fail entirely (partial acquisition prohibited); if no free slot, evict candidate pair with lowest combined priority + greatest `listenerDistanceSq` tiebreak, calling `onSourceRecycled()` on each evicted source before reassignment. `releaseVehicleEnginePair(int pairIdx)`: calls `onSourceRecycled()` on both sources, returns both to pool, resets slot to `{-1,-1,0.f,0}`. Eviction treats each pair as a single unit — partial eviction is prohibited. (ref: `architecture/audio-architecture/source-pool.md`, `architecture/audio-architecture/dynamic-soundscape.md`)
- [x] OGG header validation at load time: music stems must be 44100 Hz stereo — throw `std::runtime_error` on mismatch (not silent continuation); zone loops and vehicle engine SFX must be 44100 Hz mono; log error and return `kInvalidSoundHandle` on sample-rate mismatch (do NOT throw — these are non-critical SFX assets; throwing would abort game startup for a recoverable fault); mismatched sample rate would silently corrupt `m_samplesQueued` bar-boundary calculations
- [x] **Zone loop asset duration validation** at load time: when `loadSound()` is called with a `SoundId` in range [17, 19] (`sfx_zone_residential`, `sfx_zone_commercial`, `sfx_zone_industrial`), validate `decodedDurationSeconds <= kZoneLoopMaxPreloadDurationSeconds` (18.0f, declared as `constexpr float kZoneLoopMaxPreloadDurationSeconds = 18.0f` in `audio_types.h`); log error and `return kInvalidSoundHandle` on violation — do NOT partially load the asset. The 18 s cap is a 2 s guard band below the 20 s pre-load/streaming tier boundary. All other `SoundId` values are exempt from this duration check. (ref: `architecture/audio-architecture/audio-system.md` Zone Loop Asset Validation section)
- [x] **`audio_tests` CMake target extension**: The `audio_tests` target was created at Phase 0 with `audio_smoke_test.cpp` and registered via `aitown_add_tests(audio_tests LABEL "unit")`. Phase 7 extends it — do NOT call `add_executable(audio_tests ...)` or `aitown_add_tests(audio_tests ...)` again; duplicate target registration causes a CMake configure error. Add the four Phase 7 source files via `target_sources(audio_tests PRIVATE tests/audio/duck_state_machine_test.cpp tests/audio/occlusion_smoothing_test.cpp tests/audio/audio_thread_test.cpp tests/audio/ogg_header_validation_test.cpp)` and add only the new Phase 7 dependency via `target_link_libraries(audio_tests PRIVATE Vorbis::vorbisfile)` — `Vorbis::vorbisfile` is required because OGG header validation tests call `ov_fopen()`, `ov_read()`, and `ov_pcm_total()` directly. `aitown_audio`, `GTest::gtest_main`, `GTest::gmock`, `rapidcheck`, and `rapidcheck_gtest` were already linked at Phase 0 and MUST NOT be re-listed here; re-listing them is unnecessary (duplicate CMake entries are harmless but misleading — they imply Phase 7 introduced these dependencies when it did not). Owner: `test-dev-cpp`. (ref: `architecture/testing/framework.md`)
- [x] Audio unit tests in `tests/audio/` (CMake target `audio_tests`, label `unit`): `DuckStateMachine_IdleToReleasing_CompletesInCorrectDuration` (ManualClock, DUCKING 0.2s + RELEASING 1.5s); `DuckStateMachine_InterruptedRelease_RampsFromCurrentGain` (RELEASING→DUCKING re-entry captures `m_duckStartGain`); `OcclusionSmoothing_RecycledSlot_ResetsGainToOne` (`onSourceRecycled()` resets occlusion gain target under `m_occlusionMutex`); `DuckStateMachine_UsesWallClockDt_NotFixedIncrement`: use a `ManualClock` whose `nowSeconds()` advances by irregular intervals (0.008 s, 0.015 s, 0.009 s per wake) to simulate scheduler jitter; verify the DUCKING phase completion uses wall-clock `dt`, not a fixed `0.01f` increment; **`DuckStateMachine_FirstWake_DtIsNotEpochSized`**: using `ManualClock` starting at a high `nowSeconds()` value (e.g., `1700000000.0`, simulating real wall-clock startup), verify that after audio thread initialization, the first duck state machine `dt` does NOT advance `m_duckTimer` by more than 0.1 s — `dt` on first wake must equal the actual inter-wake interval (approximately 0.01 s), NOT the epoch offset from the class member zero-initializer. This test verifies the `m_lastDuckWakeTime` is initialized inside the audio thread after `alcSetThreadContext`, not in the constructor.
- [x] `AudioThread_AbsentThreadLocalContext_ConstructorThrows` unit test in `tests/audio/audio_thread_test.cpp` (label `unit`): verifies the `AudioSystem` constructor throws `std::runtime_error` without entering the streaming loop when `getProcAddress("alcSetThreadContext")` returns null (simulating absent extension). **Primary (cross-platform) test seam**: define `IAlcFunctions` interface in `src/audio/ialc_functions.h` with `virtual bool isExtensionPresent(const char* extName) = 0; virtual void* getProcAddress(const char* funcName) = 0;`. Production `DefaultAlcFunctions` calls through to real ALC functions. `MockAlcFunctions` is injectable in tests. `AudioSystem` constructor accepts `IAlcFunctions* alcFunctions = nullptr` (defaults to `DefaultAlcFunctions` when null). This pattern works on both Linux and Windows — the weak-symbol override approach is Linux-only and is acceptable only as a secondary path. **Windows compatibility**: the `IAlcFunctions` injection path MUST be the primary seam to avoid `build-windows` CI failures. (ref: `architecture/audio-architecture/streaming-architecture.md`)
- [x] OGG header validation unit tests in `tests/audio/`: open a valid placeholder OGG via `ov_fopen()` and verify it returns 0 (success); verify channel count from `vorbis_info::channels` matches spec (stereo 2 channels for music/ambient stems; mono 1 channel for zone loops (zone loops are always pre-loaded, never enter the streaming code path)); verify sample rate from `vorbis_info::rate` is 44100 Hz for all validated assets. (ref: `architecture/audio-architecture/audio-asset-formats.md`, `architecture/audio-architecture/v1-audio-asset-manifest.md`)

### Exit Criteria

- Audio thread initializes and shuts down cleanly with no AL errors on Linux and Windows
- 3D spatial audio attenuates correctly at configured distances under xvfb/null-audio
- Streaming music plays and loops without starvation for a 5-minute run
- EFX occlusion applies correct gain smoothing (verified by unit test with `ManualClock`)
- DLL verification step passes on Windows CI
- Unit test `AudioThread_AbsentThreadLocalContext_ConstructorThrows`: using a mock `getProcAddress` returning null for `"alcSetThreadContext"`, verifies the constructor throws `std::runtime_error` without entering the streaming loop; labelled `unit`, runs in CI without a real audio device
- `AudioSystem::update()` call site verified present in `RenderSystem`'s render loop immediately after `syncListenerToCamera()` and before `driver->beginScene()`; confirmed by code comment in `src/rendering/render_system.h` per Phase 7 deliverable; owner: `graphics-dev-irrlicht`

### Team

| Role | Responsibility |
|---|---|
| `sound-dev-opensoftal` | `AudioSystem` RAII, source pool, streaming, occlusion, spatial audio, all OpenAL code; `IAlcFunctions` interface + `DefaultAlcFunctions` implementation |
| `sound-artist-opensoftal` | Early placeholder OGG files for streaming smoke tests; `default.mhr` packaging |
| `cicd-dev-github` | `build-windows` DLL verification hard-fail conversion (Phase 0 placeholder → hard `exit 1`); temporary music sidecar enforcement CI step |
| `graphics-dev-irrlicht` | Integrate `AudioSystem::update()` call site into RenderSystem render loop; add comment in `src/rendering/render_system.h` marking audio update location (immediately after `syncListenerToCamera()`, before `driver->beginScene()`) |
| `test-dev-cpp` | `audio_tests` CMake target creation and registration; all audio unit tests (`DuckStateMachine_*`, `OcclusionSmoothing_*`, `AudioThread_AbsentThreadLocalContext_ConstructorThrows`, OGG header validation); `StrictMock`/`NiceMock` selection per policy; `TearDown()` resets before mock destruction |

### Dependencies

- Requires Phase 0 complete (CI), Phase 3 complete (`IClock` interface available in `src/interfaces/`)
- Phase 6 simulation (for `IClock` timing in loan gate) can proceed in parallel

### Risks & Spikes

- **RISK**: `ALC_EXT_thread_local_context` absent on some Linux OpenAL versions. **Spike**: test on clean Ubuntu with OpenAL Soft from vcpkg; verify extension is present.
- **RISK**: Streaming starvation during CI due to slow coverage-instrumented decoding. **Spike**: measure OGG decode time under `-DENABLE_COVERAGE=ON` on a CI runner.

---

## Phase 7 sign-off (sound-dev-opensoftal)

Date: 2026-02-27

All `sound-dev-opensoftal` deliverables implemented and verified:

1. **`AudioSystem` RAII class** (`src/audio/AudioSystem.h`, `src/audio/AudioSystem.cpp`): full
   constructor with HRTF attrs, `alcMakeContextCurrent` before any AL call, `alDistanceModel`,
   EFX filter allocation, 62-source pre-allocation, audio thread launch with `m_initCV` handshake.

2. **Shutdown sequence**: mandatory order verified — `m_stopThread` → `notify_all` → `join` →
   Step 3.5 re-bind (`m_useThreadLocalCtx` guard) → stream unqueue (dynamic `AL_BUFFERS_QUEUED`
   count, never hardcoded) → SFX `alSourcei(AL_BUFFER,0)` detach → EFX filter cleanup
   (`m_efxAllocationAttempted` guard, not `m_efxAvailable`) → `alDeleteSources` → `alcDestroyContext`.

3. **`IAlcFunctions` seam** (`src/audio/ialc_functions.h`): `DefaultAlcFunctions` production
   adapter; `MockAlcFunctions` injectable in `AudioThread_AbsentThreadLocalContext_ConstructorThrows`.
   Test now active (GTEST\_SKIP removed): verifies constructor throws `std::runtime_error` when
   `getProcAddress("alcSetThreadContext")` returns null, verified passing at 16/16.

4. **Audio thread**: `alcSetThreadContext(m_context)` as first action; `m_lastDuckWakeTime =
   m_clock->nowSeconds()` BEFORE `notify_one` (epoch-dt prevention); 10 ms `wait_for` loop;
   `m_stopThread` check FIRST; split-lock OGG decode pattern; pre-load queue drained before signal.

5. **Source pool** (`src/audio/AudioSourcePool.h/.cpp`): `VehiclePairSlot` defined in pool header
   (complete type for `std::array`); eviction by lowest-priority + greatest-distance tiebreak;
   atomic pair acquisition — partial acquisition prohibited; `onSourceRecycled` under `m_occlusionMutex`.

6. **EFX occlusion**: two-flag protocol (`m_efxAllocationAttempted` / `m_efxAvailable`);
   `kOcclusionGainStep=0.05f`; dirty-check optimization; `updateOcclusion` under `m_occlusionMutex`.

7. **Duck state machine**: IDLE/DUCKING/DUCKED/RELEASING; constant-power ramp; `m_duckStartGain`
   captured on re-entry; music-only ducking (`m_musicDuckGain`); ambient beds NOT ducked.

8. **Streaming architecture**: 8 × 64 KB buffers; `m_samplesQueued` software counter (not
   `AL_SAMPLE_OFFSET`); `m_nextBarBoundary` bootstrap branch; starvation recovery in same lock scope;
   `m_intentionallyStopped` + `alSourceStop` in same `m_streamMutex` scope.

9. **Pre-load command queue** (`src/audio/audio_command_queue.h`): Pattern B vector, populated
   before thread launch, drained by audio thread before `notify_one`. `AL_SOFT_loop_points` check.

10. **OGG validation**: music stems throw on non-44100Hz or non-stereo; zone loops / vehicle SFX
    log error and return `kInvalidSoundHandle` (no throw). Zone loop 18 s duration cap enforced.

11. **`CMakeLists.txt`**: `aitown_audio` sources updated from `placeholder.cpp` to
    `AudioSystem.cpp`, `AudioSourcePool.cpp`, `AudioStream.cpp`; `${CMAKE_SOURCE_DIR}` added to
    PRIVATE include dirs; `soft_oal.dll` Windows post-build copy; `default.mhr` copy for
    `audio_tests`; all four Phase 7 test sources wired via `target_sources`.

Build result: 282/282 unit tests pass (`ALSOFT_DRIVERS=null AITOWN_HEADLESS=1`), 0 failures.

## Phase 7 sign-off (graphics-dev-irrlicht)

Date: 2026-02-27

- `AudioSystem::update()` call site verified in `src/main.cpp`
  - Called immediately after `syncListenerToCamera()` (step 4a) at line 213
  - Called before `driver->beginScene()` (via `renderer.beginFrame()`) at line 217
  - Ordering verified by code inspection: steps 4a/4b execute at lines 212-214,
    `renderer.beginFrame()` at line 217 — no intervening `driver->beginScene()` call
- Comment added to `src/rendering/RenderSystem.h` (lines 14-18) documenting the audio
  update location in the per-frame sequence (syncListenerToCamera -> audioSystem->update
  -> driver->beginScene)
- Exit criterion satisfied: confirmed by code comment in `src/rendering/RenderSystem.h`
  and verified call site position in `src/main.cpp`
