## Phase 3: Simulation Core

### Goal
Deliver the full V1 city simulation engine: economy, zoning, population growth, traffic, and service coverage — all fully testable through injected interfaces.

### Deliverables
- [ ] **`simulation_tests` CMake target**: `add_executable(simulation_tests tests/simulation/economy_test.cpp tests/simulation/traffic_test.cpp tests/simulation/zoning_test.cpp tests/simulation/service_coverage_test.cpp tests/simulation/population_test.cpp tests/simulation/save_system_test.cpp tests/simulation/undo_system_test.cpp)` linking `aitown_sim GTest::gtest_main GTest::gmock rapidcheck rapidcheck_gtest`; registered via `aitown_add_tests(simulation_tests LABEL "unit")`. All test `.cpp` files that will EVER be added to `simulation_tests` must be listed as sources at this target's creation — do not add them incrementally. `tests/simulation/save_system_test.cpp` is listed here as a **Phase 3 stub** (`SUCCEED()` placeholder); its full implementation is Phase 8. `tests/simulation/undo_system_test.cpp` is listed here as a **Phase 3 stub** — simulation-side undo mechanics tests (`UndoState_SingleLevel_ExpiresAfterSecondBudgetTick_SimulationOnly`, `UndoState_FundsRefunded_SimulationSide`) belong in `simulation_tests` (no `MockUIBackend` needed); UI-facing undo tests (countdown display, button grayout) belong in `ui_tests` Phase 5. Listing both stubs here prevents structural CMakeLists changes in Phase 5 and Phase 8. (ref: `architecture/testing/framework.md`)
- [ ] `CitySimulation` class accepts `IRenderer*`, `IAudioSystem*`, `ISimulationRNG*`, `IClock*` at construction (non-owning pointers); all simulation test fixture `SetUp()` calls must pass all 4 required constructor arguments: `std::make_unique<CitySimulation>(&renderer_, &audio_, &rng_, &clock_)` where `rng_` is `ManualRNG` and `clock_` is `ManualClock`. Add `ManualRNG rng_` and `ManualClock clock_` as fixture members to both `CitySimulationUnitTest` and `CitySimulationPropertyTest`. The spec fixture examples showing only 2 arguments are incomplete — all 4 are required. (ref: `architecture/testing/testability-architecture.md`)
- [ ] `SimulationConstants` namespace with all named constants: `road_maintenance_cost_per_tile=10`, `road_placement_cost_per_tile=500`, `service_upkeep_fire_station_per_tick=500`, `service_upkeep_police_station_per_tick=400`, `service_upkeep_power_plant_per_tick=1000`, `service_upkeep_water_tower_per_tick=300`, `wage_fraction_of_revenue=0.20`, `loan_repayment_ticks=12`, `ticks_per_year=12`, `density_unlock_scale` per difficulty, `R_raw_material_rate=0.05`, `C_goods_consumption_rate=0.25`, `base_income_per_resident_low=50`, `base_income_per_resident_medium=50`, `base_income_per_resident_high=55` (**note**: `road_lod2_color` is NOT in `SimulationConstants` — it is a rendering constant placed in `src/rendering/render_constants.h` per Phase 6; `src/simulation/` must not depend on rendering artifact values) (dollars/resident/month — used in the Economy invariant property test; no inline hardcoding of these values in test bodies); bond use limits: `bond_max_uses_easy=3`, `bond_max_uses_normal=2`, `bond_max_uses_hard=1`; base day duration (post-V1 speed hook — all 1.0 in V1): `base_day_duration_easy=1.0`, `base_day_duration_normal=1.0`, `base_day_duration_hard=1.0` (ref: `architecture/game-design/economy-model.md`, `architecture/game-design/traffic-system.md`, `architecture/game-design/zoning-system.md`, `architecture/game-design/simulation-time.md`)
- [ ] **Simulation Time System**: base tick rate 1 in-game day = 1 real second at 1×; month = 30 days; speed multipliers 1×/3×/10×/pause; default starting speed 3×; `ticks_per_year=12` named constant (ref: `architecture/game-design/simulation-time.md`)
- [ ] **Economy Model**: tax revenue formula `base_income_per_resident[tier] × actual_population × zone_tax_rate`; utility fees (power $5/covered R tile, water $3/covered R tile per tick); wages formula `total_C_I_tax_revenue × 0.20 × employment_fill_rate`; `budget_surplus_pct` formula; zero-revenue guard; loan mechanic (forced loan, debt cap, cooldown, 120 s real-time gate); grace period 120 s waiving road maintenance and service upkeep; density unlock thresholds (difficulty-scaled); density upgrade rate limiter 20%/tick; density unlock 3-consecutive-month revenue counter per tier tracked in `CitySimulation`; counter resets if revenue drops below threshold between months; counters serialized in save state; persistent HUD debt indicator: widget element reading `ICitySimulation::getOutstandingDebt()`, hidden when debt == 0, shows outstanding debt amount in red; loan issuance fires a Normal toast via `NotificationManager` with loan amount and repayment terms; `getOutstandingDebt()` added to the minimum `ICitySimulation` methods list (expanded further in Phase 5) (ref: `architecture/game-design/economy-model.md`)
- [ ] Loan mechanics: first-loan debt-cap override; loan pooling; per-loan independent repayment; interest `outstanding_debt × (0.05 / 12)` before repayment; Emergency Municipal Bond (**5%/year — same unified rate as forced loans**; 24-tick repayment, availability by difficulty: Easy=3/Normal=2/Hard=1); loan remainder on last tick (ref: `architecture/game-design/economy-model.md`)
  - **RESOLVED — Emergency Municipal Bond interest rate**: The bond rate is **5%/year** per `architecture/game-design/economy-model.md`. `architecture/ui-ux/modal-dialog-system.md` has been updated to match (`economy-model.md` is the canonical authority for simulation numbers). All code and tests use 5%/year.
- [ ] **Zoning System**: zone types R/C/I with Low/Medium/High density tiers; base desirability 50; Industrial→R penalty (Chebyshev distance, -20 at d=1, linear falloff to d=5); Commercial→R bonus (+10 at d=1 only); demand formulas (R: `C_I_worker_capacity / max(1, current_R_population)`; C: `R_population / total_C_worker_capacity`; I: `(R_raw × 0.05 + C_goods × 0.25) / I_production_capacity`); demand bootstrapping ticks 0–5 (R=50%, C=25%, I=15%, linear decay); demand floors post-tick 6 (R≥20%, C≥10%, I≥10%); demand floor applies ONLY to `occupancy_increase_this_tick`; emigration/depopulation path runs independently and is NOT blocked by demand floor (zombie population anti-pattern prevention per `zoning-system.md`); density upgrade wave re-evaluation per tick; hard gate Med-Industrial before High-Residential; simultaneous density unlock processing order enforced within a single tick: (1) Med-R/Med-C, (2) Med-I, (3) High-R (gated on Med-I activation in the same tick), (4) High-C, (5) High-I. All threshold crossings evaluated in this strict order before any tile upgrade wave fires; density upgrade wave end conditions: (a) all eligible tiles upgraded, OR (b) no remaining eligible tiles have `demand_factor > 0.75` — wave ends without auto-restart; unlock remains active and future ticks re-evaluate eligibility. (ref: `architecture/game-design/zoning-system.md`)
- [ ] **Population Density & Growth**: residents per tile per spec table; growth formula `max_density × demand_factor × (desirability/100)`; growth cap +10%/tick; decay cap -15%/tick (both relative to `max_density_for_tier`); population update each budget tick (ref: `architecture/game-design/population-density-growth.md`)
- [ ] **Traffic System**: agent-based A* pathfinding; speed formula `max(max_speed × 0.05, max_speed × (1 - occupancy/capacity))`; min road capacity 1 vehicle; agent timeout 120 simulation seconds (logged as extreme-travel-time, NOT null-path); 5-tick rolling average travel time for R/C demand; 3-tick rolling average for I demand; smoothstep demand curves: R (T=25 to T=60), C (T=30 to T=65), I (T=40 to T=80); null-path default 0.5 if all window ticks null; zone served by road if edge-adjacent (4-directional); congestion graduated penalty (-10% at 31–40% speed, -18% at 21–30%, -25% at ≤20%); `demand_pressure_pct` field in `QueryResult` (ref: `architecture/game-design/traffic-system.md`)
- [ ] Road parameters: max_speed=13.9 m/s (50 km/h); segment capacity=8 vehicles/tile; traffic signals with configurable green/red durations (ref: `architecture/game-design/traffic-system.md`)
- [ ] **Service Coverage**: fire radius 800 m, police 600 m, water tower 700 m, power plant graph traversal; `service_level_pct` per building; desirability penalty -5/tick for uncovered tiles (after first tick of absence); recovery +8/tick on restoration; budget deficit degradation at -10% surplus (fire first, then police, then water, then power last; power uses BFS brownout to floor(max_depth × 0.70)); radius halved for radius-based services under deficit; zero-tiles-in-range guard: when `total_buildable_tiles_in_range == 0`, display 'N/A — no served zones in range' instead of computing `service_level_pct`; denominator computed once at placement and cached (ref: `architecture/game-design/service-coverage.md`)
- [ ] **Undo System**: single-level undo; covers zone/road placement and demolition; expires at second budget tick after action; undo blocked during blocking modal; undo button grayed via `setElementEnabled(false)` (not `setElementVisible(false)`); countdown display in HUD (amber at < 5 s or when window ≤ 6 s total); funds refunded at original cost; `CitySimulation::getUndoExpiryTimeSeconds() -> double` returns the real-time (seconds, from `IClock::nowSeconds()`) when the current undoable action expires — required for HUD undo countdown display in Phase 5 (ref: `architecture/game-design/undo-system.md`)
- [ ] **Game Progression — Sandbox mode**: difficulty starting funds Easy=$1M / Normal=$500K / Hard=$200K; milestone notifications at 1K/10K/50K/100K/500K population; City Rating tiers (Village/Town/City/Metropolis/Megalopolis) with population thresholds per spec; City Rating shown in HUD resource bar (ref: `architecture/game-design/game-progression-modes.md`)
- [ ] **Game Over Flow** (Scenario mode only): -50% deficit for 3+ consecutive months; progressive warning system (CRITICAL toasts at months 1 and 2, auto-slow to 1× on month 1, red flashing indicator on month 2); streak-break feedback toast; `transitionToGameOver()` no-op in Sandbox mode (ref: `architecture/game-design/game-over-flow.md`)
- [ ] `ISimulationRNG` in `src/interfaces/`; `ManualRNG` in `tests/simulation/manual_rng.h` (two independent sequences: intSeq and floatSeq; strict mode throws on exhaustion; validates floatSeq in [0, 1); **`verifyAllConsumed()` method** asserts both `m_intIdx == m_intSeq.size()` AND `m_floatIdx == m_floatSeq.size()` — over-provisioning is as defective as under-provisioning and masks regressions; call `rng_.verifyAllConsumed()` in `TearDown()` for any strict-mode `ManualRNG` fixture) (ref: `architecture/testing/testability-architecture.md`). **ManualRNG call-sequence audit required for all Phase 3 tests**: before writing any test that initializes `ManualRNG` with a fixed `floatSeq`, count the exact number of `nextFloat()` calls that the simulation will make during that test. Under-provisioning `floatSeq` causes `std::logic_error` at runtime (not a compile error). For demand-calculation tests, every occupancy update and desirability calculation may call `nextFloat()`; provision at least N+2 float values where N is the number of tiles being evaluated per tick. Document the expected call count as a comment in each fixture: `// floatSeq: 3 values — 1 per R tile occupancy roll per tick`.
- [ ] `IClock` / `WallClock` in `src/interfaces/`; `ManualClock` in `tests/simulation/manual_clock.h` (ref: `architecture/testing/testability-architecture.md`)
- [ ] Property-based tests (RapidCheck, `tests/simulation/`):
  - Economy invariant (treasury accounting exact, all named constants, wages formula, interest timing interest-before-repayment, multi-loan pooling, first-loan override, utility fees, road placement cost) (ref: `architecture/testing/property-based-tests.md`)
  - Debt cap invariant (`rc::gen::inRange(7, 24)`, ManualClock advance 121 s before deficit ticks) (ref: `architecture/testing/property-based-tests.md`)
  - Loan cooldown invariant (ref: `architecture/testing/property-based-tests.md`)
  - Loan activation gate (real-time gate, not tick count; ManualClock < 120 s) (ref: `architecture/testing/property-based-tests.md`)
  - Traffic invariant: finite path for any connected source/dest (ref: `architecture/testing/property-based-tests.md`)
  - Zoning invariant: desirability in [0, 100] (ref: `architecture/testing/property-based-tests.md`)
- [ ] Fixed-seed unit tests: `LoanGate_FiresAtExactly120Seconds` (integer advances 119.0 + 1.0); `BudgetSurplus_ZeroRevenue_ReturnsZero`; `BudgetSurplus_ZeroRevenue_NoDeficitConsequences`; `BudgetSurplus_LargeDeficit_Representable`; `GameOverThreshold_RequiresConsecutiveTicks`; `LoanRepayment_NonDivisiblePrincipal_LastTickAbsorbsRemainder` ($10,001 over 12 ticks); `DensityUnlock_SimultaneousThreshold_MedIBeforeHighR`: revenue jump from $30K to $150K in a single tick activates Med-I before allowing High-R; `GameOver_SandboxMode_NoTransition`: verify `transitionToGameOver()` is a no-op in Sandbox mode (does not open modal, does not call `playSound(stinger_game_over)`) even after 3+ consecutive −50% deficit ticks; `GameOver_Month1_AutoSlowsTo1x`: on first month of ≥ −50% streak, `setSpeed(SpeedMultiplier::x1)` is called exactly once; `GameOver_StreakReset_On49PctDeficit`: streak counter resets when a tick fires at −49% (just above the −50% threshold) (ref: `architecture/testing/property-based-tests.md`)
- [ ] `DensityUnlock_RequiresThreeConsecutiveMonths_NotOneSpike`: advance revenue to threshold for 2 ticks, drop below, verify no unlock fires; then advance 3 consecutive ticks above threshold, verify unlock fires. Uses `ManualRNG` and `ManualClock`. (ref: `architecture/game-design/economy-model.md`)
- [ ] `CitySimulation::getDensityUnlockState()` method exposed on `CitySimulation`: returns a struct with `consecutive_months_above_threshold[6]` (one counter per density tier, 0–2 range, represents the consecutive-month window state) and `unlock_flags[6]` (bool per tier); required for Phase 8 save round-trip test to verify counter persistence across save/load. (ref: `architecture/game-design/economy-model.md`, `architecture/game-design/save-system.md`)
- [ ] **Traffic rolling-average serialization**: `CitySimulation` must expose `getTrafficRollingAverage(ZoneType)` (returning the last 5-tick rolling window state for C demand and last 3-tick window for I demand); these values must be included in the simulation save state and verified in `SaveSystem_RoundTrip_PreservesFullCityState` in Phase 8. **Speed state serialization**: `CitySimulation` must expose `getSpeedMultiplier()` returning the current `SpeedMultiplier` enum; this value must also be serialized so a loaded save resumes at the saved speed. Both are `gamedesign-lookandfeel` deliverables; `test-dev-cpp` verifies them in Phase 8 save round-trip tests.
- [ ] **`getDemandPressurePct` documentation**: `ICitySimulation::getDemandPressurePct(ZoneType)` returns the **city-wide** demand pressure for the given zone type (weighted average of demand_factor across all tiles of that type, not a per-tile value). This city-wide semantic must be documented in `ICitySimulation.h` comments and referenced in `HUD Layout` demand bar display spec. Per-tile demand is available via `QueryResult::demand_pressure_pct` (Query/Inspector Panel only).
- [ ] **`current_C_population` disambiguation**: in the Commercial demand formula `C demand = R_population / total_C_worker_capacity`, `current_C_population` (if referenced elsewhere in the spec) refers to **actual occupancy** — the number of workers currently employed at C tiles — NOT the capacity (maximum possible workers). Capacity is `total_C_worker_capacity`. Document this distinction in `SimulationConstants` comments and `zoning-system.md` demand formula references to avoid ambiguity in implementation. (ref: `architecture/game-design/zoning-system.md`)
- [ ] **`monthly_shortfall` definition**: define `monthly_shortfall = max(0, total_monthly_expenses − monthly_revenue)` where `total_monthly_expenses = road_maintenance + service_upkeep + wage_cost`. This quantity is used in the forced loan amount formula `max(monthly_shortfall × 3, monthly_revenue × 0.5, $10,000)`. The definition must appear in `CitySimulation` comments (near the forced loan trigger logic) and be referenced in `ForcedLoan_Amount_Formula` test. (ref: `architecture/game-design/economy-model.md`)
- [ ] `GracePeriod_RoadPlacementCost_NotWaived` unit test (label `unit`): set `ManualClock` to 60 s (within grace period), place a road tile, verify treasury is immediately decremented by $500 (`SimulationConstants::road_placement_cost_per_tile`); verify no road maintenance deduction occurs in the same tick; advance `ManualClock` to 121 s, fire budget tick, verify road maintenance is now deducted. (ref: `architecture/game-design/economy-model.md`)
- [ ] `ServiceCoverage_NewlyPlacedTile_NoPenaltyOnFirstTick` unit test (label `unit`): place an R tile with no fire station in range; run one budget tick; verify desirability has NOT decreased by 5 (no penalty on first tick of absence); run second budget tick; verify desirability has decreased by 5. (ref: `architecture/game-design/service-coverage.md`)
- [ ] `BondRepayment_NonDivisiblePrincipal_LastTickAbsorbsRemainder_24Tick` unit test (label `unit`): issue Emergency Municipal Bond with principal $24,001 over 24 ticks; verify ticks 1–23 each repay $1,000 (= `floor($24,001 / 24)` = $1,000); verify tick 24 repays $1,001 (= remainder, $24,001 − 23 × $1,000). (ref: `architecture/game-design/economy-model.md`)
- [ ] Zone placement fires `IAudioSystem::playPositionalSound(SFX_EARTHWORKS, tile_position)` (or equivalent callback on `IAudioSystem`) when earthworks cost > 0 at zone placement time — enables Phase 7 `sfx_earthworks` wiring. (ref: `architecture/audio-architecture/v1-audio-asset-manifest.md`) **Required Phase 3 unit test**: `EarthworksCost_Nonzero_FiresAudioCallback` (label `unit`) in `tests/simulation/zoning_test.cpp`: place a zone tile on terrain with slope > 15° (earthworks cost > 0), use `EXPECT_CALL(*m_audio, playPositionalSound(SFX_EARTHWORKS, _, SoundPriority::NORMAL))` on `StrictMock<MockAudioSystem>`, verify exactly one call fires. Owner: `gamedesign-lookandfeel` (simulation event) + `test-dev-cpp` (test). Phase 7 `sfx_earthworks` wiring is blocked if this callback is not delivered in Phase 3.
- [ ] `BondModal_ExhaustedUses_ButtonGrayedOut`: when `outstanding_bond_uses == 0`, verify `setElementEnabled(false)` called on the Emergency Bond button in the forced loan modal (uses `StrictMock<MockUIBackend>`) (ref: `architecture/game-design/economy-model.md`, `architecture/ui-ux/modal-dialog-system.md`). **NOTE**: this test uses `MockUIBackend` and verifies UI behaviour — it belongs in the **`ui_tests`** CMake target (Phase 5), NOT in `simulation_tests`. List it here as a Phase 3 design requirement (the simulation logic that exposes `outstanding_bond_uses == 0`) but assign implementation to Phase 5's `ui_tests`. Attempting to compile it in `simulation_tests` will fail because `simulation_tests` does not link `MockUIBackend`.
- [ ] `ForcedLoan_Amount_Formula`: verify loan amount = `max(monthly_shortfall × 3, monthly_revenue × 0.5, $10,000)` using three independent test cases covering each branch as the binding maximum (ref: `architecture/game-design/economy-model.md`)
- [ ] `DensityUpgradeWave_EndsOnLowDemand_NoAutoRestart`: unlock a density tier then set all eligible tiles to `demand_factor = 0.74` (below 0.75 threshold); verify upgrade wave halts after one tick evaluation and does not auto-restart on the subsequent tick; verify unlock flag remains active and re-evaluation occurs when a future tick raises demand above threshold (ref: `architecture/game-design/zoning-system.md`)
- [ ] `DemandFloor_DoesNotPreventEmigration_OnBadConditions`: place R tiles with low desirability and no commercial/industrial adjacency; verify that demand floor does not prevent population from declining over multiple ticks (emigration path runs independently of demand floor). (ref: `architecture/game-design/zoning-system.md`) **Note**: this test must include a `// floatSeq: N values — M per desirability/population roll per tick × K ticks` audit comment; if the emigration path does not call `nextFloat()`, document explicitly: `// floatSeq: 1 placeholder — emigration path does not call nextFloat()`. Call `rng_.verifyAllConsumed()` in `TearDown()`.
- [ ] `CommercialDemand_NullPath_NoRollingSamples_DoesNotCrashOrDivideByZero` (label `unit`): place C tiles with no adjacent R population and no road access for 5+ ticks (all traffic window ticks are null-path); verify `getDemandPressurePct(COMMERCIAL)` returns 0.5 (null-path default) and no divide-by-zero or crash occurs. Guards the zero-denominator path in the C demand formula (`R_population / total_C_worker_capacity`) when total R population = 0. (ref: `architecture/game-design/traffic-system.md`, `architecture/game-design/zoning-system.md`)
- [ ] `ZombiePopulation_Commercial_EmigrationForcedWhenDemandZero` (label `unit`): place C tiles, establish initial population, then disconnect all road access and remove all adjacent R population so `demand_factor = 0` for C zone; run 10 budget ticks; verify C tile population trends toward 0 (emigration fires despite demand floor — demand floor only blocks `occupancy_increase_this_tick`, not the emigration path). Add equivalent `ZombiePopulation_Industrial_EmigrationForcedWhenDemandZero` for I zone. Add `ZombiePopulation_Residential_EmigrationForcedWhenDemandZero` (label `unit`): place R tiles, establish initial population, then remove all adjacent C/I tiles and all road access so `demand_factor = 0` for R zone; run 10 budget ticks; verify R tile population trends toward 0 (emigration path runs independently of R demand floor). All three zombie-population tests must be in `tests/simulation/zoning_test.cpp`. (ref: `architecture/game-design/zoning-system.md`)
- [ ] `BondUseCount_DecrementedOnIssuance` (label `unit`): issue an Emergency Municipal Bond; verify `outstanding_bond_uses` is decremented by 1 from its initial difficulty-based value (Easy=3→2, Normal=2→1, Hard=1→0). `BondUseCount_FloorAtZero_NoDoubleDecrement` (label `unit`): issue the last allowed bond (count goes to 0); attempt a second issuance (should be rejected — no additional decrement below 0); verify `outstanding_bond_uses` remains 0. Both tests belong in `simulation_tests/economy_test.cpp` using `StrictMock<MockAudioSystem>` and `ManualClock` (no `MockUIBackend` needed — the UI-layer `BondModal_ExhaustedUses_ButtonGrayedOut` test belongs in Phase 5 `ui_tests`). (ref: `architecture/game-design/economy-model.md`)
- [ ] Traffic unit tests:
  - `[ ] TrafficAgent_Timeout_LoggedAsExtremeTravel_NotNullPath`: agent timing out at 120 simulation seconds is logged as extreme-travel-time, NOT treated as null-path
  - `[ ] TrafficDemand_NullPath_Default_Is0_5`: zone tile with no adjacent road computes `demand_factor = 0.5` for null-path ticks (generic — applies when all window ticks for any zone type are null)
  - `[ ] IndustrialDemand_NullPath_Default_Is0_5_After3Ticks` (label `unit`): place I tiles with no road access for exactly 3 simulation ticks (I demand uses a 3-tick rolling average window); verify `getDemandPressurePct(INDUSTRIAL)` returns 0.5 after 3 null-path ticks. Guards against using the R/C 5-tick window for I demand (incorrect reuse would delay the null-path default to tick 5, producing wrong demand values for 2 ticks). Must be in `tests/simulation/traffic_test.cpp`. (ref: `architecture/game-design/traffic-system.md`)
  - `[ ] TrafficDemand_SmoothstepCurve_Residential_BoundaryValues`: at T=25 s `demand_factor_R = 1.0`; at T=60 s `demand_factor_R = 0.0`
  - `[ ] TrafficCongestion_GraduatedPenalty_31to40PctSpeed`: road segment running at 31–40% of max speed applies exactly −10% demand penalty (ref: `architecture/game-design/traffic-system.md`)
- [ ] Power coverage unit tests: `PowerCoverage_ConnectedTiles_AreCovered`; `PowerCoverage_IsolatedBuilding_Reports100Pct`; `PowerCoverage_DeficitDegradation_ReducesBFSRadius` (N=10 chain, boundary node verification); `PowerCoverage_MultipleBuildings_NoStacking` (ref: `architecture/testing/property-based-tests.md`)
- [ ] `ServiceDegradation_Order_FireFirstThenPoliceWaterPowerLast` (label `unit`): place one fire station, one police station, one power plant, and one water tower; trigger a −10% budget surplus deficit; verify `sfx_service_degrade` is fired exactly once with the fire station's service degraded first (halved radius), police second, water third, power last — degradation order must match the spec sequence (fire → police → water → power BFS brownout). Uses `StrictMock<MockAudioSystem>` with ordered `EXPECT_CALL`. (ref: `architecture/game-design/service-coverage.md`)
- [ ] `UndoSystem_Countdown_AmberImmediately_AtHighSpeed` (label `unit`): set simulation speed to 10× (which compresses real time per simulation second); advance `ManualClock` such that the undo window, originally 6 s total at 1×, is entirely within amber territory immediately at 10× speed (if the 6 s window is compressed to 0.6 real seconds at 10×, the amber threshold of < 5 s fires immediately); verify `setElementText()` is called on the undo button with amber formatting without any non-amber period. Guards high-speed edge case where the undo countdown is never shown in non-amber state. (ref: `architecture/game-design/undo-system.md`)
- [ ] `StrictMock<MockAudioSystem>` / `StrictMock<MockRenderer>` for unit tests; `NiceMock` for property tests; `TearDown()` resets `sim_` before mock destruction (ref: `architecture/testing/testability-architecture.md`)
- [ ] Seed reproduction on RapidCheck failure: print `// Reproduce with seed: 0x<hex>`; add fixed-seed regression before closing finding (ref: `architecture/testing/procedural-generation-seeds.md`)

### Exit Criteria
- Economy, traffic, and zoning simulations run for 60 budget ticks without crash
- All unit and property-based tests pass with 80%+ coverage gate green on `coverage-linux`
- Loan mechanics fire at the correct deficit thresholds and respect the 120 s real-time gate
- City Rating transitions fire correctly at the defined population thresholds
- Traffic demand coupling produces visible zone-growth/decay feedback in manual play-testing

### Team
| Role | Responsibility |
|---|---|
| `gamedesign-lookandfeel` | Economy balance, traffic thresholds, zoning demand tuning, service coverage tuning |
| `test-dev-cpp` | All simulation unit/property tests, `ManualRNG`, `ManualClock`, `MockAudioSystem`, `MockRenderer`, StrictMock/NiceMock setup |

### Dependencies
- Requires Phase 1 complete (for `IRenderer`, `IAudioSystem` interfaces; `IAudioSystem` resides in `src/interfaces/`)
- Phase 2 terrain buildability data (slope > 15°) needed before earthworks cost can be tested end-to-end (can be stubbed in unit tests)

### Risks & Spikes
- **RISK**: Loan pooling with multi-loan scenarios produces complex remainder arithmetic. **Spike**: prototype the multi-loan repayment formula in isolation and validate against the spec's $10,001 example before wiring into `CitySimulation`.
- **RISK**: Demand coupling bootstrapping decay may cause oscillation in early game at 3× speed. **Spike**: run simulation for 10 ticks at 3× with a blank map and trace demand_factor values per zone type.
