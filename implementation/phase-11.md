## Phase 11: Save System & Game Flow

### Goal

Deliver complete save/load, main menu game flow, scenario mode win/loss conditions, and clean game-over path.

### Deliverables

- [ ] **Save System**: JSON format V1; 1 auto-save slot + 3 manual slots; auto-save every 120 real seconds OR 5 budget ticks (whichever comes first; timer pauses during pause/modal); auto-save triggered immediately when forced loan dialog becomes active (before modal is shown); auto-save triggered when player opens the Pause Menu (before displaying it); save file paths: `~/.config/aitown/saves/` (Linux), `%APPDATA%\aitown\saves\` (Windows); manual save slot selection dialog showing 3 slots with timestamps, overwrite confirmation modal; Quit-to-Desktop / Quit-to-Main-Menu unsaved-changes check and blocking modal ("You have unsaved progress. Save and Quit / Quit Without Saving / Cancel"); Load Last Save loads most recent save across all slots (auto-save or manual), sorted by timestamp; Load Last Save button grayed with tooltip "No save file found." if no save exists (ref: `architecture/game-design/save-system.md`)
- [ ] Scenario mode save directory (`saves/scenarios/`) is NOT created in V1 — Scenario mode is grayed post-V1. The `ScenarioState` struct (with `win_condition_progress`, `elapsed_ticks`, `scenario_id`) is serialized internally and exists in the V1 code, but the directory and any scenario save files are post-V1 scope. (ref: `architecture/game-design/save-system.md`, `architecture/game-design/game-progression-modes.md`)
- [ ] **Game Over Flow integration**: `UIManager::transitionToGameOver()` contains no internal `GameMode` check. The `GameMode` guard is enforced exclusively at the `UIManager::update()` call site: check `GameMode == Scenario` before calling `transitionToGameOver()`. `CitySimulation` must NOT reference `GameMode` (a UI-layer enum in `src/ui/ui_types.h`) at any point — `CitySimulation` evaluates the game-over condition purely on simulation state. `transitionToGameOver()` only fires in Scenario mode (no-op path in Sandbox is via the guard in `UIManager::update()`, not inside `transitionToGameOver()`); progressive warning system: month 1 CRITICAL toast "City Finances Critical — 2 months to bankruptcy if deficit continues" + `setSpeed(SpeedMultiplier::x1)` auto-slow (player retains speed selector control); month 2 CRITICAL toast "City Finances Critical — 1 month to bankruptcy" + persistent red flashing budget indicator in resource bar; month 3 game-over modal (title "City Bankrupt"; body: current debt, months in deficit; options: Load Last Save (after deserialization completes, the `graphics-dev-irrlicht` loading controller (see Loading screen integration deliverable below) must call `UIManager::onGameLoaded()` before the first `UIManager::update()` tick — per the `stinger_milestone` dispatch contract in the `stinger_milestone` wired deliverable above) / Return to Main Menu); streak-break Normal toast "Deficit streak broken — finances stabilizing" when deficit recovers above −50% (red flashing indicator removed; speed not artificially constrained); streak-reset rule: streak counter resets on any tick with deficit > −50%; clean exit on any game-over modal option (clean simulation shutdown before transitioning; no abrupt exit). **`gamedesign-ux` scope for game-over modal is in-scope (wires to Sandbox no-op and Scenario triggering path). Win modal and HUD goal tracker are stub layouts only in V1 — no functional wiring; post-V1 enablement deferred.** (ref: `architecture/game-design/game-over-flow.md`, `architecture/game-design/game-progression-modes.md`)
- [ ] **Main Menu integration**: "Load Game" button state (grayed + tooltip "No saves found" / "Save data is corrupted — check [path]" / enabled); New Game → loading screen → terrain generation → gameplay transition; Quit-to-Main-Menu clean simulation shutdown (ref: `architecture/ui-ux/main-menu-new-game-flow.md`)
- [ ] **Scenario mode** (V1 scope: grayed "Post-launch" in New Game screen; Sandbox mode fully operational). Internal simulation code: `ScenarioState` struct with `win_condition_progress`, `elapsed_ticks`, and `scenario_id` fields — serialized and deserialized in `SaveSystem_RoundTrip`. The struct is internal and not player-accessible in V1; Scenario mode is grayed post-V1. Scenario save directory (`saves/scenarios/`) is NOT created in V1. (ref: `architecture/game-design/game-progression-modes.md`, `architecture/game-design/save-system.md`)
- [ ] `keybindings.json` at correct platform path; loaded at startup; invalid Q/E bindings from file rejected with warning (ref: `architecture/ui-ux/hotkey-scheme.md`)
- [ ] `stinger_milestone` wired to City Rating transitions (via Phase 10 `AudioSystem`); City Rating HUD label was delivered in Phase 8. **Dispatch ownership**: `UIManager` observes `CityRatingTier` changes (via `ICitySimulation::getCityRating()`) each `UIManager::update()` call and calls `AudioSystem::triggerStinger(StingerType::MILESTONE)` — the same pattern already established in Phase 10 for `stinger_crisis`. `CitySimulation` must NOT call `triggerStinger` directly. **Tier boundaries** (from `CityRatingTier` enum in `simulation_types.h`): Village→Town at 1K, Town→City at 10K, City→Metropolis at 50K, Metropolis→Megalopolis at 500K population. The dispatch logic checks `CityRatingTier` transitions, NOT raw population values — `stinger_milestone` fires when and only when the `CityRatingTier` enum value changes upward. **100K rule**: the 100K population milestone fires a toast notification only — do NOT call `triggerStinger(StingerType::MILESTONE)` at 100K population, because 100K does not correspond to a `CityRatingTier` enum boundary in V1. **Per-frame polling pseudocode**: `if (m_sim->getCityRating() > m_previousCityRating) { m_audio->triggerStinger(StingerType::MILESTONE); } m_previousCityRating = m_sim->getCityRating();` — the stinger fires exactly once per upward tier transition; no stinger fires on load: UIManager must call `m_previousCityRating = m_sim->getCityRating()` in its load-completion handler (e.g., `UIManager::onGameLoaded()`) to re-initialize the cache to the current state; `m_previousCityRating` is NOT serialized by SaveSystem. The `graphics-dev-irrlicht` loading controller (see Loading screen integration deliverable below) must call `UIManager::onGameLoaded()` immediately after deserialization completes and before gameplay resumes, so that `m_previousCityRating` is re-initialized to the current tier before the first `update()` tick. (ref: `architecture/game-design/game-progression-modes.md`, `architecture/audio-architecture/dynamic-soundscape.md`) **Phase 11 UIManager stinger dispatch test**: `UIManager_StingerMilestone_FiresOnTierTransition` in `tests/ui/ui_manager_stinger_test.cpp` (label: `unit`; added to `ui_tests` CMake target). This test verifies: (a) `triggerStinger(StingerType::MILESTONE)` is called exactly once when `getCityRating()` returns a higher value than the previous frame; (b) no stinger fires when `UIManager::onGameLoaded()` is called (cache re-initialized to the current tier, suppressing a spurious transition); (c) no stinger fires at 100K population (same tier as 50K→Metropolis, tier value unchanged — 100K does not correspond to a `CityRatingTier` enum boundary). Uses `StrictMock<MockCitySimulation>`, `StrictMock<MockAudioSystem>`, and `NiceMock<MockUIBackend>`. `MockCitySimulation` and `MockAudioSystem` are `StrictMock` (direct assertions on `getCityRating()` and `triggerStinger()` calls). `MockUIBackend` is `NiceMock` because `UIManager` makes incidental backend calls (element visibility, text, alpha) during `update()` and `onGameLoaded()` that are not under assertion in this test — consistent with the `UIManagerModalTest` fixture pattern. (ref: `architecture/testing/testability-architecture.md`)

- [ ] **Loading screen integration** (`graphics-dev-irrlicht`): (a) the main loop during terrain generation follows a strict per-frame sequence: `audioSystem->syncListenerToCamera(cam)` → `audioSystem->update(realDeltaSeconds)` → `UIManager::update(dt)` → `terrainSystem->update(dt)` → `TerrainSystem::flushPendingRebuilds()` once per frame (100 ms CPU budget per call — NOT a blocking loop-until-empty; exits after 100 ms regardless of remaining queue depth; called exactly once per frame, NOT in a loop-until-empty for the loading session) → `driver->beginScene()` → `sceneManager->drawAll()` → `UIManager::draw()` → `driver->endScene()` to render one loading screen frame advancing the spinner animation — this ensures the spinner animates every frame and the loading screen never stalls. `terrainSystem->update(dt)` is required even during the loading screen to advance LOD state and process any queued rebuilds that `flushPendingRebuilds()` did not drain within its 100 ms budget; without it, stale LOD state accumulates across frames and chunks queued after the 100 ms deadline are never promoted. The spinner animation, progress bar, and Cancel button are UIManager elements, so `UIManager::draw()` must be included or the loading UI never renders. [Note: `guiEnvironment->drawAll()` is intentionally omitted from the loading screen loop — UIManager renders all spinner/progress bar/cancel-button elements via `UIManager::draw()` using Irrlicht draw primitives; the Irrlicht GUI environment is not used for loading screen elements. Hover tile highlight rendering is also omitted: no tile interaction occurs during terrain loading.] [graphics-artist-2d-texture scope: spinner and progress bar are procedurally rendered using Irrlicht draw primitives (`IVideoDriver::draw2DRectangle` and equivalent calls) — no sprite sheet additions required for Phase 11.] [graphics-artist-3d-model scope: no road tile meshes required — road tile LOD geometry is procedurally generated in C++ per `architecture/asset-standards/3d-model-standards.md`; do NOT author road `.b3d`/`.obj` files.] (b) integration test named `TerrainSystem_FlushPendingRebuilds_ProcessesAllChunks_NoDuplication` in `tests/integration/terrain_loading_test.cpp` (label `integration`; added to `integration_tests` target via `target_sources(integration_tests PRIVATE tests/integration/terrain_loading_test.cpp)`): uses a `TerrainSystemTest` fixture with an `EDT_NULL` Irrlicht device and null audio driver (no `xvfb` needed); queue N=5 chunks with distinct IDs in `TerrainSystem`, call `flushPendingRebuilds()`, verify the deque is empty after the call and no chunk ID was processed twice (deduplication check using `std::unordered_set<uint64_t>`). This is a NEW integration test — distinct from `TerrainSystem_FlushPendingRebuilds_EDT_NULL_DoesNotCrash` in Phase 5 which only verifies no-crash; both coexist in the `integration_tests` target; this Phase 11 test verifies deduplication correctness and empty-deque post-condition. CTest label: `integration`. (c) After the loading screen loop completes (deserialization done and terrain fully built), the loading controller calls `UIManager::onGameLoaded()` **synchronously once** — before the main game loop begins and before the first `UIManager::update()` tick of the gameplay session. This is the canonical and sole call site for `onGameLoaded()`; all load paths (New Game start, Load Last Save from Main Menu, Load Last Save from Game-Over modal) must invoke it at this point. (ref: `architecture/graphics-architecture/procedural-terrain.md`, `architecture/graphics-architecture/irrlicht-device-lifecycle.md`)
- [ ] Save system unit tests in `tests/simulation/` (CMake target `simulation_tests`, label `unit`). SaveSystem is constructed with an `IClock* clock` parameter (same injection pattern as `AudioSystem` and `CitySimulation`); production code passes `WallClock`, all save system tests (units and integration) pass `ManualClock` for deterministic time advancement.
  - `[ ] SaveSystem_RoundTrip_PreservesFullCityState`: serialize `CitySimulation` state to JSON string (no file I/O), deserialize, verify all fields match; verified fields must include at minimum: treasury balance, zone layout, population per tile, road layout, `outstanding_debt`, `outstanding_bond_uses`, `density_unlock_flags` per tier, `density_unlock_revenue_counter` per tier (3-consecutive-month window state), game-over streak counter, auto-save timer state, **`speed_multiplier`** (loaded save must resume at the saved speed — **Phase 11 serializes this** as part of the SaveSystem implementation; verify `getSpeedMultiplier()` after load matches the saved value), `population_milestone_fired[5]` (all 5 boolean flags match — milestones that fired before save must not re-fire after load), `m_buildingVariantCounters` (all 9 round-robin integer counters, indexed by `zone * 3 + tier`; restoring these ensures building variant selection continues the existing round-robin sequence after load rather than restarting from counter=0 and producing incorrect visual repetition — per `architecture/asset-standards/3d-model-standards.md` §91, counter serialisation is Phase 11 scope). Note: `m_previousCityRating` (UIManager stinger cache) is NOT in the save format; it is re-initialized by UIManager post-load. (ref: `architecture/game-design/save-system.md`, `architecture/game-design/economy-model.md`)
  - `[ ] SaveSystem_CorruptedJSON_ReturnsError`: loading invalid JSON returns an explicit error, not a crash
  - `[ ] SaveSystem_SchemaVersion_MismatchIsRejected`: `schema_version` field mismatch returns an explicit error
  - `[ ] SaveSystem_AutoSave_TriggersAtCorrectIntervals`: use `ManualClock` to advance time; verify auto-save fires at 120 real-second interval and at every 5th budget tick, whichever comes first (ref: `architecture/game-design/save-system.md`)
  - `[ ] SaveSystem_RoundTrip_CounterResetBehavior_AfterLoad`: save a game state with `density_unlock_revenue_counter = 2` (2 of 3 consecutive months above threshold) for at least one tier, load the save, simulate one budget tick with revenue below the threshold for that tier, verify the counter resets to 0 and no density unlock fires. Verifies counter persistence and correct reset behaviour across save/load cycles. (ref: `architecture/game-design/economy-model.md`, `architecture/game-design/save-system.md`)
  - `[ ] SaveSystem_AutoSave_SuspendedWhilePaused`: uses `StrictMock<MockCitySimulation>` to verify paused state flags; use `ManualClock` to advance time 120+ real seconds while `CitySimulation` is in paused state; verify auto-save does NOT fire during pause; un-pause and advance one tick; verify auto-save fires immediately. (ref: `architecture/game-design/save-system.md`)
  - `[ ] SaveSystem_AutoSave_ForcedLoanDialogTriggers`: construct `CitySimulation` (via `StrictMock<MockCitySimulation>`) and `SaveSystem` with `ManualClock`; trigger forced loan dialog event; verify auto-save fires immediately before the modal is shown (saved state is recoverable by load). Label: unit. (ref: `architecture/game-design/save-system.md`)
- [ ] Save system integration test in `tests/integration/save_system_integration_test.cpp` (CMake target `integration_tests`, label `integration`; added to `integration_tests` target via `target_sources(integration_tests PRIVATE tests/integration/save_system_integration_test.cpp)`):
  - `[ ] SaveSystem_AutoSave_SuspendedDuringModal`: uses `NiceMock<MockCitySimulation>` for the simulation dependency and `NiceMock<MockUIBackend>` passed to a real `UIManager` instance to simulate modal state (via `UIManager::showForcedLoanDialog(LoanTerms{})`); use `ManualClock` to advance time 120+ real seconds while the modal is open; verify auto-save does NOT fire; call `UIManager::closeModal()`; advance one tick; verify auto-save fires immediately. Uses `NiceMock` per integration test policy (CLAUDE.md). (ref: `architecture/game-design/save-system.md`)
- [ ] **CI/CD — 85% per-file floor gate** (`cicd-dev-github`): Add per-file 85% floor gate to `coverage-linux` job in `.github/workflows/ci.yml` (per `architecture/testing/coverage.md` § Phase 11 `src/simulation/` per-file 85% floor — deferred from Phase 6 and implemented here): immediately after the `src/simulation/` SF preflight step and **before** the 95% total gate, add an awk step that fails the build if any `src/simulation/` file has line coverage below 85%.

### Exit Criteria

- Save and load round-trips preserve full city state (treasury, zones, population, roads)
- Game-over flow triggers correctly after 3 consecutive −50% deficit months in Scenario mode
- Main menu correctly reflects save file state (grayed/enabled/corrupted)
- `keybindings.json` persists and loads correctly on both Linux and Windows
- All save system unit tests pass with coverage gate green (95% total line coverage gate + 85% per-file floor gate for `src/simulation/` — implemented as a Phase 11 CI deliverable in `.github/workflows/ci.yml`, deferred from Phase 6)

### Team

| Role | Responsibility |
|---|---|
| `gamedesign-lookandfeel` | JSON serialization schema, save slot management, Quit-to-Desktop unsaved-changes modal, game-over consequence tuning, scenario win-condition logic |
| `gamedesign-ux` | Main menu flow integration, game-over and win modal UI, HUD goal tracker |
| `graphics-dev-irrlicht` | Loading screen integration (spinner, cancel affordance, `TerrainSystem` flush call sequence) |
| `graphics-artist-2d-texture` | Confirm loading screen spinner and progress bar are procedurally rendered via `IVideoDriver::draw2DRectangle` — no sprite sheet assets required in Phase 11. |
| `graphics-artist-3d-model` | Verify asset naming conventions from Phase 9 are stable across save/load; no new asset authoring required in Phase 11 |
| `sound-dev-opensoftal` | Verify `stinger_milestone` fires only at City Rating tier transitions (1K, 10K, 50K, 500K); confirm 100K population fires toast only, no stinger. Verify DUCKED→RELEASING gate queries only sources[55] (`stinger_crisis`) and sources[56] (`stinger_milestone`) — `stinger_game_over` (sources[57]) is idle in V1 and must NOT be queried in the DUCKED→RELEASING check. (`stinger_game_over` and game-over audio are Scenario-mode post-V1 scope.) |
| `test-dev-cpp` | Save system unit tests (`SaveSystem_RoundTrip_PreservesFullCityState`, `SaveSystem_CorruptedJSON_ReturnsError`, `SaveSystem_SchemaVersion_MismatchIsRejected`, `SaveSystem_AutoSave_TriggersAtCorrectIntervals`, `SaveSystem_RoundTrip_CounterResetBehavior_AfterLoad`, `SaveSystem_AutoSave_SuspendedWhilePaused`, `SaveSystem_AutoSave_ForcedLoanDialogTriggers`) and integration test (`SaveSystem_AutoSave_SuspendedDuringModal` moved to `integration_tests`) |
| `cicd-dev-github` | Add per-file 85% floor gate awk step to `coverage-linux` job in `.github/workflows/ci.yml` |

> **Note**: Platform-specific save path resolution (`~/.config/aitown/saves/` on Linux, `%APPDATA%\aitown\saves\` on Windows) is documented in `architecture/game-design/save-system.md` and is owned by `gamedesign-lookandfeel` as part of the JSON serialization deliverable.

### Dependencies

- Requires Phase 8 complete (modal dialog system, notification system)
- Requires Phase 6 complete (simulation state serializable)
- Requires Phase 10 complete (stinger audio triggered by City Rating transitions)

### Risks & Spikes

- **RISK**: JSON save format schema versioning not specified in V1; adds risk for forward compatibility. **Spike**: define a `"schema_version": 1` field in the JSON root and add a version check on load before any field parsing.
