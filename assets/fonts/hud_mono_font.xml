<?xml version="1.0"?>
<!-- AI Town HUD monospace font — Phase 10 placeholder.
     Replace with a real Irrlicht bitmap font exported from FontTool or bmGlyph
     at 18 px monospace, covering ASCII 32–126, before Phase 10 exit review.
     The companion texture file hud_mono_font_0.png must reside in the same
     directory. IrrlichtUIBackend loads this file in its constructor via
       m_guiEnv->getFont("assets/fonts/hud_mono_font.xml")
     and stores it in m_monoFont for use by setElementMonoFont(handle).
     If the file is absent or malformed, m_monoFont remains null and
     setElementMonoFont() is a no-op (graceful fallback, no crash).
     This stub passes the Phase 10 CI asset-presence gate (Step 12e in ci.yml)
     without satisfying the full exit criterion ("loads without error, no
     fallback warning logged"). The real font delivery must replace this file.
     Numeric elements that call setElementMonoFont(): treasury balance display,
     population count display, tax rate fields, monthly revenue/expense fields,
     density unlock progress threshold value, in-game date/time display.
     See architecture/ui-ux/hud-layout.md Font Loading — Monospace requirement. -->
<Font type="bitmap" Texture="hud_mono_font_0.png" WrongCharacter="32" MaxHeight="18">
</Font>
