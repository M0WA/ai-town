## Phase 5: HUD & UI Panels

### Goal
Deliver all V1 UI panels: HUD resource bar, toolbar, minimap, tax rate panel, query/inspector panel, modal dialog system, notification system, settings/pause menu, and full input arbitration.

### Deliverables
- [ ] **HUD Layout**: resource/budget bar (top); time controls + speed selector (top-right); toolbar (left panel, x:8–72 px, y:64–600 px, 48×48 px icons, 8 px margin); undo button (x:8–72, y:608–656 px, `setElementEnabled` for grayed state); unsaved-changes dot (16×16 px, x:1796–1812, y:8–24 px); grace period indicator beneath resource bar with interactive tooltip on hover/click: 'During the grace period, road maintenance ($10/tile/month) and building upkeep costs are waived. These costs will begin in approximately Xs' (where X = `floor(120 − elapsed)`) 'Estimated monthly upkeep when active: $[calculated from current city].' This real-seconds wording replaces any 'month count' phrasing per `architecture/ui-ux/hud-layout.md`. Estimated figure updates each real second via `CitySimulation::estimateMonthlyUpkeep()`. Assigned jointly to `gamedesign-lookandfeel` (simulation query) and `gamedesign-ux` (tooltip display); demand pressure bar (x:8–72, y:664–744 px, R/C/I bars with ≥9pt labels or numeric tooltip); active tool indicator (x:8–72, y:752–784 px); notification bell (x:1820–1868, y:8–56 px); City Rating label in HUD resource bar (reads `CitySimulation::getCityRating()`); stinger hookup deferred to Phase 7 (ref: `architecture/ui-ux/hud-layout.md`, `architecture/game-design/game-progression-modes.md`)
- [ ] Grace period countdown: 'Cost waiver: Xs remaining' label beneath resource bar where X = `floor(120 − elapsed)`; reads `IClock::nowSeconds()` elapsed since game start; updates every real second via wall-clock (not simulation delta). **Grace period indicator tooltip text**: "During the grace period, road maintenance ($10/tile/month) and building upkeep costs are waived. These costs will begin in approximately Xs" where X = `floor(120 − elapsed)` — this real-seconds wording replaces any 'month count' phrasing; the grace period is a real-time measurement per `architecture/ui-ux/hud-layout.md`, not month-aligned. The tooltip second sentence remains: "Estimated monthly upkeep when active: $[calculated from current city]." On expiry (elapsed ≥ 120 s), fade the indicator from full opacity to 0 over 0.5 real seconds using `IUIBackend::setElementAlpha`, then call `setElementVisible(false)`. (ref: `architecture/ui-ux/hud-layout.md`, `architecture/game-design/economy-model.md`)
- [ ] Density unlock progress indicator in resource bar: compact display showing current monthly revenue vs. next locked density tier threshold (at difficulty-adjusted value), e.g., 'Revenue: $42K / $50K'; reads from `ICitySimulation::getNextUnlockThreshold(difficulty)` and `ICitySimulation::getCurrentMonthlyRevenue()`; hidden once all tiers unlocked (ref: `architecture/game-design/economy-model.md`, `architecture/ui-ux/hud-layout.md`)
- [ ] Density unlock preview: resource bar tooltip shows 'After Unlock: ~+$X/month expenses' projection when current monthly revenue is within 10% of any unlock threshold; projection computed as estimated wage increase from upgrading 20% of eligible tiles (one upgrade wave tick); formula agreed between `gamedesign-lookandfeel` and `gamedesign-ux` before implementation
- [ ] **Budget Detail Panel**: 320×200 px floating panel; triggered by hover/click on the treasury balance field in the resource bar; shows 8 named line items: Tax revenue R (residential), Tax revenue C (commercial), Tax revenue I (industrial), Wages, Road maintenance, Service upkeep per type (fire/police/power/water as sub-items), Utility fees (power/water), Net monthly balance; Z-order: above all HUD elements, below modal scrim; dismisses on outside click; Density Unlock Preview Tooltip shown as a supplemental line when current monthly revenue is within 10% of any unlock threshold. All values read from `ICitySimulation` and `SimulationConstants`; required for player to diagnose overspending from high-density Commercial/Industrial zones. (ref: `architecture/ui-ux/hud-layout.md`, `architecture/game-design/economy-model.md`)
- [ ] **Tax Rate Panel**: 300×200 px floating panel; 3 rows (R/C/I); +/- buttons with key-repeat (400 ms initial, 150 ms repeat, ±5 pp hold cap); direct text entry; rate bounds 1%–25%; dismiss-click pass-through (outside clicks NOT consumed — pass through to allow camera pan in same click, per `input-arbitration.md` Priority 4); "Tax changes cannot be undone" label; unsaved-changes sets `m_hasUnsavedChanges=true`; pending rate indicator: after Tax Rate Panel closed with changed rates, resource/budget bar shows amber label 'Tax rates updating next budget cycle' until next budget tick commits the change; state tracked as `bool m_taxRatePending`; label cleared on tick commit (ref: `architecture/ui-ux/tax-rate-panel.md`, `architecture/ui-ux/input-arbitration.md`)
- [ ] **Query/Inspector Panel**: 240×160 px; cursor-offset formula with 3-step cascade (primary → fallback → edge-snap); tile type fields (zone, road, service); data refresh: budget data per tick, traffic data every 10 frames, service data per tick; "Updated N seconds ago" line; Escape consumed by panel (fires passive toast "Panel closed — press Escape again to open Pause Menu" if simulation running — this toast is a non-interactive display element ONLY: no click-dismiss, no input consumption, not registered in the input arbitration priority chain; a second Escape press within the 1.5 s window passes through the toast to UIManager and opens the Pause Menu normally); toolbar and minimap carve-out exceptions on dismiss-click (ref: `architecture/ui-ux/query-inspector-panel.md`)
- [ ] **Minimap**: 200×200 px (x:1720–1920, y:880–1080); R=green, C=blue, I=orange zone coding; road network grey lines; camera viewport rectangle (min 8×8, max 190×190 px); click-to-pan; overlay toggle row at top edge (x:1720–1752, y:848–880 px); Service Coverage overlay (fire=red, police=blue, power=yellow, water=cyan); legend panel (80×100 px) above label strip; label strip 16 px; legend at y:732–832 px (NOT inside minimap bounds); budget-tick cadence updates; scrim consumes all minimap clicks when blocking modal active (ref: `architecture/ui-ux/minimap.md`)
- [ ] **Modal Dialog System**: `ModalDialog` owned by `UIManager` at topmost Z-order; sizes Small=480×240 / Medium=560×320 / Large=640×400 px; background scrim 50% opacity `IGUIElement` fill rect; keyboard nav (Tab, Enter, Escape); default focus on least-destructive button; 2px accent-color focus ring; auto-pause simulation on modal open; speed selector `setElementEnabled(false)` during modal (not `setElementVisible`); simulation resumes only on full modal dismiss (ref: `architecture/ui-ux/modal-dialog-system.md`)
- [ ] Forced loan dialog: Large (640×400 px), two-screen (Accept/Decline → Budget Action Required); Option 1 raise all rates ×1.10 (cap 25%); Option 2 demolish last-placed; Option 3 Emergency Municipal Bond with per-difficulty use counts (Easy=3/Normal=2/Hard=1) at **5%/year** (per `economy-model.md` — see spec contradiction flag in Phase 3); Back button always enabled; deadlock prevention (debt-cap override when all three options grayed); auto-pause on open; Escape consumed (non-dismissible); Screen 1 shows standard loan terms (5%/year interest, 12-tick repayment); Screen 2 shows bond terms (5%/year interest, 24-tick repayment) (ref: `architecture/ui-ux/modal-dialog-system.md`, `architecture/game-design/economy-model.md`)
- [ ] Demolish confirmation: Small modal; "Do not ask again" checkbox with no-confirm badge on toolbar icon and Settings > Gameplay re-enable toggle (ref: `architecture/ui-ux/modal-dialog-system.md`)
- [ ] Game-over modal: Medium (560×320 px), non-dismissible; Load Last Save + Return to Main Menu; default focus on Load Last Save (ref: `architecture/ui-ux/modal-dialog-system.md`)
- [ ] WASD camera preset confirmation modal: Small (480×240 px); shows current bindings of affected keys; Apply atomically rebinds and writes `keybindings.json`; Cancel default Tab focus (ref: `architecture/ui-ux/modal-dialog-system.md`)
- [ ] **Notification System**: `NotificationManager` with `IClock*`, `IUIBackend*`, `ISimulationPauser*` injection; CRITICAL queue (top band y:20–116, 48 px fixed height, max 2 visible, no auto-dismiss, explicit dismiss via click/Enter/Delete); Normal queue (starts y:130, 40–63 px height, max 3/2/1 visible, 5 s auto-dismiss, max depth 10 log fallback); 320 px total cap; CRITICAL toast auto-pause (`setPaused(true)` on empty→non-empty, no auto-resume); CRITICAL toasts hidden during blocking modal (returned to queue front, displayed after dismiss); `dismissCriticalToast(UIElementHandle)` production API; **notification log panel: 400×500 px; anchor: top-right corner of panel aligns to bottom-right corner of bell icon** (bell icon at virtual x:1820–1868, y:8–56; panel at virtual x:1468–1868, y:56–556 — fully within 1920×1080 virtual space; **do NOT anchor bottom-left of panel to bottom-left of bell icon — that formula yields y = 56 − 500 = −444 which renders above the viewport**); Z-order: above all HUD elements, below modal scrim; dismiss on outside click; scrollable list of last 50 notifications with timestamp, priority badge (CRITICAL/Normal color coding), and message text; toggled open/closed via B key and bell icon click; unread-count badge on bell resets to 0 on log open; log persists in session (cleared on New Game) (ref: `architecture/ui-ux/notification-system.md`)
- [ ] **Settings / Pause Menu**: pause menu (Resume, Settings, Save, Quit to Main Menu, Quit to Desktop); `m_hasUnsavedChanges` dirty flag (set on zone/road/demolish/tax change and economy events; cleared on successful save); auto-save every 120 real seconds or 5 budget ticks (whichever comes first; timer pauses when game paused or modal active); auto-save triggered when player opens Pause Menu (before displaying it); auto-save on forced loan dialog becoming active; auto-save failure shows CRITICAL toast; manual save failure shows blocking error modal (Retry/Cancel); Quit-to-Desktop and Quit-to-Main-Menu check `m_hasUnsavedChanges`; Settings tabs (Graphics, Controls, Audio, Gameplay); Graphics tab display-change countdown (10 s wall-clock via `IClock::nowSeconds()`); Controls tab rebinding with conflict detection and Swap/Cancel; Restore Defaults confirmation (ref: `architecture/ui-ux/settings-pause-menu.md`)
- [ ] **Input Arbitration** (strict priority order per `input-arbitration.md`): (1) ModalDialog — blocks all except camera MMB/RMB/scroll; (2) CRITICAL toast dismiss (skipped when modal active or no CRITICAL toast visible); (3) QueryPanel/InspectorPanel — Escape consumed, dismiss-click consumed except toolbar/minimap carve-out; (4) TaxRatePanel — Escape consumed, outside clicks pass-through (NOT consumed — per spec contradiction #7 resolution, `input-arbitration.md` takes precedence); (5) UIManager HUD controls + Ctrl+Z undo; (6) CameraController; mutual exclusion: QueryPanel and TaxRatePanel cannot both be open (ref: `architecture/ui-ux/input-arbitration.md`)
- [ ] **Hotkey Scheme**: Z=Zone, R=Road, U=Utilities, D=Demolish, I=Inspector, T=Tax panel, B=Notification log, Space=Pause, +/==speed up, -=speed down, Escape=pause menu (gameplay), Ctrl+Z=Undo (non-rebindable, chord), Ctrl+S=Save (non-rebindable, chord); Q/E reserved (grayed in rebinding UI, rejected if typed); `keybindings.json` at platform save path (ref: `architecture/ui-ux/hotkey-scheme.md`)
- [ ] WASD preset: atomic rebind (PanUp=W, PanDown=S, PanLeft=A, PanRight=D, Demolish→X); confirmation preview modal before applying (ref: `architecture/ui-ux/hotkey-scheme.md`)
- [ ] **Main Menu & New Game Flow**: full-screen main menu (New Game/Load Game/Settings/Quit); New Game screen (Sandbox only, Scenario grayed "Post-launch"); difficulty radio buttons; map seed text input (integer validation, inline error label); Randomize button; Start City → loading spinner with percentage + Cancel affordance (Cancel grayed/labeled "Finalizing..." at last checkpoint); minimum 0.5 s display; Escape = Back on all pre-gameplay screens; keyboard Tab navigation on all screens; Load Game grayed with correct tooltip (ref: `architecture/ui-ux/main-menu-new-game-flow.md`)
- [ ] **Resolution & UI Scaling**: `UIScaler` uniform scale with letterbox/pillarbox; mouse un-projection with letterbox offset; minimum 1280×720; no hardcoded pixel offsets; minimum body font 14 px virtual (11 px physical floor); monospace for numeric readouts; **Colorblind Mode toggle placed in Settings > Graphics tab, Accessibility subsection** per `architecture/ui-ux/resolution-ui-scaling.md` and `architecture/ui-ux/settings-pause-menu.md` (both specs are already aligned — no contradiction remains). The toggle MUST NOT appear in any other Settings tab. **Colorblind Mode must be fully wired in Phase 5** — the toggle must functionally switch: (1) minimap zone colors to colorblind-safe pattern-supplemented encodings (per `resolution-ui-scaling.md` encoding table); (2) demand pressure bar R/C/I colors to use pattern/shape cues instead of hue-only coding; (3) zone tool cursor tints to colorblind-safe variants; (4) service coverage overlay to pattern-supplemented encodings (hatching or shape markers, not color alone). Toggle state is persisted in `Settings` and applied on Settings load. Deferring colorblind wiring to Phase 9 (hardening) is not acceptable — the functional wiring must be delivered with the toggle in Phase 5. (ref: `architecture/ui-ux/resolution-ui-scaling.md`, `architecture/ui-ux/settings-pause-menu.md`)
- [ ] **UIManager event dispatch** aligned to `input-arbitration.md` priority order exactly: (1) ModalDialog consumes all non-camera events; (2) CRITICAL toast dismiss; (3) QueryPanel/InspectorPanel — Escape consumed, dismiss-click consumed EXCEPT toolbar and minimap carve-outs; (4) TaxRatePanel — Escape consumed, outside clicks NOT consumed (pass-through — `input-arbitration.md` is the canonical authority); (5) HUD controls + Ctrl+Z undo; (6) CameraController. **TaxRatePanel outside-clicks are NOT consumed (pass-through); QueryPanel dismiss-clicks ARE consumed except toolbar and minimap carve-outs.** (ref: `architecture/ui-ux/ui-manager.md`, `architecture/ui-ux/input-arbitration.md`)
- [ ] `ISimulationPauser.h` in `src/interfaces/` (not `src/simulation/`); `MockSimulationPauser` in `tests/ui/` (ref: `architecture/testing/testability-architecture.md`)
- [ ] **Note**: `ICitySimulation.h` interface delivered in Phase 1 (see `phase-1.md`); Phase 5 adds `UIManager` integration with the interface and completes the full HUD panel set using all `ICitySimulation` methods. `getOutstandingDebt()` is introduced via `CitySimulation` in Phase 3; `UIManager` begins reading it in Phase 5 HUD wiring. `getUndoExpiryTimeSeconds() -> double` returns the real-time seconds from `IClock` when the current undoable action expires (required for HUD undo countdown display).
- [ ] `UIManager` updated to accept `ICitySimulation*` (not concrete `CitySimulation*`) (ref: `architecture/testing/testability-architecture.md`)
- [ ] `MockCitySimulation` in `tests/ui/mock_city_simulation.h` (`NiceMock`, implements `ICitySimulation`) — stub created in Phase 1; Phase 5 adds any additional `MOCK_METHOD` declarations required by new Phase 5 HUD query methods (ref: `architecture/testing/testability-architecture.md`)
- [ ] UIManager modal/toast unit tests (StrictMock`<MockUIBackend>`, NiceMock`<MockAudioSystem/CitySimulation>`; `TearDown()` resets `ui_` before mocks destroyed): all 10 required test cases including `ModalDialog_OnClose_WithQueuedCriticalToast_AutoPauseReevaluated` (deferred `addStaticText` synchronously within `closeModal()`; `setPaused(true)` × 2 total, `setPaused(false)` × 0) (ref: `architecture/testing/testability-architecture.md`)
- [ ] `QueryPanel::computePanelPosition()` pure function unit tests: 4 required cases including third-fallback edge-snap at (960, 540) producing `{1680, 460, 240, 160}` (ref: `architecture/testing/testability-architecture.md`)
- [ ] `KeyBindings` unit tests: 5 required cases including Q/E rejection and round-trip JSON (ref: `architecture/testing/testability-architecture.md`)
- [ ] `NotificationManager` unit tests: `CriticalToast_OnPost_AutoPausesCalled`; `CriticalToast_OnLastDismiss_NoAutoResume`; `CriticalToast_SecondPost_NoDoublePause` (ref: `architecture/testing/testability-architecture.md`)
- [ ] `NotificationManagerTest` fixture MUST use `TearDown()` to reset the `NotificationManager` before `MockUIBackend` is destroyed. Use pointer reset: declare `nm_` as `std::unique_ptr<NotificationManager>` in the fixture and call `nm_.reset()` (std::unique_ptr reset, NOT `NotificationManager::reset()`) in `TearDown()`. **Do NOT add a public `reset()` method to `NotificationManager`** — it is not needed in production and should not be part of the production API. This is the same destructor-order pattern as `UIManagerModalTest`. Failure to include `TearDown()` can cause the `NotificationManager` destructor to call into an already-destroyed `MockUIBackend`, producing undefined behavior in the test runner.
- [ ] `NotificationManager` tests inject `MockSimulationPauser` (implementing `ISimulationPauser*`) — NOT `MockCitySimulation`. `UIManagerModalTest` uses `NiceMock<MockCitySimulation>` for pause/resume calls. These are distinct interfaces with separate mock classes: `MockSimulationPauser` in `tests/ui/mock_simulation_pauser.h`, `MockCitySimulation` in `tests/ui/mock_city_simulation.h`. Never substitute one for the other. (ref: `architecture/testing/testability-architecture.md`)
- [ ] `UndoSystem_BlockedDuringModal_HotkeyIgnored` and `UndoSystem_BlockedDuringModal_ButtonGrayedOut` tests (ref: `architecture/testing/testability-architecture.md`)
- [ ] Additional undo tests: `UndoSystem_ExpiresAtSecondBudgetTick_NotFirst`: after placing a zone and firing one budget tick, undo is still available (`isElementEnabled` returns true on undo button); after firing the second budget tick, undo expires (`setElementEnabled(false)` called); `UndoSystem_FundsRefunded_AtOriginalCost`: undoing a zone placement credits treasury by original placement cost; `UndoSystem_Countdown_AmberAtLowThreshold`: verify `setElementText` called on undo button with amber indicator when `remainingSeconds < 5.0` (using ManualClock)
- [ ] Additional notification tests: `NormalToast_AutoDismiss_After5Seconds`: inject ManualClock, post Normal toast, advance clock 4.9 s — toast still visible; advance 0.1 s more — `removeElement` called on MockUIBackend; `NormalToast_VisibleCountReduced_WhenCriticalActive`: with 1 CRITICAL toast visible, max 2 Normal toasts shown; with 2 CRITICAL toasts, max 1 Normal toast; `NormalToast_LogFallback_AtDepth11`: posting 11 Normal toasts with 3 already visible triggers log-fallback path without crash; `NotificationLog_AfterOpen_UnreadCountResetsToZero`
- [ ] **UI sprite sheet art** (`graphics-artist-2d-texture`): 2048×2048 source PNG with all V1 HUD icons, toolbar icons, notification bell, zone/tool cursors, minimap overlay icons. All icons reviewed and approved for legibility at 48×48 px and 16×16 px sizes before Phase 5 begins. Note: DDS export (`glTexImage2D` with `GL_RGBA8`, `GL_TEXTURE_MAX_LEVEL=0`) and final GPU upload are Phase 6 technical deliverables — Phase 5 delivers the art source and design sign-off only. (ref: `architecture/asset-standards/2d-texture-standards.md`)
- [ ] `GracePeriod_Countdown_HidesAt120Seconds` using `ManualClock`
- [ ] **`ui_tests` Phase 5 source files**: `target_sources(ui_tests PRIVATE tests/ui/uimanager_modal_test.cpp tests/ui/notification_manager_test.cpp tests/ui/query_panel_test.cpp tests/ui/keybindings_test.cpp tests/ui/undo_system_test.cpp)` — all Phase 5 `ui_tests` source files must be added to the CMake target in the first Phase 5 CMakeLists commit; do not add incrementally one at a time. This mirrors the policy from Phase 3 `simulation_tests`. (ref: `architecture/testing/framework.md`)
- [ ] Complete `IrrlichtUIBackend` implementation of all 14 `IUIBackend` virtual methods (Phase 1 smoke target only verified `addStaticText` compilation); integration test under `LABELS "integration"` verifying all 14 methods via `EDT_NULL` driver (ref: `architecture/testing/testability-architecture.md`)
- [ ] `QueryPanelEscapeFeedbackToast_EscapeWhileToastVisible_OpensPauseMenu`
- [ ] `TaxRatePanel_AfterClose_ResourceBarShowsPendingLabel`
- [ ] Settings display-change countdown modal tests: `SettingsPanel_DisplayChangeCountdown_AutoRevertsAt10Seconds` (ManualClock advance 9.9 s — assert no revert; then 10.1 s — assert revert fires); `SettingsPanel_DisplayChangeCountdown_ConfirmBeforeExpiry_PreventsRevert`
- [ ] `BudgetDetailPanel_Opens_OnTreasuryHover` (label `unit`): verify panel opens on hover/click on the treasury balance field; `BudgetDetailPanel_Shows8LineItems` (label `unit`): verify all 8 named line items (Tax R, Tax C, Tax I, Wages, Road maintenance, Service upkeep by type, Utility fees, Net monthly balance) are rendered as separate `addStaticText` calls on `StrictMock<MockUIBackend>`. Register in `tests/ui/` via `target_sources(ui_tests ...)`. (ref: `architecture/ui-ux/hud-layout.md`)
- [ ] `PauseMenu_EscapeWhenSettingsOpen_ClosesSettingsNotPauseMenu` (label `unit`): with SettingsPanel open and PauseMenuPanel also open, send Escape key; verify SettingsPanel receives Escape and closes, PauseMenuPanel remains visible. `PauseMenu_EscapeWhenSettingsClosed_ClosesPauseMenu` (label `unit`): with only PauseMenuPanel open, send Escape; verify PauseMenuPanel closes and simulation resumes (`setPaused(false)` called). These tests verify Escape routing between the two nested panels. (ref: `architecture/ui-ux/settings-pause-menu.md`)
- [ ] `LoadingScreen_Cancel_LabelChangesToFinalizing_AtLastCheckpoint` (label `unit`): advance loading progress to the last checkpoint (≥90% complete); verify Cancel button label changes to 'Finalizing...' and `setElementEnabled(false)` is called on it; verify no further cancellation is processed after this transition even if the user activates the button. (ref: `architecture/ui-ux/main-menu-new-game-flow.md`)
- [ ] **`src/ui/` coverage pre-verification checkpoint** (`test-dev-cpp`): before Phase 5 exit criteria are declared met, run `coverage-linux` on the Phase 5 branch and confirm `src/ui/` line coverage is ≥80%. Document the result as a comment (e.g., `// src/ui/ coverage: 82% — Phase 5 branch, approved`). If coverage falls short, add additional `ui_tests` before Phase 5 is closed. **Do NOT rely on Phase 9 to discover a coverage shortfall in Phase 5 work — the rework cost at Phase 9 is high.** This mirrors the Phase 2 coverage pre-raise verification pattern. Owner: `test-dev-cpp`.

### Exit Criteria
- Full HUD visible in gameplay state with correct virtual-space layout at 1920×1080 and letterboxed at 1280×720
- Tax rate panel, query panel, minimap, and modals open/close correctly; input arbitration priority verified by tests
- Notification system CRITICAL/Normal queue, auto-pause, and auto-dismiss verified by unit tests
- All modal and notification unit tests pass with coverage gate green
- Main menu and new game flow navigate correctly; loading spinner shows and cancels
- `IrrlichtUIBackend` integration test (all 14 `IUIBackend` virtual methods) passes under `ctest -L '^integration$'`
- `test-dev-cpp` confirms `src/ui/` line coverage ≥80% on Phase 5 branch before Phase 5 is closed (coverage pre-verification checkpoint documented)

### Team
| Role | Responsibility |
|---|---|
| `gamedesign-ux` | All UI panel layouts, modal content, hotkey scheme, accessibility, keyboard navigation |
| `graphics-dev-irrlicht` | `IrrlichtUIBackend`, `UIManager` event dispatch, `UIScaler` implementation |
| `graphics-artist-2d-texture` | UI sprite sheet (2048×2048 RGBA8 UNORM, no DXT compression, no mip chain), icon art |
| `test-dev-cpp` | All UIManager, NotificationManager, QueryPanel, KeyBindings, UndoSystem unit tests |

### Dependencies
- Requires Phase 1 complete (UIManager shell, IUIBackend)
- Requires Phase 3 simulation (for loan dialog triggers, economy event notifications)
- Phase 4 audio NOT required before Phase 5 — UI development uses `MockAudioSystem`; Phase 5 can begin after Phase 3 and Phase 1 are complete. Phase 4 audio must be complete before Phase 7 (where UI sound effects are wired).

### Risks & Spikes
- **Pre-condition resolved**: Colorblind Mode tab placement is confirmed as Graphics tab > Accessibility subsection — both `architecture/ui-ux/settings-pause-menu.md` and `architecture/ui-ux/resolution-ui-scaling.md` are aligned. No spec change or design ruling is required before Phase 5 implementation begins. Implement directly per spec.
- **RISK**: Synchronous `addStaticText` call within `closeModal()` for queued CRITICAL toast is a non-trivial implementation detail that tests enforce strictly. **Spike**: prototype `closeModal()` re-evaluation path with the test harness first.
- **Pre-condition resolved**: `PauseMenuPanel` is a separate class from `SettingsPanel` per `architecture/ui-ux/ui-manager.md` and `architecture/ui-ux/settings-pause-menu.md` — no further design decision is required. `PauseMenuPanel` is constructed between `InspectorPanel` and `SettingsPanel` in the UIManager construction order; `SettingsPanel` is subordinate to `PauseMenuPanel`. Both spec files are already aligned on this structure.
