## Phase 10: Dynamic Soundscape

### Goal

Deliver all V1 audio assets and the dynamic soundscape system: adaptive music with beat-boundary crossfades, ambient beds, zone loops, vehicle engine audio, stingers, all WAV SFX assets, UI sounds, and music ducking — all wired to simulation and UI events.

### Deliverables

#### UI Assets — Font (gamedesign-ux)

- [ ] Font asset: create `assets/fonts/hud_font.xml` AND `assets/fonts/hud_mono_font.xml` — **two-font path selected** (Path 2 per `architecture/ui-ux/hud-layout.md` Monospace Requirement). Decision: `hud_font.xml` is a proportional sans-serif face for labels and button text; `hud_mono_font.xml` is a monospace face applied selectively to numeric `IGUIStaticText` elements (treasury balance, population count, tax rates, percentages) via `setOverrideFont()` after each `addStaticText()` call. Rationale: a single monospace face applied to all HUD text (labels, tooltips, panel titles) degrades legibility at small sizes because monospace glyphs are wider than proportional equivalents, reducing the effective character capacity of compact panels such as the Query/Inspector Panel and the Tax Rate Panel. Separating the faces allows each to be optimised for its role. Both files must be bitmap fonts in Irrlicht's XML font format generated by Irrlicht's `makeFont` tool or equivalent. Both must render at minimum 11 physical pixels at 1280×720 (the hard floor from `architecture/ui-ux/resolution-ui-scaling.md`). A font sized at 16 virtual pixels satisfies this floor. `IrrlichtUIBackend` constructor already loads `hud_font.xml` and sets it as the environment default; the `hud_mono_font.xml` load call must be added to the constructor alongside the existing `hud_font.xml` load, storing the result in a `m_monoFont` pointer for later `setOverrideFont()` use. Both loads use graceful fallback (log a warning, do not assert) when the file is absent. **Font selection guidance for `gamedesign-ux`**: for `hud_font.xml`, a clean humanist or geometric sans-serif face (e.g. DejaVu Sans, Ubuntu, or Noto Sans) at 16 px virtual is recommended — these are freely licensed, render cleanly at small sizes, and are already common in game HUD toolchains. For `hud_mono_font.xml`, a tabular-figures monospace face (e.g. DejaVu Sans Mono, Ubuntu Mono, or Noto Sans Mono) is recommended. The two faces should share similar x-height and cap-height to avoid visual jarring when proportional and monospace text appear adjacent in the resource bar. (ref: `architecture/ui-ux/hud-layout.md` Font Loading section, `architecture/ui-ux/resolution-ui-scaling.md` Typography section)

#### UI Assets — Sprite Sheet (graphics-artist-2d-texture)

- [ ] Sprite sheet asset: author and place `assets/textures/ui/hud_sprites_ui.png` (PNG, for Irrlicht's `IVideoDriver::getTexture()`). **Authoring pipeline — mandatory steps in order**:
  1. Author source artwork as a 2048×2048 RGBA8 image in your DCC tool (Photoshop, GIMP, Aseprite, or equivalent). The canvas must be exactly 2048×2048. Lay out a 32×32 cell grid where each cell is 64×64 px. Work in linear colour space throughout (do NOT apply sRGB gamma to a UI sprite sheet — UI textures are uploaded via the linear pool).
  2. Export the source as `hud_sprites_ui.png` (32-bit RGBA PNG). Verify the export is 2048×2048 pixels with an alpha channel.
  3. Convert to DDS using **nvcompress only** — the command is `nvcompress -rgba -nomips hud_sprites_ui.png hud_sprites_ui.dds`. **Compressonator is PROHIBITED for this file** — Compressonator's ARGB_8888 stores data in BGRA byte order, which causes a silent red-blue channel swap when uploaded via `GL_RGBA` / `GL_UNSIGNED_BYTE`. nvcompress `-rgba -nomips` guarantees true RGBA byte order and suppresses mip generation. This constraint is specified in `architecture/asset-standards/2d-texture-standards.md` DDS Authoring Pipeline (UI sprite sheet row).
  4. Verify the DDS output: confirm it is 2048×2048, RGBA8 UNORM, 1 mip level (no mip chain), linear colour space, no sRGB flag. Use the following command to check the DDS header:

     ```bash
     python3 -c "
     import struct
     d = open('hud_sprites_ui.dds', 'rb').read()
     magic = d[0:4]
     assert magic == b'DDS ', f'Not a DDS file: {magic}'
     height = struct.unpack_from('<I', d, 12)[0]
     width  = struct.unpack_from('<I', d, 16)[0]
     mips   = struct.unpack_from('<I', d, 28)[0]
     fourcc = d[84:88]
     print(f'Dimensions: {width}x{height}')   # must be 2048x2048
     print(f'Mip count: {mips}')              # must be 1 (or 0 meaning 1 level)
     print(f'FourCC: {fourcc}')               # must NOT be b\"DX10\" (no sRGB extension header)
     "
     ```

     The output must show 2048x2048, mip count 1 (or 0), and the FourCC must NOT be `b'DX10'`
     (a DX10 header with a sRGB DXGI format flag is incorrect for a linear RGBA8 UI texture).

  5. Convert the verified DDS back to PNG for Irrlicht's built-in `IVideoDriver::getTexture()` loader. Use one of the following tools — **do NOT use `nvcompress` for this step** (`nvcompress` compresses images to DDS and cannot decode DDS back to PNG):

     - **ImageMagick (magick CLI, ImageMagick 7+)**: `magick hud_sprites_ui.dds hud_sprites_ui.png`
     - **ImageMagick (convert CLI, ImageMagick 6)**: `convert hud_sprites_ui.dds hud_sprites_ui.png`
     - **GIMP**: File → Open `hud_sprites_ui.dds`, then File → Export As `hud_sprites_ui.png` (disable all ICC profile / gamma options on export)

     Output the result directly as `hud_sprites_ui.png` and place it at `assets/textures/ui/hud_sprites_ui.png`. Irrlicht does not load DDS natively through `getTexture()` on all platforms; the PNG is the committed runtime asset. The DDS intermediate is not committed.
  6. Verify the final PNG is 2048×2048 RGBA8 with no visible channel corruption (spot-check a few cells against the source artwork).

  Once this file is present on disk, `IrrlichtUIBackend::m_spriteTextureReady` becomes `true` and `setElementImage()` calls replace fallback text with icons, eliminating the letter artefact from Phase 9b builds. (ref: `architecture/ui-ux/hud-layout.md` Toolbar Button Text Fallback section, `architecture/asset-standards/2d-texture-standards.md` DDS Authoring Pipeline)

#### Audio Assets (sound-artist-opensoftal)

- [ ] All V1 assets per manifest: `music_main_menu_01/02` (OGG, **48 bars = 128.00 s = 5,644,800 samples** at 44100 Hz stereo, bar-aligned seamless loop with no fade or silence at boundary, -16 LUFS/-1 dBTP, JSON sidecar mandatory: `music_main_menu_01.json` / `music_main_menu_02.json` each containing `{"bpm":90,"beats_per_bar":4}`); `ambient_day/night/dawn/dusk` (OGG, 90–120 s, stereo, pre-baked 200 ms DAW crossfade tail required AND **sample-0 click-free boundary is the PRIMARY loop quality gate** — the streaming runtime loops by `ov_pcm_seek(vf, 0)` before reaching the pre-baked tail, bypassing it; authors must set a DAW loop point at sample 0 and audition through minimum 5 cycles to confirm no click or level step at the sample-0 boundary; a bed that passes DAW-crossfade preview but fails sample-0 click-free test is REJECTED; -20 LUFS/-1 dBTP) — all four ambient beds delivered, each individually loudness-verified, and sample-0 click-free loop test completed for each; `music_calm/growth/crisis _01/02` (OGG, **36 bars = 96.00 s = 4,233,600 samples** at 44100 Hz stereo, bar-aligned seamless loop, -16 LUFS/-1 dBTP, all 8 music stems share root key and mode per Music stems deliverable, JSON sidecar mandatory, crossfade audibility demo approved per Crossfade gate item; **PRE-DELIVERY RE-EXPORT REQUIRED: `music_calm_01`, `music_calm_02`, `music_growth_01`, `music_growth_02` currently measure −13.9 LUFS — apply −2.1 LU gain reduction; `music_growth_01` clips at −0.8 dBFS true peak — apply true-peak limiter ceiling −1 dBTP; all 8 stems fail SA-3 bar-count conformance — re-export at sample-accurate target lengths above; verify with `validate_assets.py` Check #14 before Phase 10 exit review**); `sfx_zone_residential/sfx_zone_commercial/sfx_zone_industrial` (OGG, 12–18 s hard cap 18 s, mono, silence-boundary loop −60 dBFS at head and tail, -26 LUFS/-2 dBTP (authoring measurement, before runtime distance attenuation), mandatory DAW loopback verification: authors must loop the file in the DAW player, listen through the boundary, and confirm the 200 ms silence window coincides with a natural rhythmic gap — zone loops that do not pass this verification must not be delivered); all WAV SFX per manifest; `stinger_crisis` (WAV PCM mono, 2–4 s) and `stinger_milestone` (WAV PCM mono, 2–3 s) — V1 stingers only, pre-loaded non-evictable, -18 LUFS/-1 dBTP (`stinger_game_over` is post-V1 — **sources[57] is idle in V1** (allocated by `alGenSources(62, ...)` but never acquired by any code path; not reserved for any stinger in V1)); `sfx_vehicle_engine_idle/move` (OGG Vorbis, **minimum 6 s, maximum strictly less than 20 s — files at exactly 20 s are Tier 3 (streamed) and will be rejected by the CI gate (`duration >= 20.0` is the hard failure condition)**; pre-loaded, mono positional, -22 LUFS/-2 dBTP — WAV is PROHIBITED; see manifest for full duration rationale) (ref: `architecture/audio-architecture/v1-audio-asset-manifest.md`, `architecture/audio-architecture/source-pool.md`)
- [ ] Music stems: all 8 music files (2 main menu variants + 6 gameplay stems) authored in the **same root key and mode — decided: A natural minor (Aeolian mode), root A** — this is the locked root key and mode for all V1 music per `architecture/audio-architecture/dynamic-soundscape.md` Cross-tier harmonic compatibility requirement and `architecture/audio-architecture/production-briefs/music-production-brief.md` V1 final decision. Rationale: A natural minor shares the key signature of C major (no sharps or flats) while its neutral-to-melancholic Aeolian character suits the city-builder aesthetic; root A at 440 Hz aligns to concert pitch (A440); and A natural minor provides a clear harmonic vocabulary for differentiating calm, growth, and crisis intensity tiers through orchestration weight, rhythmic density, and dissonant cluster voicings within the same scale. The crisis stems are authored in A natural minor — NOT in a parallel major or any other mode (a mode change during a live crossfade produces audible harmonic dissonance; see music-production-brief.md V1 final decision). Crisis intensity differentiation comes from heavier percussion, denser layering, tighter rhythmic syncopation, dissonant cluster voicings (e.g. diminished 7ths, tritone extensions, dense minor-mode stacking), and increased orchestral density — all within A natural minor. Integer number of bars at 90 BPM (per-stem bar counts confirmed in `assets/audio/music_bar_counts.md` before any stem is submitted for merge); bar-aligned seamless loop; `{"bpm":90,"beats_per_bar":4}` in every JSON sidecar (ref: `architecture/audio-architecture/audio-asset-formats.md`, `architecture/audio-architecture/dynamic-soundscape.md`)
- [ ] Zone loops: silence-boundary at head and tail to −60 dBFS; DAW verification mandatory before delivery. **Zone loop DAW loopback verification must be a CI-enforced gate**: add as **Check #21** in `tools/validate_assets.py`: for each `sfx_zone_*.ogg`, decode the OGG and apply two independent region checks — both must pass: **(1) Leading silence check**: verify the leading `ceil(44100 × 0.1) = 4410` samples are all at or below −60 dBFS peak amplitude; **(2) Trailing silence check**: verify the trailing 4410 samples are all at or below −60 dBFS peak amplitude. The two 4410-sample windows are checked independently — failure of either window alone is sufficient to reject the file; fail the `validate-assets` CI job if any zone loop file fails this check. Owner: `sound-dev-opensoftal` (script implementation), `sound-artist-opensoftal` (asset compliance). **Phase 10 entry gate**: zone loop assets (`sfx_zone_residential.ogg`, `sfx_zone_commercial.ogg`, `sfx_zone_industrial.ogg`) MUST NOT be merged to main until Check #21 is present in CI and green — commit the script change and the asset files in the same PR or confirm the CI gate is live before the asset PR merges. **Important**: `AL_SOFT_loop_points` is a runtime OpenAL Soft attribute set via `alBufferiv()` on the GPU buffer — it is NOT an OGG Vorbis comment field. The CI gate verifies the silence-floor authoring requirement only. (ref: `architecture/audio-architecture/audio-asset-formats.md`, `architecture/audio-architecture/streaming-architecture.md`)
- [ ] Zone loop DAW sign-off: create `assets/audio/zone_loop_qa.md` documenting DAW loopback verification pass for all three zone loop assets (`sfx_zone_residential.ogg`, `sfx_zone_commercial.ogg`, `sfx_zone_industrial.ogg`); each entry must state: asset filename, DAW tool used, loop boundary confirmed natural (yes/no), and author sign-off name; commit this file to the repository before Phase 10 exit (required by exit criterion)
- [ ] OGG encoding: music -q8, ambient -q7, zone loops -q6; 44100 Hz; mono for positional, stereo for music/ambient (ref: `architecture/audio-architecture/audio-asset-formats.md`)
- [ ] **Music bar count confirmation** (`sound-artist-opensoftal`): before authoring any production stem, commit `assets/audio/music_bar_counts.md` (or equivalent document) stating the bar count for each of the 6 gameplay stems plus the 2 main menu variants, in the format: `<filename>: N bars at 90 BPM = M.MM s`. All bar counts must be positive integers. Non-integer bar counts cause bar-boundary crossfade drift over long sessions and are rejected. Example entry: `music_calm_01: 32 bars at 90 BPM = 85.33 s`. This fulfils Phase 4 SA-3. If this document was already produced during Phase 4, confirm it exists in the repository and reference it here — do not re-author it.
- [ ] **Day→night ambient direct crossfade demo** (`sound-artist-opensoftal`): commit `assets/audio/crossfade_demo_day_to_night.ogg` — a 10–15 s rendered demo of `ambient_day` tail fading into `ambient_night` head using a 3 s constant-power crossfade, with no intermediate dusk bed. This is required because at the default simulation speed of x3 the dusk bed is never heard; players always experience a direct day→night transition. The demo must be reviewed and approved by the team before `ambient_day.ogg` and `ambient_night.ogg` are declared production-final. Evaluation criterion: the 3 s overlap (both beds playing simultaneously at partial gain) must not produce an audible jarring transient or tonal clash. This demo requirement is specified in `architecture/audio-architecture/dynamic-soundscape.md` Authoring note (dawn/dusk collapse at speed ≥3×). This file is a QA artifact — it is not a runtime game asset and is not loaded by `AudioSystem`. (ref: `architecture/audio-architecture/dynamic-soundscape.md`, `architecture/audio-architecture/v1-audio-asset-manifest.md`)
- [ ] Stingers: `stinger_crisis` (WAV PCM mono, **target 3 s**, within 2–4 s range) and `stinger_milestone` (WAV PCM mono, **target 2.5 s**, within 2–3 s range) — V1 stingers only; both pre-loaded non-evictable; -18 LUFS / -1 dBTP; authored to function alongside music ducked to 0.4 gain; **onset timing requirement**: the most prominent musical content of each stinger (main impact/peak) must begin no earlier than 0.25 s into the file — this ensures the 0.2 s duck ramp completes before the stinger body hits and the stinger lands on music that is already reduced to 0.4 gain; a stinger that peaks at t=0 will land on music still at full gain, undermining the intended ducked-stinger mix balance; a 0.25 s onset also allows a brief attack transient or build before the main body without violating the duck timing; verify onset timing in DAW before delivery; `stinger_game_over` is post-V1; **sources[57] is idle in V1** — allocated by `alGenSources(62, ...)` but never acquired by any code path (ref: `architecture/audio-architecture/v1-audio-asset-manifest.md`, `architecture/audio-architecture/source-pool.md`)
- [ ] Vehicle horn `sfx_vehicle_horn`: WAV mono, 0.4–1 s minimum 0.4 s authored duration, -18 LUFS/-1 dBTP (ref: `architecture/audio-architecture/v1-audio-asset-manifest.md`)
- [ ] All WAV SFX assets from manifest with correct loudness targets and channel format — **all WAV SFX: 44100 Hz, 16-bit PCM, mono (1 channel)**; OpenAL Soft requires mono for 3D positional spatialization; non-positional SFX are also authored mono to avoid undefined multi-channel non-positional behaviour and to minimise source pool memory (ref: `architecture/audio-architecture/v1-audio-asset-manifest.md`):
  - `sfx_build_place` WAV <1 s, **mono**, positional at tile center (`AL_SOURCE_RELATIVE = AL_FALSE`), -24 LUFS/-1 dBTP
  - `sfx_build_demolish` WAV <1 s, **mono**, positional at tile center (`AL_SOURCE_RELATIVE = AL_FALSE`), -24 LUFS/-1 dBTP
  - `sfx_fire_alert` WAV 2–4 s, **mono positional** (`AL_SOURCE_RELATIVE = AL_FALSE`), CRITICAL priority, -18 LUFS/-1 dBTP
  - `sfx_police_alert` WAV 2–4 s, **mono positional** (`AL_SOURCE_RELATIVE = AL_FALSE`), CRITICAL priority, -18 LUFS/-1 dBTP
  - `sfx_power_out` WAV 1–2 s, **mono**, non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass (`AL_DIRECT_FILTER = AL_FILTER_NULL`), -22 LUFS/-1 dBTP
  - `sfx_water_out` WAV 1–2 s, **mono**, non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass (`AL_DIRECT_FILTER = AL_FILTER_NULL`), -22 LUFS/-1 dBTP
  - `sfx_road_build` WAV <1 s, **mono**, positional at tile center (`AL_SOURCE_RELATIVE = AL_FALSE`), -24 LUFS/-1 dBTP
  - `sfx_budget_warn` WAV 1–2 s minimum 1 s, **mono**, non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass (`AL_DIRECT_FILTER = AL_FILTER_NULL`), -24 LUFS/-1 dBTP
  - `sfx_loan_issued` WAV <1 s, **mono**, non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass (`AL_DIRECT_FILTER = AL_FILTER_NULL`), -24 LUFS/-1 dBTP
  - `sfx_zone_upgrade` WAV 1–2 s, **mono**, non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass (`AL_DIRECT_FILTER = AL_FILTER_NULL`), -22 LUFS/-1 dBTP
  - `sfx_service_degrade` WAV 1–2 s, **mono**, non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass (`AL_DIRECT_FILTER = AL_FILTER_NULL`), -22 LUFS/-1 dBTP
  - `sfx_intersection_tick` WAV <0.5 s, **mono positional** (`AL_SOURCE_RELATIVE = AL_FALSE`; optional distance cull >80 m; subtle traffic detail, not a notification sound — no EFX bypass), -28 LUFS/-2 dBTP
  - `sfx_earthworks` WAV <1 s, **mono**, positional at tile center (`AL_SOURCE_RELATIVE = AL_FALSE`), EFX bypass positional exception (`AL_DIRECT_FILTER = AL_FILTER_NULL`; construction occurs on open unoccluded tiles — bypass applied even though source is positional), -24 LUFS/-1 dBTP
  - `ui_click` WAV <0.2 s, **mono**, non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass (`AL_DIRECT_FILTER = AL_FILTER_NULL`), -24 LUFS/-1 dBTP
  - `ui_toast` WAV <0.3 s, **mono**, non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass (`AL_DIRECT_FILTER = AL_FILTER_NULL`), -22 LUFS/-1 dBTP
  - `ui_menu_open` WAV <0.3 s, **mono**, non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass (`AL_DIRECT_FILTER = AL_FILTER_NULL`), -24 LUFS/-1 dBTP
  - `ui_menu_close` WAV <0.3 s, **mono**, non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass (`AL_DIRECT_FILTER = AL_FILTER_NULL`), -24 LUFS/-1 dBTP
- [ ] **Crossfade audibility pre-production gate** (BLOCKING — Phase 4 gate signed off; Phase 10 must commit delivery artifacts): The Phase 4 gate (`[x]` in `implementation/phase-4.md` under "Crossfade audibility pre-production gate (BLOCKING)") was signed off as part of Phase 4 DONE, which means full music stem production is unblocked. However, the actual rendered demo files were never committed to the repository as concrete review artifacts — they were only described in the production brief gate text. Phase 10 must commit the following files to `assets/audio/crossfade_demos/` in the same PR as the music stem OGG files. Without these files the exit criterion "Crossfade audibility pre-production demo approved" cannot be verified:

  **(a) `assets/audio/crossfade_demos/crossfade_demo_calm_to_growth.wav`** — 15 s rendered mix at 44100 Hz 16-bit stereo: `music_calm_01` plays from t=0; at t=6 s begin a 3 s constant-power crossfade (`gain_out=cos(t×π/2)`, `gain_in=sin(t×π/2)`) to `music_growth_01`; both stems audible simultaneously from t=6 s to t=9 s with no harmonic clash or audible pop. Demonstrates within-tier gameplay harmonic compatibility.

  **(b) `assets/audio/crossfade_demos/crossfade_demo_mainmenu_to_calm.wav`** — 15 s rendered mix, same crossfade parameters: `music_main_menu_01` → `music_calm_01`, crossfade beginning at t=6 s. Demonstrates cross-context (main-menu-to-gameplay) harmonic compatibility.

  **(c) `assets/audio/crossfade_demos/crossfade_demo_qa.md`** — QA sign-off document stating: (i) both WAV demos were listened through in full by `sound-artist-opensoftal` and at least one other team member; (ii) no harmonic clashes, audible pops, or abrupt transitions were detected; (iii) the confirmed root key and mode (**A natural minor, Aeolian mode, root A** — per Music stems item above, `architecture/audio-architecture/dynamic-soundscape.md`, and `architecture/audio-architecture/production-briefs/music-production-brief.md`); (iv) per-stem bar count for all 8 music files (main-menu: 48 bars; gameplay: 36 bars each); (v) sign-off names and date. This document constitutes approval — no separate review meeting is required. A blocking objection (harmonic clash, audible pop, wrong key) must be filed as a PR comment; the artist must revise the demo and the qa.md before the PR can merge.

  **All 8 music files are in A natural minor (Aeolian mode, root A).** The "crisis stems in parallel major or any other mode" option is permanently rejected for V1 — all stems share A natural minor regardless of intensity tier. This eliminates any cross-tier crossfade demo requirement beyond (a) and (b) above: because all stems share the same key and mode, calm→crisis and growth→crisis crossfades are covered by the same harmonic compatibility proof demonstrated in (a).

#### Dynamic Soundscape Code (sound-dev-opensoftal)

- [ ] **Main menu music**: streams on `sources[58..59]` (reuses gameplay music sources; main menu and gameplay are mutually exclusive states — no new stream sources required); `AL_SOURCE_RELATIVE = AL_TRUE` set on sources[58..59] during main menu state; 1 s constant-power fade-out on `transitionToGameplay()` (do NOT abruptly stop — hard stop produces jarring cut; the 1 s fade bridges the transition); random variant excluding repeat; JSON sidecar read at load time (ref: `architecture/audio-architecture/dynamic-soundscape.md`)
- [ ] **Adaptive music stems**: constant-power crossfade (`gain_in=sin(t×π/2)`, `gain_out=cos(t×π/2)`); minimum 2 s default 3 s; beat-boundary sync via software sample counter `m_samplesQueued` (NOT `AL_SAMPLE_OFFSET` — `AL_SAMPLE_OFFSET` on a buffer-queue source returns offset within current buffer only, not absolute stream position, and cannot reliably compute bar boundaries); `computeSamplesPlayed()` formula: `samplesPlayed = (samplesQueued > buffersQueued * kSamplesPerBuffer) ? samplesQueued - buffersQueued * kSamplesPerBuffer : 0` (underflow guard: at stream start `samplesPlayed` returns 0, preventing a false crossfade fire); `m_nextBarBoundary > 0` guard on crossfade condition (prevents false fire at stream start); **bar-boundary bootstrap required**: audio thread must call `if (m_nextBarBoundary == 0 && m_samplesQueued > 0) { computeNextBarBoundary(...); }` before the crossfade condition check each wake — without this bootstrap, m_nextBarBoundary stays 0 forever and the `m_nextBarBoundary > 0` guard prevents crossfades from ever firing (ref: `architecture/audio-architecture/dynamic-soundscape.md` lines 84–98); audio thread wake order enforced: (1) read `AL_BUFFERS_QUEUED` once into a local, (2) read `AL_BUFFERS_PROCESSED`, (3) call `alSourceUnqueueBuffers`, (4) decode PCM, (5) call `alSourceQueueBuffers` — `AL_BUFFERS_QUEUED` must be read BEFORE `alSourceUnqueueBuffers` and stored in a single local; pass same value to both `computeSamplesPlayed()` and `computeNextBarBoundary()` to avoid race — the helper functions MUST NOT call `alGetSourcei(AL_BUFFERS_QUEUED)` independently; `AL_BUFFERS_QUEUED` must be read exactly once per wake and passed as a `ALint` parameter; interrupted crossfade formula `t_offset=(2/π)×arccos(current_gain_out)` where `current_gain_out` refers to the incoming stem B's current gain at moment of interruption (stem B becomes the new outgoing in the B→C crossfade); at most 2 stems simultaneously active; variant selection no-repeat; state escalation queues one pending transition (ref: `architecture/audio-architecture/dynamic-soundscape.md`)
- [ ] **Music ducking state machine**: IDLE → DUCKING (0.2 s ramp to 0.4) → DUCKED (hold) → RELEASING (1.5 s ramp to 1.0); `m_duckStartGain` member captured at IDLE→DUCKING and RELEASING→DUCKING re-entry; duck timer advancement: `double m_lastDuckWakeTime` member required; `IClock::nowSeconds()` captured exactly once per audio thread wake into a local `now`; `dt = float(now - m_lastDuckWakeTime); m_lastDuckWakeTime = now;`; `m_duckTimer` advanced by `dt` — do NOT use a fixed `0.01f` increment and do NOT call `nowSeconds()` twice per wake (causes drift); music stems only (NOT ambient beds); check **both V1 stinger sources (sources[55] and sources[56])** for `AL_PLAYING` before DUCKED→RELEASING; **sources[57] is idle in V1** (allocated by `alGenSources(62, ...)` but never acquired by any code path — it is not reserved for stingers in V1); sources[57] must NOT be checked in the DUCKED→RELEASING condition; stinger interruption drop if same type already in-progress; 5 s minimum between triggers of same type; `stinger_milestone` only at City Rating transitions (not raw population milestones); game-over duck + 2 s stem fade-out + `setGameOverState()` (ref: `architecture/audio-architecture/dynamic-soundscape.md`)
- [ ] **Ambient beds**: stream partition (sources[60..61]); time-of-day schedule (day=simulation state driven, dusk/night/dawn=forced Calm regardless of simulation state); crossfade 3 s constant-power; crossfade runs in real time (not simulation time) — `m_ambientCrossfadeT` advanced by wall-clock delta; minimum hold time = 1 crossfade duration before next transition; if transition requested before hold time elapses, queue and execute only after hold time passes; dawn/dusk collapsed at ≥3× speed (transitions go directly day→night and night→day); `m_ambientCrossfadeT` atomic float updated by audio thread; `setTimeOfDay()` API (ref: `architecture/audio-architecture/dynamic-soundscape.md`)
- [ ] Ambient bed streaming: runtime seek-to-0 loop (preferred) skipping DAW crossfade tail; `m_intentionallyStopped` flag (ref: `architecture/audio-architecture/streaming-architecture.md`)
- [ ] **Zone ambient loops**: up to 16 simultaneous; culled beyond 300 m; LOW priority SFX pool; silence-boundary technique; `AL_SOFT_loop_points` used if available; **zone loops are mono positional** (pre-loaded OGG, 12–18 s, SFX pool; `alBufferData` byte count = `ov_pcm_total(vf, -1) × 1 × sizeof(ALshort)` — `kSamplesPerBuffer` does NOT apply to pre-loaded zone loops; `kSamplesPerBuffer` governs streaming buffer slices only; pre-loaded OGG loads the entire decoded PCM in one `alBufferData` call using `ov_pcm_total()` for the total sample count) (ref: `architecture/audio-architecture/dynamic-soundscape.md`, `architecture/audio-architecture/source-pool.md`, `architecture/audio-architecture/v1-audio-asset-manifest.md`)
  - **[RESOLVED] Zone loop channel count**: Zone loops are **mono positional** per `v1-audio-asset-manifest.md` and `audio-asset-formats.md` — positional sounds require mono for OpenAL 3D spatialization. Zone loops are NOT streamed — they are pre-loaded into the SFX pool (not the stream partition). (ref: INDEX.md Resolved Contradiction #4)
- [ ] **Vehicle engine audio**: idle+move crossblend (idle gain 1.0 at < 3 m/s → 0.0 at ≥ 8 m/s; move inverse); `AL_PITCH` 0.75–1.35 linear with speed; class base pitch (Car=1.0, Bus/Truck=0.85); `AL_VELOCITY=(0,0,0)` for both sources; cull at >150 m; max 12 simultaneous vehicles (24 pool slots ÷ 2); paired acquisition/release (ref: `architecture/audio-architecture/dynamic-soundscape.md`, `architecture/audio-architecture/source-pool.md`)
- [ ] **Vehicle horn**: HIGH priority; per-vehicle 2 s cooldown; global cap 3 simultaneous; cull at >100 m (ref: `architecture/audio-architecture/v1-audio-asset-manifest.md`)
- [ ] **Music intensity interface**: add `void setMusicIntensity(MusicIntensity intensity)` pure-virtual method to `IAudioSystem` (`src/interfaces/IAudioSystem.h`); implement in `AudioSystem` to set `m_currentMusicIntensity` (enum: CALM, GROWTH, CRISIS); wire `CitySimulation::update()` to call `audioSystem->setMusicIntensity()` based on simulation state (CALM = normal/surplus, GROWTH = positive population/growth trends, CRISIS = consecutive deficit months >= 2 per threshold defined in economy model spec); test: `AdaptiveMusicIntensity_StateDriven_UpdatesAudioSystem` verifies `CitySimulation` calls `setMusicIntensity` with the correct tier when treasury/growth/deficit state changes — add to `simulation_tests` CMake target (NOT `audio_tests`; this test exercises `CitySimulation::update()` with `StrictMock<MockAudioSystem>`, matching the simulation test pattern) (ref: `architecture/audio-architecture/dynamic-soundscape.md`)
- [ ] Time-of-day music intensity override: day (06:00–20:00) = simulation state; dusk (20:00–23:00) / night (23:00–05:00) / dawn (05:00–06:00) = forced Calm regardless of simulation state; crossfade via beat-boundary on time-of-day boundary crossing; forced-Calm transition during in-progress crossfade uses `t_offset` interrupted formula `t_offset=(2/π)×arccos(current_gain_out)`; `AudioSystem` tracks `m_currentTimeOfDay` enum (DAY/DUSK/NIGHT/DAWN); supplied by `CitySimulation` via `AudioSystem::setTimeOfDay()` (ref: `architecture/audio-architecture/dynamic-soundscape.md`)
- [ ] Starvation recovery: reset `m_samplesQueued` after `alSourcePlay()`; recovery check in same lock scope as `alSourceQueueBuffers` (ref: `architecture/audio-architecture/streaming-architecture.md`)
- [ ] `AudioSystem::setGameOverState()` fully implemented in Phase 7; Phase 10 adds only the external event trigger: `CitySimulation::transitionToGameOver()` calls `audioSystem->setGameOverState(true)` when game-over conditions are met in Scenario mode. No stub references remain in Phase 10 — the duck state machine and `m_gameOverFade` flag are Phase 7 deliverables. (ref: `architecture/audio-architecture/dynamic-soundscape.md`)
- [ ] **`IrrlichtUIBackend` `hud_mono_font.xml` load** (`sound-dev-opensoftal` or whichever developer owns `IrrlichtUIBackend`): add the following to the `IrrlichtUIBackend` constructor immediately after the existing `hud_font.xml` load block: `irr::gui::IGUIFont* monoFont = m_guiEnv->getFont("assets/fonts/hud_mono_font.xml"); if (monoFont) { m_monoFont = monoFont; } else { logWarning("hud_mono_font.xml not found — falling back to default font for numeric elements"); }` where `m_monoFont` is a new `irr::gui::IGUIFont* m_monoFont{nullptr}` member of `IrrlichtUIBackend`. Expose `irr::gui::IGUIFont* getMonoFont() const { return m_monoFont; }` on `IrrlichtUIBackend` (not on `IUIBackend` — this is a concrete backend detail). Each numeric `IGUIStaticText` element created by `HUD` or other panels must call `element->setOverrideFont(static_cast<IrrlichtUIBackend*>(m_backend)->getMonoFont())` after `addStaticText()`, guarded by a null check (the fallback font remains the environment default when `hud_mono_font.xml` is absent). The numeric elements that require this treatment are: treasury balance display, population count display, tax rate fields (Tax Rate Panel), monthly revenue/expense fields (Budget Detail Panel), and the density unlock progress threshold value. Labels (zone names, panel titles, toolbar button text, notification body text) use `hud_font.xml` and do NOT call `setOverrideFont()`.
- [ ] Audio asset sidecar validation CI gate was added in Phase 5 (Check #14 in `tools/validate_assets.py`); Phase 10 delivers the actual music stem files with their sidecars (`music_main_menu_01/02.ogg` + `.json`, `music_calm/growth/crisis_01/02.ogg` + `.json`). Every music stem committed in Phase 10 must pass the Phase 5 sidecar CI gate. If adding a new `music_*.ogg`, add the companion `music_*.json` sidecar to the same commit — the validate-assets job will fail otherwise.
- [ ] **EFX bypass setup (ALL non-positional SFX)**: All non-positional notification-category SFX must have `alSourcei(src, AL_DIRECT_FILTER, AL_FILTER_NULL)` called on the acquired source before `alSourcePlay()`. The full EFX bypass list (11 assets — 10 non-positional + 1 positional exception): `ui_click`, `ui_toast`, `ui_menu_open`, `ui_menu_close`, `sfx_power_out`, `sfx_water_out`, `sfx_budget_warn`, `sfx_loan_issued`, `sfx_zone_upgrade`, `sfx_service_degrade`, and `sfx_earthworks` (positional exception: `AL_SOURCE_RELATIVE = AL_FALSE` / world-space, but EFX bypass required because construction occurs on open unoccluded tiles — do NOT set `AL_SOURCE_RELATIVE = AL_TRUE`). Do NOT bypass EFX for `sfx_fire_alert`, `sfx_police_alert`, or `sfx_intersection_tick` — these are positional and benefit from distance-based occlusion. (ref: `architecture/audio-architecture/v1-audio-asset-manifest.md`)

- [ ] **`ui_click` wiring** (`sound-dev-opensoftal`): Call `m_audio->playSound(UI_CLICK, SoundPriority::NORMAL, 1.0f)` in `UIManager::onEvent()` at Priority 5 (toolbar dispatch) immediately after `setActiveTool()` updates `m_activeTool`, for each of the five toolbar buttons (Zone, Road, Utilities, Demolish, Query). Also fire for zone sub-panel button clicks and Utilities sub-panel button clicks. Class: `UIManager`. Guard: `if (m_audio)`. `UI_CLICK` = SoundId 22 (`ui_click.wav`), `AL_SOURCE_RELATIVE = AL_TRUE`, EFX bypass. (ref: `architecture/ui-ux/hud-layout.md` Phase 10 Audio Wiring section)

- [ ] **`ui_toast` wiring** (`sound-dev-opensoftal`): Call `m_audio->playSound(UI_TOAST, SoundPriority::NORMAL, 1.0f)` in `NotificationManager::postCritical()` and `NotificationManager::postNormal()` immediately after the toast element is made visible via `m_backend->setElementVisible(handle, true)`. Fires once per toast appearance (not per queue enqueue — only when the toast becomes visible on screen). `NotificationManager` gains `IAudioSystem* m_audio{nullptr}` as an additional constructor parameter in Phase 10 (constructor becomes: `NotificationManager(IUIBackend*, ICitySimulation*, IClock*, IAudioSystem*)`). Class: `NotificationManager`. Guard: `if (m_audio)`. `UI_TOAST` = SoundId 23 (`ui_toast.wav`), `AL_SOURCE_RELATIVE = AL_TRUE`, EFX bypass. (ref: `architecture/ui-ux/hud-layout.md` Phase 10 Audio Wiring section)

- [ ] **`ui_menu_open` / `ui_menu_close` wiring** (`sound-dev-opensoftal`): Call `m_audio->playSound(UI_MENU_CLOSE, SoundPriority::NORMAL, 1.0f)` immediately after `setElementVisible(panel, false)` for any sub-panel being hidden in `UIManager::updateSubPanelVisibility()`. Call `m_audio->playSound(UI_MENU_OPEN, SoundPriority::NORMAL, 1.0f)` immediately after `setElementVisible(panel, true)` for any sub-panel being shown. Order: close sounds precede open sounds in the same `updateSubPanelVisibility()` call. Also fires on Budget Detail Panel and Tax Rate Panel open/close events. Class: `UIManager`. Guard: `if (m_audio)`. `UI_MENU_OPEN` = SoundId 24, `UI_MENU_CLOSE` = SoundId 25, both `AL_SOURCE_RELATIVE = AL_TRUE`, EFX bypass. Do NOT fire both open and close on the same frame for the same panel. (ref: `architecture/ui-ux/hud-layout.md` Phase 10 Audio Wiring section)

- [ ] **`sfx_build_place` wiring — zone and service placement** (`sound-dev-opensoftal`): `CitySimulation::placeZone()` and `CitySimulation::placeServiceBuilding()` each call `m_audio->playPositionalSound(SFX_BUILD_PLACE, vec3{float(tileX), 0.0f, float(tileZ)}, SoundPriority::NORMAL, 1.0f)` on successful placement (treasury deduction succeeded, tile was unoccupied). Class: `CitySimulation`. Guard: `if (m_audio)`. Fires unconditionally on success — no cooldown. `SFX_BUILD_PLACE` = SoundId 1 (`sfx_build_place.wav`), positional (`AL_SOURCE_RELATIVE = AL_FALSE`), no EFX bypass. (ref: `architecture/game-design/service-coverage.md` Audio Callbacks section, `architecture/game-design/zoning-system.md` Phase 10 Audio Callbacks section)

- [ ] **`sfx_build_demolish` wiring** (`sound-dev-opensoftal`): `CitySimulation::demolishTile()` calls `m_audio->playPositionalSound(SFX_BUILD_DEMOLISH, vec3{float(tileX), 0.0f, float(tileZ)}, SoundPriority::NORMAL, 1.0f)` on successful demolition of any tile type (zone, road, or service building). Class: `CitySimulation`. Guard: `if (m_audio)`. `SFX_BUILD_DEMOLISH` = SoundId 2 (`sfx_build_demolish.wav`), positional (`AL_SOURCE_RELATIVE = AL_FALSE`), no EFX bypass. (ref: `architecture/game-design/zoning-system.md` Phase 10 Audio Callbacks section)

- [ ] **`sfx_road_build` wiring** (`sound-dev-opensoftal`): `CitySimulation::placeRoad()` calls `m_audio->playPositionalSound(SFX_ROAD_BUILD, vec3{float(tileX), 0.0f, float(tileZ)}, SoundPriority::NORMAL, 1.0f)` on successful road placement. Class: `CitySimulation`. Guard: `if (m_audio)`. `SFX_ROAD_BUILD` = SoundId 3 (`sfx_road_build.wav`), positional (`AL_SOURCE_RELATIVE = AL_FALSE`), no EFX bypass. (ref: `architecture/game-design/traffic-system.md` Phase 10 Audio Callbacks section)

- [ ] **`sfx_earthworks` wiring** (`sound-dev-opensoftal`): `CitySimulation::placeZone()`, `CitySimulation::placeRoad()`, and `CitySimulation::placeServiceBuilding()` each call `m_audio->playPositionalSound(SFX_EARTHWORKS, vec3{float(tileX), 0.0f, float(tileZ)}, SoundPriority::NORMAL, 1.0f)` before the `SFX_BUILD_PLACE`/`SFX_ROAD_BUILD` call, but ONLY when `earthworksCostOverride > 0`. Class: `CitySimulation`. Guard: `if (m_audio && earthworksCostOverride > 0)`. `SFX_EARTHWORKS` = SoundId 4 (`sfx_earthworks.wav`), positional (`AL_SOURCE_RELATIVE = AL_FALSE`), EFX bypass (`AL_DIRECT_FILTER = AL_FILTER_NULL` — construction occurs on open unoccluded tiles). (ref: `architecture/game-design/zoning-system.md` Phase 10 Audio Callbacks section, `architecture/game-design/traffic-system.md` Phase 10 Audio Callbacks section)

- [ ] **`sfx_zone_upgrade` wiring** (`sound-dev-opensoftal`): `CitySimulation::doDensityUnlockTick()` calls `m_audio->playSound(SFX_ZONE_UPGRADE, SoundPriority::NORMAL, 1.0f)` once per tile upgraded in the density upgrade wave, immediately after `tile.densityTier` is incremented. Class: `CitySimulation`. Guard: `if (m_audio)`. One call per upgraded tile — at most 20% of eligible tiles per zone type per tick (the upgrade wave cap already limits the rate; no additional cooldown is needed). `SFX_ZONE_UPGRADE` = SoundId 5 (`sfx_zone_upgrade.wav`), non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass. (ref: `architecture/game-design/zoning-system.md` Phase 10 Audio Callbacks section)

- [ ] **`sfx_service_degrade` wiring** (`sound-dev-opensoftal`): `CitySimulation::tick()`, inside the per-building degradation loop for Fire Station, Police Station, AND Water Tower buildings, calls `m_audio->playSound(SFX_SERVICE_DEGRADE, SoundPriority::NORMAL, 1.0f)` immediately after the RNG roll succeeds (`ISimulationRNG::nextFloat() < SimulationConstants::service_degradation_probability_per_tick`) and `bldg.degraded` is set to `true`. All three radius-based service building types share this single SFX. Power Plant is excluded — it uses a deterministic brownout model with no RNG roll and fires no `SFX_SERVICE_DEGRADE` event; zone tiles losing power coverage fire `SFX_POWER_OUT` instead (see below). Class: `CitySimulation`. Guard: `if (m_audio)`. `SFX_SERVICE_DEGRADE` = SoundId 6 (`sfx_service_degrade.wav`), non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass. No additional cooldown at the call site — the 50% RNG roll and once-per-tick-per-building gate are sufficient throttles. (ref: `architecture/game-design/service-coverage.md` Phase 10 Audio Callbacks section)

- [ ] **`sfx_budget_warn` wiring** (`sound-dev-opensoftal`): `CitySimulation::tick()`, inside the budget-deficit-consequence block, calls `m_audio->playSound(SFX_BUDGET_WARN, SoundPriority::NORMAL, 1.0f)` on the tick that `budget_surplus_pct` first crosses below −25% in a deficit streak (the same tick the `BudgetDeficitWarn` notification is enqueued). Gated on `m_firstRevenueTicked` — never fires before a non-zero revenue tick. Class: `CitySimulation`. Guard: `if (m_audio && crossedDeficitThreshold)` where `crossedDeficitThreshold` is true on the tick the surplus first drops to ≤ −0.25 and the previous tick's surplus was > −0.25, or the streak counter has just re-entered deficit from zero. `SFX_BUDGET_WARN` = SoundId 7 (`sfx_budget_warn.wav`), non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass. (ref: `architecture/game-design/economy-model.md` Phase 10 Audio Callbacks section)

- [ ] **`sfx_loan_issued` wiring** (`sound-dev-opensoftal`): `CitySimulation::tick()`, immediately after forced loan principal is added to `outstanding_debt` and the loan-issued toast is enqueued, calls `m_audio->playSound(SFX_LOAN_ISSUED, SoundPriority::NORMAL, 1.0f)`. Fires once per loan issuance event (not once per deficit tick). Class: `CitySimulation`. Guard: `if (m_audio)`. `SFX_LOAN_ISSUED` = SoundId 8 (`sfx_loan_issued.wav`), non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass. (ref: `architecture/game-design/economy-model.md` Phase 10 Audio Callbacks section)

- [ ] **`sfx_power_out` wiring** (`sound-dev-opensoftal`): `CitySimulation::tick()`, inside the power coverage evaluation pass, calls `m_audio->playSound(SFX_POWER_OUT, SoundPriority::NORMAL, 1.0f)` when a zone tile transitions from powered to unpowered for the first time in a coverage cycle (either due to a brownout BFS-depth reduction or a full power plant loss). A per-tile `tile.wasPowered` bool gates the call: fires when `tile.wasPowered == true` and the current coverage BFS result is uncovered; sets `tile.wasPowered = false` immediately after the audio call. The flag is restored to `true` (silently, no SFX on recovery) when coverage is restored. Fires once per coverage-loss event per tile — not once per tick while the tile remains unpowered. Class: `CitySimulation`. Guard: `if (m_audio && tile.wasPowered && !currentlyCovered)`. `SFX_POWER_OUT` = SoundId 9 (`sfx_power_out.wav`), non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass. (ref: `architecture/game-design/service-coverage.md` Phase 10 Audio Callbacks section)

- [ ] **`sfx_water_out` wiring** (`sound-dev-opensoftal`): `CitySimulation::tick()`, inside the water coverage evaluation pass, calls `m_audio->playSound(SFX_WATER_OUT, SoundPriority::NORMAL, 1.0f)` when a zone tile transitions from water-covered to uncovered for the first time in a coverage cycle (either because the Water Tower degraded and halved its radius, or because the tower was demolished). A per-tile `tile.wasWaterCovered` bool gates the call: fires when `tile.wasWaterCovered == true` and the current water coverage evaluation finds the tile uncovered; sets `tile.wasWaterCovered = false` immediately after the audio call. The flag is restored to `true` (silently, no SFX on recovery) when coverage is restored. This is the tile-level notification to the player that water service has been lost — it is distinct from and independent of the building-level `SFX_SERVICE_DEGRADE` that fires when the Water Tower building enters its degraded state. Fires once per coverage-loss event per tile — not once per tick while the tile remains uncovered. Class: `CitySimulation`. Guard: `if (m_audio && tile.wasWaterCovered && !currentlyWaterCovered)`. `SFX_WATER_OUT` = SoundId 10 (`sfx_water_out.wav`), non-positional (`AL_SOURCE_RELATIVE = AL_TRUE`), EFX bypass. (ref: `architecture/game-design/service-coverage.md` Phase 10 Audio Callbacks section)

- [ ] **`sfx_fire_alert` and `sfx_police_alert` wiring** (`sound-dev-opensoftal`): `CitySimulation::tick()`, within the per-tile desirability update pass, calls `m_audio->playPositionalSound(SFX_FIRE_ALERT, vec3{float(tile.tileX), 0.0f, float(tile.tileZ)}, SoundPriority::CRITICAL, 1.0f)` (or `SFX_POLICE_ALERT`) when `tile.desirability <= SimulationConstants::service_alert_desirability_threshold` (= 20) AND `!tile.alertFired`. The `tile.alertFired` flag is set to `true` immediately after the audio call and is reset to `false` when `tile.desirability` recovers above the threshold (to allow re-firing in a future crisis episode). If the tile is covered by both Fire Station and Police Station, Fire Station takes priority (only `SFX_FIRE_ALERT` fires — no double-fire). Class: `CitySimulation`. Guard: `if (m_audio && !tile.alertFired && tile.desirability <= SimulationConstants::service_alert_desirability_threshold)`. `SFX_FIRE_ALERT` = SoundId 11, `SFX_POLICE_ALERT` = SoundId 12. Both are positional (`AL_SOURCE_RELATIVE = AL_FALSE`), CRITICAL priority, NO EFX bypass. `SimulationConstants::service_alert_desirability_threshold = 20` added to `simulation_constants.h`. (ref: `architecture/game-design/service-coverage.md` Phase 10 Audio Callbacks section)

- [ ] **`sfx_intersection_tick` wiring** (`sound-dev-opensoftal`): `CitySimulation::tick()`, inside the traffic signal update loop, calls `m_audio->playPositionalSound(SFX_INTERSECTION_TICK, vec3{float(signal.tileX), 0.0f, float(signal.tileZ)}, SoundPriority::LOW, 1.0f)` each time a traffic signal completes a phase change (green→red or red→green). Pre-acquisition distance cull: before calling `AudioSystem::playPositionalSound`, check `distance(listenerPos, signalPos) > 80.0f` and skip the call entirely if true. Listener position is obtained from `m_renderer->getListenerPosition()` (or equivalent). Class: `CitySimulation` (with listener position queried from `IRenderer`). Guard: `if (m_audio && distance <= 80.0f)`. `SFX_INTERSECTION_TICK` = SoundId 16 (`sfx_intersection_tick.wav`), positional (`AL_SOURCE_RELATIVE = AL_FALSE`), `SoundPriority::LOW` (first evicted under pool pressure), no EFX bypass. (ref: `architecture/game-design/traffic-system.md` Phase 10 Audio Callbacks section, `architecture/audio-architecture/v1-audio-asset-manifest.md`)
- [ ] Audio crossfade unit tests in `tests/audio/`: `Crossfade_InterruptedFormula_NoDomainErrorAtBoundary` (verify `t_offset` at `current_gain_out=1.0` returns 0 and `current_gain_out=0.0` returns 1 with no arccos domain error); `StingerMilestone_OnlyAtCityRatingTransition_NotRawPopulation` (100K population without City Rating transition → stinger NOT triggered; 1K population at Village→Town transition → stinger triggered); `AudioStream_BarBoundary_UsesConsistentBuffersQueuedPerWake` (`AL_BUFFERS_QUEUED` read once per wake; same value passed to both `computeSamplesPlayed()` and `computeNextBarBoundary()`); `AudioStream_BarBoundary_StreamStart_NoFalseFire` (verify crossfade condition does not fire when `m_samplesQueued < buffersQueued * kSamplesPerBuffer` and `m_nextBarBoundary = 0`); `NotificationSFX_EFXBypass_DirectFilterSetToNull` (for each of the 10 non-positional notification-category SFX: verify `alSourcei(src, AL_DIRECT_FILTER, AL_FILTER_NULL)` is called on the acquired source before playback; for `sfx_earthworks` (positional exception — `AL_SOURCE_RELATIVE = AL_FALSE` but EFX bypass required per manifest): verify bypass is applied and `AL_SOURCE_RELATIVE` remains `AL_FALSE`; verify `sfx_fire_alert` and `sfx_police_alert` do NOT have EFX bypass applied). CMake wiring: add `target_sources(audio_tests PRIVATE tests/audio/crossfade_interrupted_formula_test.cpp tests/audio/stinger_milestone_test.cpp tests/audio/audio_stream_bar_boundary_test.cpp tests/audio/notification_sfx_efx_bypass_test.cpp)` — do NOT call `add_executable(audio_tests ...)` or `aitown_add_tests(audio_tests ...)` again (would create duplicate target).
- [ ] **UIManager City Rating milestone callback**: `onCityRatingTransition()` handler calls `m_audio->triggerStinger(StingerType::MILESTONE)` on City Rating tier transitions (Village→Town at 1K / Town→City at 10K / City→Metropolis at 50K / Metropolis→Megalopolis at 500K); 5-second per-type cooldown gate enforced via `m_lastMilestoneStingerFireTime`; raw population milestones do NOT trigger milestone stinger — only City Rating tier changes do; wired to `CitySimulation::onCityRatingTransition()` callback (ref: `architecture/game-design/game-progression-modes.md`, `architecture/audio-architecture/dynamic-soundscape.md`)
- [ ] Audio property tests using `rc::check` must print `// Reproduce with seed: 0x<hex>` on failure and add a fixed-seed regression test before closing the finding, per `architecture/testing/procedural-generation-seeds.md`. (ref: `architecture/testing/procedural-generation-seeds.md`)

### Exit Criteria

- Main menu music plays and transitions smoothly (1 s fade) into gameplay on start
- Music intensity crossfades fire at bar boundaries (within a maximum 371 ms late window — fires at next audio thread wake after bar boundary; crossfade NEVER fires early relative to bar boundary) without audible pops
- Ambient beds loop seamlessly using runtime seek-to-0 (no audible boundary artifact)
- Stingers duck music to 0.4 gain, not ambient beds; duck releases after all active stingers finish
- Vehicle engine audio blends correctly; no Doppler artifacts; culled beyond 150 m
- All V1 audio assets delivered with loudness targets verified by loudness meter
- All four ambient beds (`ambient_day`, `ambient_night`, `ambient_dawn`, `ambient_dusk`) delivered, each individually loudness-verified, and runtime seek-to-0 loop verified for each
- Zone loop DAW loopback verification sign-off documented for all three zone loop assets and committed to the repository (e.g. as `assets/audio/zone_loop_qa.md` or equivalent text file referencing each asset by name)
- Crossfade audibility pre-production gate: `assets/audio/crossfade_demos/crossfade_demo_calm_to_growth.wav`, `crossfade_demo_mainmenu_to_calm.wav`, and `crossfade_demo_qa.md` all committed to the repository; `crossfade_demo_qa.md` contains sign-off confirming no harmonic clashes and records the locked root key (A natural minor, Aeolian mode, root A) and per-stem bar counts; `assets/audio/crossfade_demo_day_to_night.ogg` committed and confirmed no jarring transition before ambient bed asset lock
- All WAV SFX from manifest wired to simulation and UI events; no V1 manifest asset undelivered. Specifically verified per wiring point: `sfx_build_place` fires from `CitySimulation::placeZone()` and `placeServiceBuilding()` on success; `sfx_build_demolish` fires from `CitySimulation::demolishTile()` on success; `sfx_road_build` fires from `CitySimulation::placeRoad()` on success; `sfx_earthworks` fires from `placeZone()`/`placeRoad()`/`placeServiceBuilding()` when `earthworksCostOverride > 0`; `sfx_zone_upgrade` fires from `CitySimulation::doDensityUnlockTick()` once per upgraded tile; `sfx_service_degrade` fires from `CitySimulation::tick()` on Fire Station, Police Station, or Water Tower building degradation RNG success; `sfx_budget_warn` fires from `CitySimulation::tick()` on first deficit-threshold crossing in a streak; `sfx_loan_issued` fires from `CitySimulation::tick()` on each loan issuance; `sfx_power_out` fires from `CitySimulation::tick()` on zone tile power coverage loss with `tile.wasPowered` transition guard; `sfx_water_out` fires from `CitySimulation::tick()` on zone tile water coverage loss with `tile.wasWaterCovered` transition guard; `sfx_fire_alert`/`sfx_police_alert` fire from `CitySimulation::tick()` on tile desirability ≤ `service_alert_desirability_threshold` (= 20) with `tile.alertFired` guard; `sfx_intersection_tick` fires from `CitySimulation::tick()` on traffic signal phase change, pre-culled at > 80 m; `ui_click` fires from `UIManager::onEvent()` Priority 5 on toolbar/sub-panel button press; `ui_toast` fires from `NotificationManager::postCritical()`/`postNormal()` when toast becomes visible; `ui_menu_open`/`ui_menu_close` fire from `UIManager::updateSubPanelVisibility()` on panel show/hide
- **All `audio_tests` unit tests** (including all five Phase 10 test cases in four source files: `Crossfade_InterruptedFormula_NoDomainErrorAtBoundary`, `StingerMilestone_OnlyAtCityRatingTransition_NotRawPopulation`, `AudioStream_BarBoundary_UsesConsistentBuffersQueuedPerWake` and `AudioStream_BarBoundary_StreamStart_NoFalseFire` [both in `audio_stream_bar_boundary_test.cpp`], `NotificationSFX_EFXBypass_DirectFilterSetToNull`) pass on both Linux and Windows CI without a real audio device
- **All `simulation_tests` unit tests** (including `AdaptiveMusicIntensity_StateDriven_UpdatesAudioSystem` added in the Music intensity interface deliverable) pass on both Linux and Windows CI
- Zone loop silence-floor CI gate (Check #21 in `validate_assets.py`) green before zone loop assets merge to main; gate verifies that ALL samples in the leading `ceil(44100 × 0.1) = 4410` samples AND ALL samples in the trailing 4410 samples are at or below −60 dBFS peak amplitude (two independent windows)
- Font assets `assets/fonts/hud_font.xml` AND `assets/fonts/hud_mono_font.xml` both load without error in `IrrlichtUIBackend` constructor on both Linux and Windows CI (no fallback warning logged for either file); numeric `IGUIStaticText` elements (treasury balance, population count, tax rates, percentages) apply `hud_mono_font.xml` via `setOverrideFont()` and render in monospace (ref: `architecture/ui-ux/hud-layout.md` Monospace Requirement — Path 2 selected)
- Sprite sheet (`assets/textures/ui/hud_sprites_ui.png`) present on disk: `m_spriteTextureReady` is `true` and toolbar buttons display sprite icons (not fallback letter text) in a rendered frame

### Team

| Role | Responsibility |
|---|---|
| `gamedesign-ux` | Font asset delivery (`assets/fonts/hud_font.xml` proportional sans-serif + `assets/fonts/hud_mono_font.xml` monospace for numeric readouts) |
| `graphics-artist-2d-texture` | Sprite sheet asset delivery (`assets/textures/ui/hud_sprites_ui.png`) |
| `sound-artist-opensoftal` | All V1 audio assets, loop authoring, loudness targeting, crossfade audibility test |
| `sound-dev-opensoftal` | Dynamic soundscape code, music intensity interface, music crossfade, duck state machine, vehicle engine, zone loops, all SFX wiring callsites in `CitySimulation` and `UIManager`/`NotificationManager`, `NotificationManager` audio constructor parameter |

### Dependencies

- Requires Phase 7 complete (AudioSystem, source pool, streaming infrastructure)
- Requires Phase 6 simulation (for time-of-day from `CitySimulation`, City Rating transitions for `stinger_milestone`, service degradation events for `sfx_service_degrade`/`sfx_fire_alert`/`sfx_police_alert`, budget events for `sfx_budget_warn`/`sfx_loan_issued`)
- Requires Phase 8 complete (UI event hooks for `ui_click`, `ui_toast`, `ui_menu_open`, `ui_menu_close`, and `sfx_build_place`/`sfx_build_demolish`/`sfx_road_build` placement callbacks)
- **`sfx_earthworks` requires Phase 6 simulation zone placement event callback**: `CitySimulation` must fire `IAudioSystem::playPositionalSound(SFX_EARTHWORKS, tile_position)` (or equivalent) when earthworks cost > 0 at zone placement time — this callback is delivered in Phase 6 and enables `sfx_earthworks` wiring in Phase 10. (ref: `architecture/audio-architecture/v1-audio-asset-manifest.md`)
- **Phase 4 SA-2/SA-3 crossfade demo gate**: crossfade audibility sub-gate A (major→major demo) and music bar count confirmation (SA-3) must be confirmed complete — either verified as delivered during Phase 4 or produced at Phase 10 start — before full music stem authoring begins. See the crossfade audibility deliverable above. This is a Phase 10 start gate for music production, not a Phase 10 exit gate.
- **`MusicIntensity` enum and `setMusicIntensity()` in `IAudioSystem`**: Phase 10 adds these. Any Phase that depends on music intensity wiring must use Phase 10 or later. The thresholds are defined in `architecture/game-design/economy-model.md` Music Intensity Tiers section: CALM = `budget_surplus_pct >= 0%`; GROWTH = net population increase this tick; CRISIS = `consecutive_deficit_months >= 2`. These thresholds are final and locked.
- **`nvcompress` must be available in the development environment for sprite sheet authoring** — it is not installed by default on all workstations. `graphics-artist-2d-texture` must confirm `nvcompress` is available before beginning sprite sheet conversion. On Linux: install from the NVIDIA Texture Tools package or build from source. On Windows: download NVIDIA Texture Tools Exporter and add `nvcompress.exe` to PATH. Compressonator must NOT be used as a substitute for this specific asset.
- **`NotificationManager` constructor change**: Phase 10 adds `IAudioSystem*` as a fourth constructor parameter (`NotificationManager(IUIBackend*, ICitySimulation*, IClock*, IAudioSystem*)`). All existing test fixtures that construct `NotificationManager` directly must be updated to pass a `NiceMock<MockAudioSystem>` (or null pointer if the test does not exercise toast audio). `MockAudioSystem` is in `tests/simulation/mock_audio_system.h`.
- **`SimulationConstants::service_alert_desirability_threshold = 20`**: Added to
  `src/simulation/simulation_constants.h` alongside `service_uncovered_desirability_penalty_per_tick`
  and `service_recovery_desirability_per_tick`. No action required — use
  `SimulationConstants::service_alert_desirability_threshold` at all call sites; do not hardcode
  the literal `20`.
- **`tile.wasPowered`, `tile.wasWaterCovered`, `tile.alertFired`**: All three per-tile bool
  fields are declared in `CitySimulation::TileData` (`src/simulation/CitySimulation.h`).
  Defaults: `wasPowered = true`, `wasWaterCovered = true`, `alertFired = false`. No further
  action required — fields are ready for Phase 10 wiring. All three must be serialised in the
  Phase 11 save system (see spike note in Risks section).
- **`CitySimulation::doDensityUnlockTick()`**: Confirmed implemented in
  `src/simulation/CitySimulation.cpp` and declared in `src/simulation/CitySimulation.h`.
  `sfx_zone_upgrade` fires from within this method once per upgraded tile. No action required.
- **`IRenderer::getListenerPosition()`**: Already present in `src/interfaces/IRenderer.h`
  and implemented in `src/rendering/IrrlichtRenderer.cpp`. `MockRenderer` already has the
  corresponding `MOCK_METHOD`. Use `m_renderer->getListenerPosition()` directly in the
  `sfx_intersection_tick` 80 m pre-cull in `CitySimulation::tick()`.
- **3D model assets — no blocker for Phase 10 start** (`graphics-artist-3d-model` review, 2026-03-04):
  All positional SFX in Phase 10 (`sfx_fire_alert`, `sfx_police_alert`, `sfx_build_place`,
  `sfx_build_demolish`, `sfx_road_build`, `sfx_earthworks`, `sfx_intersection_tick`) fire at
  tile-coordinate positions (`vec3{tileX, 0.0f, tileZ}`) derived from simulation state, not
  from rendered scene node positions. Vehicle engine SFX (`sfx_vehicle_engine_idle`,
  `sfx_vehicle_engine_move`, `sfx_vehicle_horn`) use vehicle agent positions managed by
  `AudioSystem`, not scene node transforms. Zone loop positional sources use zone centroid world
  positions from the simulation, not from rendered building scene nodes. Phase 10 audio wiring
  is therefore fully decoupled from service building and zone building 3D model delivery. No
  3D model asset is on the Phase 10 critical path. Service building models
  (`svc_fire_station`, `svc_police_station`, `svc_power_plant`, `svc_water_tower`) remain
  Phase 9 deliverables; their absence does not block Phase 10 start or exit. The service
  building model spec (naming convention, LOD budgets, atlas cell, collision mesh) is now
  documented in `architecture/asset-standards/3d-model-standards.md` under Service Building
  Model Standards.

### Risks & Spikes

- **RISK**: Beat-boundary crossfade accuracy (±1 buffer period ≈ ±371 ms) may be audibly noticeable at slow tempos. **Spike**: test with 90 BPM stems at 1× speed; verify crossfade lands within the first beat of a bar.
- **RISK**: Interrupted crossfade `t_offset` formula uses arccos — verify no domain error when `current_gain_out` is exactly 0.0 or 1.0. **Spike**: unit test interrupted crossfade at t=0 and t=1 boundaries.
- **RISK**: `AL_BUFFERS_QUEUED` read order relative to `alSourceUnqueueBuffers` is critical — reading after unqueue produces underestimated queue depth and premature crossfade fire. **Spike**: add the `AudioStream_BarBoundary_UsesConsistentBuffersQueuedPerWake` test before any music integration to enforce ordering.
- **RISK**: Day→night direct ambient crossfade may sound unnatural — the day bed's energetic tail may clash with the night bed's quiet opening at the 3 s crossfade overlap. **Spike**: produce the `crossfade_demo_day_to_night.ogg` demo early in ambient bed production (before mastering the final 90–120 s beds) so that structural tail/head content decisions can be made while rework cost is still low. If the overlap sounds harsh, adjust the day bed's tail energy downward in the final 10 s and/or adjust the night bed's head to have a gentle upward entry rather than an immediate full-level start.
- **RISK**: `nvcompress` RGBA byte-order guarantee vs. platform-specific build variation — the `-rgba -nomips` flags are documented for NVTT 2.x. Verify the installed nvcompress version and its RGBA byte order output before committing the sprite sheet. **Spike**: create a 2×2 red pixel test PNG (`R=255, G=0, B=0, A=255`), convert with `nvcompress -rgba -nomips red_test.png red_test.dds`, then load and inspect the DDS pixel value — it must read as R=255, G=0, B=0, not B=255, G=0, R=0. If the byte order is wrong, the nvcompress version is non-standard and an alternative lossless DDS-to-PNG pipeline must be used.
- **RISK**: `hud_mono_font.xml` and `hud_font.xml` may have mismatched x-heights, causing visual jarring in the resource bar where proportional label text and monospace numeric text appear on the same row. **Spike**: render a sample resource bar string such as "Treasury: $123,456" using both fonts side-by-side in a test render; if cap-height differs by more than 2 physical pixels at 1280×720, select fonts from the same type family (e.g. Noto Sans + Noto Sans Mono) which share x-height by design.
- **RISK**: `sfx_intersection_tick` fires on every signal phase change across the entire map; at high city scales (hundreds of intersections) this could generate hundreds of `playPositionalSound` calls per simulation tick, causing SFX pool churn. **Spike**: profile at 500+ intersection count; if pool eviction rate exceeds acceptable threshold, add a per-simulation-tick cap (e.g., at most 8 `sfx_intersection_tick` acquisitions per tick, prioritised by closest to listener). The 80 m pre-acquisition cull already provides the primary throttle.
- **RISK**: `sfx_zone_upgrade` fires once per upgraded tile during a density upgrade wave; a wave upgrading 200+ tiles (at 20% of a large city) could produce 40+ non-positional sounds simultaneously. **Spike**: verify AudioSystem handles rapid same-frame `playSound()` calls without crashing (each call acquires an independent pool slot — if pool is exhausted, excess calls are silently dropped per priority eviction policy; LOW/NORMAL priority is first evicted). If rapid upgrade sounds are perceived as a jarring burst, add a 100 ms per-type cooldown in `AudioSystem::playSound()` for `SFX_ZONE_UPGRADE`.
- **RISK**: `tile.alertFired` flag is a new per-tile bool that must be serialised in the save file. If Phase 10 ships before the save system (Phase 11) is complete, `alertFired` may not persist across save/load cycles, causing re-fire of service alerts on load. **Spike**: confirm `tile.alertFired` is included in the tile serialisation struct in the save system spec before Phase 10 exits. If the save system is not yet implemented, document the known gap and track it as a Phase 11 integration item.
